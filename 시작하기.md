# 통돌 Note — 시작하기

**단계 1 (대상자)** 까지 끝났습니다.
이 폴더의 코드는 모두 돌려서 확인한 것입니다.

---

## 1. Bun 설치 (한 번만)

**Bun**은 이 프로그램을 돌리는 도구입니다. Node.js 같은 것인데 더 빠르고,
데이터베이스와 실행파일 만들기가 안에 들어 있어서 따로 설치할 게 적습니다.

윈도우 **PowerShell**을 열고 아래 한 줄을 붙여넣으세요.

```powershell
powershell -c "irm bun.sh/install.ps1 | iex"
```

설치가 끝나면 **PowerShell을 껐다가 다시 켠 뒤** 확인합니다.

```powershell
bun --version
```

---

## 2. 준비물 내려받기 (한 번만)

**반드시 이 폴더 안에서** PowerShell을 열어야 합니다.

```powershell
cd "$HOME\Desktop\care-saas\tongdol-note"
bun install
```

---

## 3. 실행하기

### 실제 모습으로 볼 때 — 창 하나

```powershell
bun run start
```

브라우저가 자동으로 열립니다. **사용자가 보게 될 모습이 이것입니다.**

### 개발할 때 — 창 두 개

고칠 때마다 화면이 바로 바뀌는 방식입니다.

**첫 번째 창** (서버) `bun run dev`
**두 번째 창** (화면) `bun run dev:web`
→ 브라우저에서 **http://localhost:5173**

### 실행파일로 만들기

```powershell
bun run compile
```

`통돌Note.exe` 가 만들어집니다. 이 파일과 `dist-web` 폴더를
같이 옮기면 다른 PC에서도 Bun 설치 없이 돌아갑니다.

---

## 4. 단계 1에서 확인할 것

처음 켜면 **기관 개설 화면**이 나옵니다. 기관명과 관리자 계정을 만들면
바로 대상자 화면으로 들어갑니다.

합격 기준은 이것 하나였습니다.

> 손으로 대상자 5명을 넣고, 이름으로 검색되고,
> 한 명을 「중단」으로 바꾸면 90일 뒤 날짜가 표시된다.

직접 해 보실 순서

1. **대상자 5명 등록** — 오른쪽 위 「+ 대상자 등록」
2. **찾기 칸에 이름 일부를 입력** — 연락처·주소로도 찾아집니다
3. **읍면동·상태로 거르기**
4. **한 명을 눌러 「상태 바꾸기」 → 「중단」 → 적용**
   → *「중단」으로 바꿨습니다. 종결 검토 기한은 ○○○○-○○-○○ 입니다.*
   목록으로 돌아가면 **「2026-11-20 (90일 남음)」** 처럼 보입니다.
5. **설정** — 기관 정보, 화면 밝기, 단가표, 본인부담 구간

### 나이가 자동으로 맞는지

제미나이 코드는 나이를 `2026 - 출생연도`로 박아 두어서 해가 바뀌면
전원이 한 살씩 틀리게 됩니다. 이번 것은 생일이 지났는지까지 따져
**만 나이**를 냅니다. `1938-04-11` 을 넣고 오늘 기준으로 맞는지 보시면 됩니다.

---

## 5. 무엇이 들어 있나

### 자료함 (표 21개)

단계 1에서 화면이 붙은 표는 **기관 · 지자체 프로필 · 서비스 · 본인부담 구간 ·
계정 · 대상자 · 상태이력 · 동의** 입니다.
나머지 표(의뢰·제공인력·배정·실적·청구·규칙변경이력·출력이력)는
**미리 만들어만 두었습니다.**

> 표를 나중에 *추가*하는 건 쉽지만 *바꾸는* 건 화면을 다 뜯어야 합니다.
> 그래서 구조는 지금 다 잡아 두고 화면만 단계별로 붙입니다.

### 「복지부 지침 표준」 프로필

처음 켤 때 **2026년 의료·요양 통합돌봄 사업안내** 기준 단가가
자동으로 들어갑니다. 설정 화면에서 확인하실 수 있습니다.

| 코드 | 서비스 | 단위 | 단가 | 휴일 |
|---|---|---|---|---|
| DL-HOUSE | 가사지원 | 시간 | 24,000 | 36,000 |
| DL-MEAL | 식사지원 | 식 | 10,100 | 13,500 |
| DL-MOVE | 동행지원 | 시간 | 17,000 | 25,500 |
| DL-BATH | 방문목욕 | 회 | 77,000 | 115,500 |
| DL-HAIR | 이미용서비스 | 회 | 24,000 | — |
| HO-SAFE | 안전생활환경개선 | 회 | 견적 | 생애한도 200만 |

본인부담 구간은 **면제 / 20% / 100%** 세 가지입니다.

---

## 6. 폴더 구조

```
tongdol-note/
├── server/                  기관 PC에서 도는 부분
│   ├── index.ts             서버 — 127.0.0.1 에만 귀를 엽니다
│   ├── db.ts                자료함 열기 · 최초 설치 판단
│   ├── schema.ts            표 구조 (표 21개)
│   ├── seed.ts              복지부 지침 표준 단가
│   ├── auth.ts              비밀번호 · 로그인 유지
│   ├── util.ts              날짜 · 나이 계산
│   └── api.ts               화면이 부르는 주소들
├── web/                     화면
│   ├── index.html
│   └── src/
│       ├── main.tsx
│       ├── App.tsx          로그인 여부에 따라 화면을 고릅니다
│       ├── theme.ts         밝게/어둡게
│       ├── style.css        색은 여기 한 곳에서만
│       ├── lib/
│       │   ├── api.ts       서버와 이야기하는 창구
│       │   └── router.ts    작은 길잡이
│       ├── components/
│       │   ├── Layout.tsx   왼쪽 차림표 + 본문
│       │   ├── RecipientForm.tsx
│       │   └── Deadline.tsx 90일 기한 표시
│       └── pages/
│           ├── Setup.tsx    최초 기관 개설
│           ├── Login.tsx
│           ├── Recipients.tsx
│           ├── RecipientDetail.tsx
│           └── Settings.tsx
├── data/                    ← 자료함이 여기 생깁니다 (백업 대상)
└── dist-web/                ← 묶인 화면 (자동 생성)
```

---

## 7. 왜 이렇게 만들었나

**서버가 `127.0.0.1` 에만 귀를 엽니다.**
바깥에서 이 PC로 들어올 수 없고, **윈도우 방화벽 경고가 뜨지 않습니다.**

**화면도 이 프로그램이 직접 내줍니다.**
"화면은 우리 서버에서, 데이터는 기관 PC에서" 방식이면
크롬이 **로컬 네트워크 접근 경고**를 띄웁니다. 같은 곳에서 내주면 없습니다.

**대상자의 민감한 정보는 `payload` 칸 하나에 몰아 담습니다.**
성명·생년월일·연락처·주소·좌표가 전부 이 칸 하나의 JSON입니다.
나중에 암호화할 때 **이 칸만 암호문으로 바꾸면 전환이 끝납니다.**
대신 검색·필터에 쓰는 값(읍면동·상태·검색문자열)은 밖에 따로 두었습니다.
암호화하면 검색이 안 되기 때문입니다.

**비밀번호는 되돌릴 수 없는 형태로 저장됩니다.**
Argon2 방식입니다. 자료함 파일을 통째로 가져가도 비밀번호는 못 읽습니다.

**로그인 흔적은 30일 뒤 사라집니다.**
프로그램을 켤 때마다 만료된 것을 지웁니다.

**상태를 바꾼 기록은 지워지지 않습니다.**
「이용 → 중단 → 이용」이 전부 남습니다. 나중에 지자체가 물어볼 때
기관이 내밀 수 있는 것이 이 이력입니다.

---

## 8. 다음 단계

**단계 2 — 의뢰 인박스.** 지자체가 보내주는 대상자 명단 엑셀을 올리면
차수별로 대사(對査)해서 **신규 / 유지 / 변경 / 삭제**를 가려내는 화면입니다.
해남에서 실제로 받으신 파일 3개 시트로 맞춰 볼 예정입니다.

시작하기 전에 **단계 1을 직접 돌려보고 걸린 시간과 막힌 곳을 적어 두세요.**
