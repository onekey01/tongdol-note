# 통돌 Note — 남은 일

> **앞으로 할 일은 `할일.md` 를 보세요.** 이 파일은 지금까지 무엇을
> 어떤 이유로 정했는지 남긴 **기록**입니다. 두 파일이 어긋나면 `할일.md` 가 맞습니다.

이야기 나누다가 "나중에 하자"로 미룬 것들입니다.
그 단계가 되면 여기부터 봅니다.

마지막 정리: 2026-08-28

---

## 지금까지 된 것

- **단계 0** 뼈대 — 서버·화면·자료함 연결
- **단계 1** 대상자 — 등록·검색·상태·90일 기한, 대리인 2명, 방문 전 확인(반려동물·CCTV), 식사 연속 미수령
- **단계 2** 의뢰 접수 — 암호 걸린 엑셀 읽기, 전체 시트 대사, 관리자 확인 목록
- 곁가지 — 기관 직인, 영수증 출력, 해남군 규칙(100원 절삭), 한글 서식(.hwpx) 채우기

---

## 단계 3 — 제공인력 · 배정

### 사람 관리를 하나로
`app_user` 에 이미 role(admin / staff / worker / vendor)이 있습니다.
**사람은 하나, 역할은 여러 개** 로 갑니다. 관리자가 제공인력을 겸하는 경우가 있는데
따로 관리하면 한 사람이 두 곳에 등록됩니다.

- [x] 관리자 정보 수정 (담당자가 바뀌면 지금은 손쓸 방법이 없음)
- [x] 계정 추가·비밀번호 재설정·퇴사 처리
- [x] 제공인력 등록 — 자격·교육·배상책임보험 만료일
- [x] 대상자별 지원인력 배정

### 임금·근태 관리 제외 체크박스
이미 채용된 직원이 이 사업을 거드는 경우가 있습니다.
그 사람 인건비를 이 사업으로 계산하면 정산이 틀어집니다.

- [x] `worker` 에 「별도관리」 칸 하나
- [ ] **제외해도 제공 실적과 일지는 그대로 쌓입니다.** 임금 계산에서만 빠집니다.
      실적까지 빠지면 기록지에 구멍이 납니다.

### 현장앱
- [ ] 제공인력이 보는 화면 — 대상자명·주소·연락처·서비스명 4가지는 반드시
- [ ] 방문 전 확인(반려동물·CCTV·주의) 그대로 노출
- [ ] QR 스캔 + 위치 오차반경 기록
- [ ] 위치정보 동의는 계약 단위. 퇴사 처리하면 자동 철회
- [ ] 안드로이드·아이폰 설치 설명서 (QR 또는 PDF)

---

## 단계 4 — 정산 · 출력

- [x] 제공실적 입력 (시간형·식사형·공사형)
- [x] 청구서·영수증에 실제 금액 (지금은 견본 숫자)
- [x] `calc_snapshot` — 청구할 때 단가·부담률·반올림을 통째로 얼려 넣기
      **과거 청구가 나중에 바뀌면 안 됩니다**
- [x] 생애한도 잔여 표시 (안전생활환경개선 200만원 등)
- [x] 설정에서 **서비스 신규 등록** — 지침에 없는 서비스가 오는 경우
      (지금은 의뢰 접수에서 기존 서비스로 지정만 가능)
- [x] **한글 서식 엔진을 파이썬 → TypeScript 로 옮기기** (2026-08-24)
      `server/zip.ts` · `server/xml.ts` · `server/hwpx.ts`
      → 아무것도 안 고치고 저장하면 **1바이트도 안 다름**,
        같은 값을 넣으면 **파이썬이 만든 것(한글이 실제로 연 파일)과 같은 파일**
- [x] 월별 기록지 일괄 만들기 — `server/recordbook.ts` · 기록지 화면 (hwpx, 여러 명 zip)
- [x] 기록지 **PDF** — 한글이 인쇄로 만듭니다 (2026-08-27, 7칸 문제 해결)

---

### 한글 엔진 — 확인한 것 (2026-08-24)

| | 확인 | 결과 |
|---|---|---|
|1|읽고 아무것도 안 고치고 저장|49,017 → 49,017바이트. **1바이트도 안 다름** (서식·작성예시 둘 다)|
|2|XML 8조각 각각 다시 쓰기|전부 글자 하나까지 같음|
|3|**파이썬이 만든 것과 대조** (한글이 실제로 연 파일)|**같은 문서**|
|4|이상한 값 13가지 (빈칸·300자·40줄·`<&>`·이모지)|전부 통과|
|5|일부러 부숴 보기 (줄·표·칸·colPr 지우기)|5가지 모두 **점검에서 막힘**|

- [x] **한글에서 열어보기** — 2026-08-24 확인. 3개 모두 정상적으로 열림
      (한 장짜리 · 여러 장 나뉜 것 둘 다)

### PDF 엔진 — 확인한 것 (2026-08-24)

한글 서식(.hwpx)과 달리 청구서·계약서는 **PDF 로 직접 그립니다.**
원본 서식이 구형 `.hwp`(2진 파일)이라 그렇기도 하고, PDF 가 더 자유롭기도 합니다.

| | 확인 | 결과 |
|---|---|---|
|1|이상한 값 7가지 (빈칸·200자 주소·서비스 6종·특수문자·이모지·긴 메모)|청구서·계약서 **모두 통과**|
|2|한꺼번에 만들기|청구서 8명 8쪽 997KB · 계약서 7명 14쪽 1,020KB|
|3|브라우저에서 실제로 눌러 받기|3가지 다 정상|
|4|글꼴에 없는 글자|**「□」로 보이게** 함 (소리 없이 사라지지 않게)|

**겪은 것 — 다시 안 밟게 적어 둡니다**

- `pdf-lib` 의 `subset: true` 는 **한글 글꼴에서 글자를 통째로 없앱니다.**
  「통합돌봄 서비스 이용 본인부담금 청구서」 → 「통합돌봄 서비 서」.
  오류도 안 나고 글자를 복사하면 제대로 나옵니다 — **뽑아 보기 전엔 모릅니다.**
  → 글꼴을 **미리** 잘라 두고 `subset: false` 로 넣습니다
- 사람마다 PDF 를 만들어 이어 붙이면 **글꼴이 사람 수만큼** 들어갑니다 (3명에 2.9MB).
  → 한 문서에 쪽만 늘립니다 (3명 983KB)
- `billing.one()` 이 `copay`(금액)로 `copay`(부담률 정보)를 덮어쓰고 있었습니다.
  청구서에 「undefined (NaN%)」 가 찍혀서 알았습니다 → `copayTier` 로 이름을 갈랐습니다
- 쪽번호를 다는 함수는 **첫 쪽이 이미 열린 뒤에** 걸립니다 → 1쪽만 번호가 빠졌습니다

- [x] **청구서·계약서 한글/PDF 확인** — 2026-08-24. 3개 모두 정상

### 직인과 계약서 (2026-08-24 추가)

- **계약서 제공기관 쪽에 설정의 직인이 찍힙니다.** 이용자 쪽은 서명선만 —
  그 자리에서 직접 하시는 것이라 도장 자리를 미리 그려 두지 않습니다
- 직인이 없으면 점선 동그라미가 나옵니다. 손도장을 찍으시면 됩니다
- **대리인·비상연락처는 비어 있는 것이 정상입니다.**
  계약서는 초기상담을 가서 그 자리에서 씁니다 — 미리 알기 매우 어렵습니다.
  그래서 「—」를 안 찍고 **손으로 적을 자리를 넉넉히** 비워 뒀습니다 (칸 높이를 키웠습니다).
  서비스 종류·계약기간도 의뢰가 아직 없으면 빈칸으로 나갑니다

**직인에서 잡은 함정** — WEBP 그림을 직인으로 올릴 수 있었는데,
**PDF 에 넣을 수 있는 그림은 PNG·JPG 뿐**입니다.
설정 화면에는 잘 보이는데 청구서·계약서에는 안 찍히고 오류도 안 납니다.
→ 이제 어떤 그림을 올리셔도 **화면에서 PNG 로 바꿔** 저장하고,
   긴 변 600px 로 줄입니다 (배경 투명도는 그대로 살아남습니다).
   서버에서도 PNG·JPG 만 받도록 한 번 더 막았습니다.

### ⚠ 서식은 우리가 바꿀 수 있는 것이 아닙니다 (2026-08-24, 두 번 바로잡음)

**지자체에 내는 서류의 서식을 임의로 고치는 일은 없습니다.**

**첫 번째 잘못** — PDF 를 직접 그리면서 제목 띄어쓰기, 표 머리의 「(주/월/기타)」 누락,
줄 수, 칸 붙임, 바탕색을 바꾸고 **서식에 없는 작성일·서명란·직인까지** 넣었습니다.
서식 파일에서 글자와 칸 너비를 읽어 오도록 고쳤습니다.

**두 번째 잘못 — 이게 근본이었습니다.**
글자와 너비를 읽어 와도 **글꼴·선 굵기·여백·용지는 여전히 제가 정한 값**입니다.
아무리 비슷해도 **같은 서식이 아닙니다.**
**다시 그리는 방식 자체가 틀렸습니다.**

**지금 방식 — 그리지 않습니다**

```
서식 파일(.hwpx)  ← 지자체가 준 그 파일. 손대지 않습니다
      │
      │  칸에 값만 채움 (+ 용지 가로)
      ↓
   채운 .hwpx  ──[ 한글에게 시킴 ]──→  .pdf
```

PDF 는 **한글이 만듭니다.** 한글에서 「파일 → PDF로 저장」 을 누른 것과 같은 파일입니다.
사람이 하던 일을 그대로 대신할 뿐이라 **서식이 같고 확장자만 다릅니다.**

- `도구/한글PDF변환.ps1` — 한글을 불러 변환. 한 번 띄워 여러 개를 한꺼번에
- `server/hwp2pdf.ts` — 서버가 이걸 부릅니다
- **한글이 없는 PC 에서는 PDF 단추가 안 보입니다.** hwpx 를 받아 직접 저장하시면 됩니다

**용지를 가로로 돌립니다 — 이건 되돌리는 것입니다**

| | |
|---|---|
|표 폭|**256.68mm**|
|세로 A4 글자 폭|170.00mm → 표가 **86.7mm 잘림**|
|가로 A4 글자 폭|**256.99mm** → 여유 **0.31mm**|

서식이 **가로로 만들어졌는데 세로로 저장**된 것입니다.
현장에서도 담당자가 열 때마다 손으로 가로로 바꿔 인쇄하고 계십니다.
그래서 원래 모습을 되돌리는 것이고, **서식 파일이 아니라 내보내는 사본에만** 적용합니다.

**검사** — `bun 서식지킴시험.ts`

```
■ 1. 서식 파일 자체
  ★ 읽고 아무것도 안 고치고 저장하면 그대로 — 49017 → 49017 바이트
■ 2~4. 채운 문서 (서비스 6종 → 2장)
  ★ 이름표가 서식 글자 그대로 — 값 칸 말고는 한 글자도 안 바뀜
  ★ 표 생김새가 서식과 같음 (줄·칸·너비·가로세로 붙임)
  ★ 용지를 가로로 · 여백은 손대지 않음
  ★ 고르기 문구는 그대로, 표시만 ☑ 로
★ 손댄 것: 값 칸 채우기 · 용지 가로. 그 둘뿐입니다.
```

**서식 파일 자체는 처음부터 손대지 않았습니다.**
`care-saas\서식\` 의 원본과 프로그램 폴더의 것이 같은 파일입니다
(md5 `9677fd89…`, 49,017바이트).

- [x] **한글 PDF 변환을 PC 에서 확인** — 2026-08-27. 3개 파일 모두 정상,
      회차 데이터(「8. 12.(1회차)」)까지 확인
- [x] **경고창 0번** — 보안 승인 모듈로 해결 (아래)

### 기록지 두 벌 · 서비스 관리 (2026-08-24 추가)

**기록지는 한글본과 PDF 본 두 가지로 나갑니다.**
값은 `gather()` 한 곳에서 나오므로 **내용은 언제나 같습니다.**
**PDF 는 그 한글본을 한글이 그대로 변환한 것**이라 서식·장수가 같을 수밖에 없습니다.
(예전에는 PDF 를 따로 그렸는데, 그 방식으로는 같아질 수 없어 버렸습니다. 아래 참고.)

> 대조하다 두 번 헛다리를 짚었습니다. 적어 둡니다.
> 1. PDF 에서 글자를 도로 뽑는 `pdftotext` 는 **표의 칸을 보이는 순서대로** 읽어서
>    두 줄로 나뉜 칸 사이에 옆 칸 글자가 끼어듭니다. 문서 잘못이 아닙니다.
>    → **종이에 실제로 찍는 `drawText` 를 가로채** 무엇을 찍었는지 직접 모으는 쪽으로 바꿨습니다.
> 2. 한글본의 **첫 장만** 보고 「PDF 에만 있다」고 했습니다.
>    6번째 서비스와 상태변화·상세내용·비고는 **마지막 장**에 있습니다. 모든 장을 이어서 봐야 합니다.

**설정에서 서비스를 만들고 단가를 고칩니다.**

- 지침에 없는 서비스를 지자체가 의뢰해 와도 프로그램을 고치러 오지 않아도 됩니다
- **「복지부 지침 표준」은 못 고칩니다** — 기준선입니다. 우리 지자체 규칙에서만 고칩니다 (서버에서 막음)
- **지우는 길은 없습니다.** 실적·의뢰·배정이 이미 가리키고 있어서 지우면
  지난 기록지와 청구서가 「이름 없는 서비스」가 됩니다. 안 쓰는 것은 **「안 씀」으로 접어 둡니다**
- 여기서 만든 것은 **「기관이 만든 것」**으로 표시됩니다 — 나중에 지침이 바뀌면 이것부터 봅니다

확인한 것 —

| 시험 | 결과 |
|---|---|
|확정된 청구서가 있는 상태에서 단가 24,000 → 33,000|금액 **안 흔들림** (108,600 그대로)|
|다시 확정 → 단가만 33,000 → 40,000|**「단가 바뀜」 뜸**, 금액은 그대로|
|단가를 되돌림|「단가 바뀜」 사라짐|
|단가만 보내는 수정 (일부 PATCH)|이름·분류·단위·기록형태 **안 지워짐**|
|빈 이름 · 음수 단가 · 「만원」 같은 글자|**전부 막힘**|
|잠긴 규칙(복지부 지침 표준) 고치기|**막힘**|

### 프로그램 하나로 만들 때 (compile) — 밟은 것

- `bun build --compile` 로 만드는 **실행 파일 이름은 영문이어야 합니다.**
  `--outfile 통돌Note` 로 하면 프로그램 안에 넣어 둔 글꼴을 못 찾고
  `ENOENT reading "...통돌Note"` 로 죽습니다.
  → `tongdol-note` 로 만든 뒤 **탐색기에서 이름을 바꾸는 것은 괜찮습니다.**
- 글꼴 폴더가 없어도 됩니다 — 확인함. exe 안에 든 글꼴로 PDF 가 제대로 나옵니다
- `서식` 폴더(기록지 .hwpx)와 `dist-web`, `data` 는 exe 옆에 있어야 합니다

---

## 단계 5 — 홈 · 다듬기  ← **끝났습니다 (2026-08-27)**

### 홈 화면
로그인하면 오늘 처리할 것이 먼저 보이게.

- [x] 오늘 종결 검토 — 기간이 끝난 의뢰
- [x] 2주 내 종결 대상 (기간이 곧 끝나는 분)
- [x] 중단 90일 경과
- [x] 식사 연속 2회 미수령
- [x] 생애한도 임박 ← 아직. 통계 탭 만들 때 같이 붙입니다

### 그 밖에
- [x] 내부 메모를 **포스트잇 형태**로 → **메모장**이 됐습니다 (아래)
- [x] `서식시험` 폴더 정리 (`_치울것` 으로 옮김)
- [x] 종사자 **휴직** — 명단에는 남고 배정에서만 빠집니다
- [x] 배정 목록에 대상자 메모 한 줄
- [x] **바탕화면에서 바로 켜기** — `통돌Note 실행.bat` → `run.ps1`
      (자료함 하루 1회 백업 · 고친 것 있으면 화면 새로 짓기 · 브라우저 자동 열기)

### 메모장 — 여러 번 고쳐 지금 모습이 됐습니다 (2026-08-27)

오른쪽 가장자리 **「메모장」** 탭. 서랍처럼 열리고 본문을 덮지 않고 밀어냅니다.

- 어느 보드인지는 **화면이 정합니다** — 홈이면 기관, 대상자 화면이면 그 어르신.
  기록지에서 이름을 누르면 그 어르신 보드가 함께 열립니다(상세내용·비고 적을 때 보라고)
- **메모지를 누르면 바로 고쳐집니다.** 「고침」 단추 없음. 끌면 옮겨지고, 모서리로 크기 조절
- **겹치지 않습니다** — 부딪히면 움직이는 쪽이 멈추거나 비켜섭니다(서버가 한 번 더 막음)
- **떼기** → 아래 「뗀 메모」(휴지통)로. **30일 뒤 저절로 사라집니다.** 사흘 안쪽은 붉게.
  다시 붙이면 **보드 맨 아래**로 갑니다(옛 자리로 돌아가면 겹칩니다)
- **지금은 관리자만** 씁니다. 넓히려면 `perm.ts` 의 `"memo.use"` 한 줄
- 시험 — `bun 메모휴지통시험.ts` (30가지) · 브라우저 시험 38가지

---

## 맞춤돌봄케어에서 가져올 것

`참고_맞춤돌봄케어.md` 에 정리했습니다. 단계별로 옮기면 —

**단계 4 — 만든 것**

- [x] 제공실적 (계획 대비 · 미제공 사유 · 식사 연속 미수령 자동 셈)
- [x] 정산 (횟수 × 단가 · 본인부담 절삭 · **calc_snapshot 얼리기** · 수납 · 생애한도)

- [x] **hwpx.py 를 TypeScript 로 옮기기** ← 기관 PC에 파이썬이 없어서. 제일 큰 일이었습니다
- [x] 월별 기록지 일괄 생성 (HWPX · 여러 명이면 zip)

**단계 4 — 남은 것**

- [x] **PDF 엔진** (`server/pdf.ts`) — 한글 글꼴 포함, 표·줄바꿈·쪽나눔
- [x] **본인부담금 청구서** (`server/invoice.ts`) — 정산 화면에서 PDF
- [x] **서비스 제공·이용 계약서** (`server/contract.ts`) — 대상자 화면에서 PDF

- [x] **기록지 PDF** (`server/recordbook-pdf.ts`) — 한글본과 나란히
- [x] **설정에서 서비스 신규 등록 · 단가 수정**

**단계 4 — 남은 것**

- [ ] (없음 — 단계 5 로 넘어갑니다)

**단계 4 (정산·출력)**
- [x] 출력을 두 갈래로 — 「화면 그대로 인쇄」와 「지자체 보고자료 출력」
      → 목록은 `인쇄목록.md`. **통계 탭 만들 때 한 번에** 붙입니다
- [x] 계획 대비 실적 (제공인력별 · 대상자별)
- [~] ~~「미계획」 분류~~ — **없앴습니다.** 계획 요일이 아닌 날 방문도 그 주를 채운 것으로 셉니다
- [x] 년·월 옮겨 다니는 탭 (◀ 2026 ▶ · 1~12월)
- [~] ~~현황 표는 횟수 + 시간 두 축으로~~ — **안 만듭니다.** 지침에 소요시간 기준이 없습니다

**현장앱 (모바일) — 단계 5 다음. 잊지 말 것**

지금 만든 **제공실적 화면은 사무실에서 보고 고치는 자리**입니다.
실제로 「오늘 다녀왔다」를 찍는 곳은 제공인력의 휴대폰이고, 그건 아직 없습니다.

| | 현장앱 (제공인력·휴대폰) | 제공실적 (관리자·PC) |
|---|---|---|
|무엇을|그날 방문을 **찍는다** (QR·위치·일지)|한 달치를 **보고 고친다**|
|고칠 수 있는 것|**일지 글만.** 제공·미제공 판정은 못 바꿈|**전부.** 지난 날짜도 바로잡음|
|보이는 범위|자기에게 배정된 대상자만|전부|

- [ ] 제공인력 로그인 → 오늘 갈 곳 목록
- [ ] 방문 찍기 (QR + 위치 오차반경) → 제공/미제공
- [ ] 일지 작성 · **일지만 나중에 수정 가능**
- [ ] 사진·서명 첨부
- [ ] 오프라인에서 찍고 나중에 올리기 (시골은 전파가 끊깁니다)

**단계 5 (홈·다듬기)** — 끝났습니다
- [x] 화면 맨 위 상시 숫자띠 → 홈 화면에 크게 (이용/중단/종결/전체)
- [x] 배정 목록에 대상자 메모 한 줄 (「청각장애로 전화통화 불가」 같은 것)
- [x] 종사자 고용상태에 **휴직** 추가 — 로그인은 두고 배정에서만 뺍니다

---

## 접근권한 (v7 에서 붙였습니다)

역할별로 손이 닿는 범위를 `server/perm.ts` 한 곳에서 정합니다.
화면은 이 표를 받아 차림표를 감출 뿐이고, **진짜 잠금은 서버에 있습니다.**

| |관리자|사무직원|제공인력|외부업체|
|---|---|---|---|---|
|대상자 보기|전부|전부|배정된 사람만|배정된 사람만|
|대상자 고치기|○|○|✕|✕|
|의뢰 접수|○|○|✕|✕|
|종사자 명단|전부|전부|본인만|본인만|
|계정·역할·계약종료|○|✕|✕|✕|
|설정 보기 / 고치기|○ / ○|○ / ✕|✕|✕|

남은 것

- [x] 제공인력이 자기 대상자를 보려면 **배정**이 먼저 있어야 합니다 (단계 3)
- [ ] 누가 언제 무엇을 열어 봤는지 기록 (지자체 점검에서 물을 수 있음)

---

## 정해진 것 — 지자체 확인 완료 (2026-08-27)

오래 매달려 있던 네 가지에 답이 나왔습니다. **여기서부터는 이 답이 기준입니다.**

### 1. 종결·중단은 **별도 통보가 없습니다**

공문도, 명단 표시도, 전화도 없습니다. → **지금 방식이 맞았습니다.**
기간이 끝난 것을 홈에서 알려 주고 **사람이 보고 판단**합니다.
프로그램이 저절로 종결시키지 않는 것도 그대로 둡니다 — 통보가 없으니
프로그램이 앞질러 종결시키면 살아 있는 건을 죽입니다.

- [x] 할 일 없음. 확정.

### 2. 기간이 겹치는 의뢰 — **둘 다 유효**, 단 **재의뢰 표시**

4.21~6.30 이 있는데 5.11~7.17 이 새로 와도 **덮지 않고 둘 다 삽니다.**
다만 **지자체가 못 걸렀을 수도 있어서** 눈에 띄게 표시해야 합니다.

- [x] 같은 대상자 · 같은 서비스에 **기간이 겹치는 의뢰**가 들어오면 「재의뢰」로 표시
- [x] 의뢰 인박스와 대상자 화면에서 보이게
- [ ] **지우거나 덮지 않습니다.** 표시만 하고 판단은 사람이

> 지금 코드의 「재의뢰」는 **종결·중단이었다가 다시 온 것**만 잡습니다(`inbox.ts`).
> 이용 중인데 기간이 겹쳐 들어오는 것은 아직 못 잡습니다. 그것이 이 할 일입니다.

### 3. 청구 시점 — **설정에서 고릅니다. 지자체 몫과 이용자 몫을 따로**

후불제에서 기관마다 갈립니다. 그리고 **둘이 서로 다를 수 있습니다** —
지자체에는 매달 올리면서 어르신께는 기간 끝나고 한 번에 받는 곳이 있습니다.
그래서 설정이 **한 칸이 아니라 두 칸**입니다.

**셋 중에 고릅니다** (2026-08-27 정함)

| 값 | 이름 | 어떻게 자르나 |
|---|---|---|
| `monthly` | **달마다** | 달력 달. 8월분은 8.1~8.31. 지자체 명단·기록지와 줄이 맞습니다 |
| `anniversary` | **시작일부터 한 달씩** | 5.11 에 시작했으면 5.11~6.10 이 1회차, 6.11~7.10 이 2회차 |
| `on_end` | **일괄** | 의뢰 기간이 끝나면 그동안 것을 한 장으로 |

**아직 안 끝난 구간은 확정하지 않고 「쌓이는 중」으로 보여 줍니다.**
얼마가 쌓였는지 모르면 사람이 불안해합니다. 보여는 주되 확정은 안 합니다.

- [x] 자료함에 자리 만들기 (2026-08-27, 스키마 v13)
      - `region_profile.claim_timing` — 지자체 청구
      - `region_profile.copay_timing` — 본인부담금
      - `billing.kind` — 이 종이가 무엇에 대한 것인가 (both | claim | copay).
        두 시점이 같으면 **한 장에 둘 다**(both, 지금까지와 같음),
        다르면 **갈라집니다**. 옛 청구서는 전부 both 로 채웠습니다
- [x] **구간 자르기** (`server/period.ts`) — 시험 34가지 통과
      - 달을 넘길 때, 해를 넘길 때, 윤년
      - **1월 31일의 한 달 뒤는 2월 31일이 아닙니다.** 그믐으로 자릅니다 —
        안 자르면 3월 3일로 넘어가 회차가 통째로 어긋납니다
      - **조각 사이에 틈이 없습니다.** 틈이 있으면 그날 실적이
        어느 청구서에도 안 들어갑니다
      - 종료일을 모르면 **오늘이 든 구간까지만.** 앞날은 미리 만들지 않습니다
- [x] 정산이 이 구간을 쓰도록 (`billing.ts`) ← **다음**
- [x] 「쌓이는 중」 표시 (정산 화면)
- [x] 설정 화면에 두 칸 — **로직이 끝난 뒤에 내놓습니다.**
      눌러도 아무 일 없는 칸을 먼저 보이면 안 됩니다
- [x] **이미 확정된 청구서는 안 흔들립니다** (`calc_snapshot` 이 지킵니다)
- [ ] → **관리자 사용설명서**에 이 설정의 뜻과 고르는 기준을 넣습니다

### 4. 한글 2010 이하 — **제약을 명시합니다**

형식을 다시 정하지 않습니다. `.hwpx` 를 못 쓰는 PC 에서는
**기능에 제약이 있을 수 있다고 안내문에 적어 둡니다.**

- [ ] 설치 안내문·설명서에 한 줄: 한글 2014 이상이 필요하고,
      그 아래 판에서는 기록지 한글본을 못 여는 제약이 있습니다
- [x] PDF 는 영향 없음 — 한글이 만들어 주므로 받는 쪽은 판을 안 탑니다

---

## 다음에 할 일 (2026-08-27 기준)

단계 0~5 는 끝났습니다. 여기서부터가 남은 몸통입니다.

### 0. 지금 손에 든 것 — 청구 시점 잇기

- [x] `billing.ts` 가 `period.ts` 의 구간을 쓰도록 (2026-08-27)
- [x] 두 시점이 다르면 청구서를 **갈라서** 발행 (`billing.kind`) (2026-08-28)
- [x] 정산 화면에 「쌓이는 중」 표시 (2026-08-28)
- [x] 설정 화면에 두 칸 (지자체 청구 · 본인부담금) (2026-08-27)

**정산 갈라 발행** (2026-08-28) — 서버 35가지 + 화면 22가지 시험 통과.

기관이 「지자체는 달마다, 본인부담금은 끝날 때 한 번」으로 정할 수 있습니다.
그러면 **묶는 기간 자체가 다릅니다** — 7월 지자체 청구는 7.1~7.31 인데
본인부담금은 5.11~8.10 을 통째로 받습니다.
**기간이 다른 두 금액을 한 장에 적으면 그 종이는 거짓말이 됩니다.**

- [x] 청구를 **몫**으로 나눔 — `both`(시점 같음) / `claim` / `copay`
- [x] 몫마다 **제 기간·제 상태·제 단추.** 지자체만 확정하고 본인부담금은 두는 것이 됩니다
- [x] **각 종이에는 제 돈만** — 지자체 몫의 본인부담금은 0, 반대도 0.
      두 장을 더해도 두 번 안 셉니다
- [x] `billing` 찾기·저장에 `kind` 를 넣음 — 기간이 우연히 같아도 안 섞입니다
- [x] **어르신께 드리는 청구서는 본인부담금 기간으로** 나옵니다
      (전에는 지자체 시점으로 잡아, 갈린 기관에서 엉뚱한 기간이 찍혔습니다)
- [x] **빈 종이는 안 만듭니다** — 그 구간에 실적이 없는 몫은 줄 자체를 안 냅니다
      (0회·0원짜리 줄이 붉은 「미확정」을 달고 앉아 있으면, 눌러도 막히는 단추입니다)
- [x] 「한꺼번에 확정」이 **몫 단위**로 돕니다 — 지자체 몫은 확정할 수 있는데
      본인부담금이 쌓이는 중이라고 함께 건너뛰면 안 됩니다
- [x] 사람 수와 **종이 수**를 따로 셉니다. 「한꺼번에 확정 (3)」은 종이 셋입니다
- [x] 「쌓이는 중」 — 상태 고르개에 넣고, 그 몫에는 **확정 단추를 아예 안 내놓습니다**
      (눌러 봐야 막히기만 합니다). 대신 언제 확정하면 되는지 적어 줍니다

### 0-1. 제공실적 **달력 UI** — 됐습니다 (2026-08-28)

가로로 1일부터 31일까지 늘어놓던 것을 **대상자별 달력**으로 바꿨습니다.
27가지 시험 통과.

- [x] 왼쪽 대상자 목록 + 오른쪽 그 사람의 한 달 달력
- [x] 공휴일 — 색 + **이름까지** (「15 광복절」). 토·일도 색으로
- [x] 달력 칸에서 바로 찍기 (계획 → 제공 → 미제공), 못 간 사유가 칸에 남음
- [x] 서비스가 둘이면 칸 안에 「가사 ●」·「식사 ✕」로 갈라 보임
- [x] 아직 안 채운 개수를 왼쪽 목록에 뱃지로 — 사람을 열어 보지 않아도 보임

**상태 탭** (2026-08-28) — 왼쪽 목록 위에 **「이용 · 중단 · 종결」 셋**.
**처음 열면 이용 중인 분만** 보입니다 (대상자 목록 화면과 같은 규칙).
29가지 시험 통과.

- [x] 위 숫자(계획·제공·미제공·아직)도 **탭을 따라갑니다.** 목록과 숫자가 서로 다른 말을 하면 안 됩니다
- [x] 「한꺼번에 제공 처리」는 **보이는 사람만** 채웁니다 (`assignIds` 로 명단을 집어 보냄)
- [x] 비어 있어도 탭 셋은 그대로 — 「중단 0」과 탭이 없는 것은 다릅니다

**말은 지침을 따릅니다** — 2026 지침 전수 검색: 「중지」 1건(그마저 「집중지원」의 일부),
**「중단」 32건**. 그래서 「중단」입니다.

**「못 감」도 「미제공」으로** (2026-08-28). 지침이 **「적정 제공여부(제공/미제공)」·
「미제공 사유」**로 짝을 이루고, 서식에도 그 말이 그대로 나옵니다.
「부재」로 부르자는 이야기가 있었는데 **부재는 미제공 사유 중 하나**라
(부재·거부·입원·본인요청·기관사정·기상재해·기타) 같은 말로 부르면
「부재 5건」이 못 간 것 전부인지 사유가 부재인 것인지 구별이 안 되고,
**입원으로 못 간 것까지 부재로 세어져 지자체에 틀린 말**을 하게 됩니다.

| 상태 | 어디에 | 왜 |
|---|---|---|
| 대기 | **탭 없음** | 제공인력이 안 붙은 분. 계획도 실적도 없어 빈 달력만 나옵니다. 명단은 홈에 있습니다 |
| 직권중단 | **종결 탭** | 서비스가 끝난 것은 같고 왜 끝났는지만 다릅니다 → 이름 옆에 표로만 |
| 취소 | **탭 없음** | 서비스가 아예 안 나간 분. 다만 실적이 있으면 「취소 n명은 목록에 없습니다」로 알려 줍니다 |

**시험이 이따금 실패하던 이유** — 시험이 공휴일을 표에 직접 꽂아서 서버의
30초 캐시가 안 비워졌습니다. 제품은 설정 화면에서 넣으면 그 자리에서
캐시를 버립니다(`공휴일캐시버리기()`). 시험을 **화면이 쓰는 길 그대로**로 고쳤습니다.

**가로 표는 없앴습니다.** 「아직 안 채운 사람 찾기」는 왼쪽 목록의
`제공 7/12` 와 뱃지가 대신합니다. 옛 손시험 스크립트(`/tmp/t18·t22·t23.mjs`)는
그 표를 보고 있어 지금은 안 맞습니다 — 다시 쓸 일이 있으면 고쳐야 합니다.

### 1. 현장앱 (모바일) — 제일 큰 것

관리자 화면은 **사무실에서 보고 고치는 자리**입니다.
「오늘 다녀왔다」를 찍는 곳은 제공인력의 휴대폰이고, **그건 아직 없습니다.**
이것이 없으면 실적을 여전히 손으로 옮겨 적어야 합니다 —
**종이를 없애자고 만든 프로그램인데 종이가 그대로 남습니다.**

- [ ] 제공인력 로그인 → **오늘 갈 곳 목록** (대상자명·주소·연락처·서비스명 4가지 필수)
- [ ] 방문 전 확인(반려동물·CCTV·주의) 그대로 노출
- [ ] **방문 찍기** — QR + 위치 오차반경 → 제공/미제공
- [ ] 일지 작성 — **일지 글만 나중에 수정 가능.** 제공·미제공 판정은 못 바꿈
- [ ] 사진·서명 첨부
- [ ] **오프라인에서 찍고 나중에 올리기**
- [ ] 위치정보 동의는 계약 단위. 퇴사하면 자동 철회
- [ ] 안드로이드·아이폰 설치 설명서
- [ ] 여기까지 되면 **권한을 다시 손봅니다** (`perm.ts`) — 메모장을 사무직원에게 열지도 여기서

### 2. 상담 기록 — **제1 기능인데 아직 없습니다**

`수익화.md` 에서 정한 것: **이 프로그램의 제1 기능은 상담 실무자용**이고
통합돌봄 보조는 제2 기능입니다. 그런데 **지금 있는 것은 제2 기능뿐**입니다.

통합돌봄 사업이 바뀌면 지금 만든 것의 상당 부분이 같이 흔들립니다.
상담 기록이 서 있으면 사업이 바뀌어도 쓰던 사람은 계속 씁니다.

- [ ] 위노트의 상담일지를 보고 우리 모양 정하기 (회기 기록 · 사례 관리)
- [ ] 대상자 하나에 상담 기록이 쌓이는 구조
- [ ] 무료로 열 범위 정하기

### 3. 통계 탭 + 인쇄 — 됐습니다 (2026-08-28)

41가지 시험 통과.

- [x] 년·월 옮겨 다니는 탭 (◀ 2026 ▶ · 한 해 · 1~12월).
      **아직 안 온 월은 눌러지지 않고**, 표에서도 0 이 아니라 가운뎃점(·)입니다.
      **한 월을 골라도 열두 칸은 그대로 있습니다** — 사라지면 다른 월로 넘어가려고
      「한 해」를 거쳐 돌아와야 합니다
- [x] 계획 대비 실적 — **서비스별 · 대상자별 · 제공인력별 · 행정동별** 네 보기를
      **탭으로**. 넷을 한꺼번에 펴 두었더니 대상자가 늘수록 한없이 길어졌습니다.
      기간은 위에서 고른 그대로입니다
- [x] **열 자리를 못박음**(`table-layout: fixed`) — 탭을 넘겨도 숫자 열이
      같은 자리에 섭니다. 안 그러면 이름 길이에 따라 숫자가 좌우로 튑니다
- [x] **못 간 사유** 세기 — 지자체가 「주 2회인데 왜 여섯 번뿐입니까」 물을 때의 답
- [x] **생애한도** — 임박(80%)·넘음. 홈에도 올라갑니다
- [x] **인쇄** — 브라우저 인쇄(`@media print`). 차림표·단추·메모장 빠지고
      선과 글자만. 머리글에 **기관명 · 화면 · 뽑은 때 · 뽑은 사람**(Layout 이 한 곳에서)

**정한 것 둘.**

1. **「시간」 축은 안 만듭니다.** 지침에 서비스별 소요시간이 없고, 그래서
   주간계획에도 시각을 안 넣기로 이미 정했습니다. 없는 값을 만들어 채우면
   그 숫자가 지자체 서류로 나갑니다. **시간은 현장앱이 붙어야 생깁니다.**
2. **돈은 통계에서 안 셉니다.** 금액은 정산 화면 하나가 셈합니다.
   두 곳에서 따로 세면 언젠가 서로 다른 말을 합니다.
   생애한도만 예외인데, 그것도 셈은 한 군데(`stats.ts`)서 하고 홈이 빌려 씁니다.
3. **「계획밖」을 없앴습니다** (2026-08-28). 지자체가 정한 것은 **횟수**(「주 2회」)이고
   요일은 그것을 어디에 놓을지 정한 **안내**입니다. 화·목 계획인데 수·목에
   다녀왔으면 그 주는 채운 것인데, 예전에는 그 수요일을 「계획밖(◆)」으로 갈라
   **채웠는데도 계획을 안 지킨 것처럼** 읽혔습니다.
   같이 고친 것 — **계획 수가 부풀던 버그.** 계획을 「깔린 칸 수」로 세다 보니
   계획 요일이 아닌 날에 다녀올 때마다 그 칸이 함께 들어와 「주 2회」가
   3회가 되었습니다. 이제 **계획 = 계획 요일 칸 수**(가야 할 횟수)입니다.
   24가지 시험(`계획셈시험.ts`).
4. **표에서 「아직」 열을 뺐습니다.** 옆의 「처리율」과 붙어 **「아직 처리율」**로
   읽혔습니다. 처리율이 100%가 아니면 그것이 곧 「아직 남았다」는 말이고,
   전체 「아직」 개수는 위 요약 칸에 그대로 있습니다.
5. **처리율 = (제공 + 못 감) ÷ 계획.** 못 간 것도 「처리한 것」에 넣습니다 —
   사유를 적어 두었으면 지자체에 답할 수 있으니까요. 제공만 세면 부재가 잦은
   어르신을 맡은 제공인력이 일을 안 한 것처럼 보입니다.
6. **쪽 번호는 브라우저가 붙입니다.** 인쇄 창의 「머리글 및 바닥글」을 켜야 하고,
   그건 우리가 대신 켤 수 없어서 화면에 그렇게 적어 두었습니다.

- [ ] 출력 두 갈래 중 「지자체 보고자료 출력」 — 기록지(HWPX·PDF)는 이미 있고,
      나머지는 서식이 없다는 것이 `인쇄목록.md` 결론입니다. 더 만들 것 없음
- [ ] 다른 화면들 인쇄 다듬기 (대상자 명단 · 종사자 명부 등) — 얼개는 다 깔렸습니다

### 4. 남에게 건네줄 수 있게 (배포)

지금은 **이 PC 에서만** 돕니다. 파일럿 기관에 넣으려면 —

- [ ] `bun build --compile` 로 exe (이름은 **영문**, 그 뒤 탐색기에서 바꾸는 건 괜찮음)
- [ ] 함께 넣을 것 — `서식` 폴더 · `dist-web` · **`도구/FilePathCheckerModuleExample.dll`**
      (DLL 을 빠뜨리면 40명에 경고창 80번이 뜹니다)
- [ ] 설치 안내문 한 장 — **한글 2014 이상 필요**하다는 제약도 여기에
- [x] 자료함 백업·되돌리기 — 설정의 **백업정보** (2026-08-28)
- [ ] 기능 잠금 자리 (`plan.ts`) · 오프라인 열쇠 — `수익화.md` 참고
- [ ] 누가 언제 무엇을 열어 봤는지 기록 — 지자체 점검에서 물을 수 있습니다

### 5. 관리자 사용설명서

- [ ] 청구 시점 세 가지의 뜻과 고르는 기준
- [ ] 한글·PDF 내보내기, 경고창이 뜰 때
- [ ] 백업정보 — 자리 정하기 · 주기 · **되돌리기** (USB 를 오갈 때 주의할 점 포함)

---

## 폴더 고르개 · 주소 찾기 (2026-08-28)

43가지 시험 통과 (`/tmp/세가지시험.mjs`).

### 백업 「따로 둘 곳」 — 눌러서 고릅니다

경로를 손으로 치면 `D;\` 같은 오타가 **몇 달 뒤에** 드러납니다.
직인처럼 파일 고르기로 하고 싶었는데, **폴더는 그렇게 못 고릅니다** —
브라우저가 보안상 고른 폴더의 **전체 경로를 안 알려 줍니다**
(`webkitdirectory` 도 `showDirectoryPicker()` 도 상대 경로·손잡이만 줍니다).

**그런데 우리 서버는 그 컴퓨터 안에서 돕니다.** 그래서 서버가 폴더를 훑어
목록으로 내주고(`/api/settings/backup/browse`, **읽기만**), 화면은 탐색기처럼
눌러 들어갑니다. 고르기 전에 **그 자리에 실제로 써 보고** 알려 줍니다.

- [x] 경로 칸은 `readonly` — 손으로 못 칩니다
- [x] 윈도는 A:~Z: 드라이브부터, 리눅스·맥은 `/` 와 내 폴더부터
- [x] 없는 폴더는 「이 이름으로 만들고 정하기」로 그 자리에서
- [x] 누르자마자 창이 뜹니다 — 자료를 기다리며 빈 화면이면 사람은 또 누릅니다

### 주소 찾기 — 포털에서 쓰시던 그 검색창

손으로 치면 같은 집이 사람마다 다르게 적힙니다(「해남읍 중앙1로 330」 ·
「해남군 해남읍 중앙1로 330」 · 「중앙1로330」). 그러면 **읍면동 거르기와
통계가 조용히 어긋납니다.** 게다가 찾아가는 사람이 보고 가는 주소입니다.

**카카오(다음) 우편번호 서비스** — 온 나라 포털·쇼핑몰이 쓰는 그 창을
그대로 띄웁니다. **무료이고, 신청도 승인키도 없습니다.**

> 처음에는 **행정안전부 개방 API**(juso.go.kr)로 만들었습니다. 되기는 됐지만
> **기관마다 승인키를 신청해서 설정에 넣어야 했습니다** — 승인키가 시스템마다
> 하나라 우리가 넣어 배포할 수가 없었기 때문입니다. 「포털에서 조회 누르면
> 나오는 그 창」이 원래 원하시던 것이었고, 그 창은 **키가 없습니다.**
> 그래서 `server/address.ts` · `/api/address` · 설정의 승인키 칸을 **전부 걷어냈습니다.**

- [x] **키도 신청도 없습니다.** 기관이 할 일이 없습니다
- [x] 도로명·지번·건물명·우편번호 **아무거나** 넣어도 찾습니다
- [x] **누르실 때** 꾸러미를 불러옵니다 — 첫 화면에서 부르면 인터넷 없는
      사무실에서 로그인 화면이 멎어 보입니다
- [x] 고르면 **읍면동도 함께** 채웁니다 (비어 있을 때만 — 사람이 고른 것이 우선).
      **읍·면·동으로 끝나는 것만** 받습니다 — 법정리(「명금리」)가 읍면동 칸에
      들어가면 그분은 어느 동에도 안 잡힙니다
- [x] 고른 뒤 **커서가 주소 칸 맨 뒤**로 갑니다. 검색창은 건물까지만 알려 주므로
      동·호수는 사람이 이어 적습니다 (포털이 상세주소 칸으로 옮겨 주는 것과 같은 뜻)
- [x] **안 되면 손으로 적는 길을 막지 않습니다.** 인터넷이 끊기면 사람 말로
      알려 주고 물러섭니다. 주소 칸은 그대로 열려 있습니다
- [x] **안 쓰는 승인키는 자료함에서 지웁니다**(`migrate()`). 쓰지도 않는 키가
      남아 있으면 백업이 USB 로 오갈 때 그것도 같이 오갑니다

**나가는 것은 직원이 친 검색어뿐입니다.** 대상자 이름·전화·생년월일은 이 창을
거치지 않습니다. **주소 찾기만 인터넷을 씁니다** — 나머지는 그대로 바깥 연결
없이 돕니다. 기관 망이 `daumcdn.net` 을 막으면 이 창은 안 열립니다(손입력은 됩니다).

**배운 것** — 겹창 클래스를 `.pick` 이라고 지었더니 프로그램이 이미 쓰던
`.pick`(테마 고르개 같은 단추 묶음)과 부딪혀 그 단추들이 620px 짜리 흰 상자가
되었습니다. `.memo-note` 때와 **똑같은 실수**였고 브라우저 시험이 잡았습니다.

**또 배운 것** — 남의 창을 앉힐 자리에 우리 글(「불러오는 중…」)을 같이 넣었더니,
남의 창이 들어오면서 그 글을 치웠고 React 가 「없는 자식을 지우려 했다」며
**화면째 죽었습니다.** 남에게 내준 자리에는 **우리가 아무것도 그리지 않습니다.**
`.dlg` 로 바꿨습니다. **새 이름은 무엇의 것인지 앞에 붙입니다.**

---

## 백업정보 · 되돌리기 (2026-08-28)

**떠 두는 것만으로는 절반입니다.** 되돌릴 수 없으면 없는 것과 같습니다.

- [x] 자리 정하기 (USB·외장하드·공유 폴더) — 정할 때 **실제로 한 번 써 보고** 확인
- [x] 주기 네 가지 · 보관 개수 — 화면이 **「며칠 전까지 되돌릴 수 있는지」**를 셈해 줌
- [x] `VACUUM INTO` — WAL 을 쓰므로 그냥 복사하면 안 옮겨진 것이 빠집니다
- [x] **우리가 만든 파일만** 지웁니다 (남의 폴더를 정했을 때 남의 파일을 건드리면 안 됨)
- [x] **되돌리기** — 견주기 → 예약 → 다음에 켤 때 바꿈 (33가지 시험 통과)

### 되돌리기가 세 걸음인 이유

1. **견주기** — 고르면 바로 안 바꾸고, 그 파일과 지금 것을 나란히 놓습니다
   (대상자·제공실적·청구서·메모·마지막 실적일). 줄어드는 칸은 붉게.
2. **예약** — 정하면 **지금 것을 먼저 한 부 떠 두고**(`tongdol-되돌리기전-…`)
   바꿀 파일을 `_복원예정.db` 로 옆에 놓습니다.
   이 안전 백업은 **자동 정리에서 뺍니다** — 무를 유일한 자리입니다.
3. **다음에 켤 때** — `server/db.ts` 가 **파일을 열기 전에** 바꿔치웁니다.
   돌아가는 도중에 발밑의 파일을 바꾸면 자료가 깨집니다.

### 정한 것 (2026-08-28) — **동기화는 현장앱 때 한꺼번에**

지금은 한 사람이 한 PC 에서 씁니다. 사무실 것을 아무도 안 건드리므로
USB 것으로 덮으면 됩니다. **다만 현장앱이 붙으면 그 전제가 깨집니다** —
제공인력이 휴대폰으로 찍으면 아무도 PC 앞에 없어도 자료가 바뀝니다.

지금 동기화를 못 만드는 이유 (자료 구조가 아직 못 받습니다) —

- **번호가 겹칩니다.** 대상자는 UUID 라 괜찮지만 제공실적·청구서·배정·
  제공인력·의뢰는 자동번호입니다. 두 컴퓨터가 각각 「103번 실적」을 만듭니다.
  `delivery.assignment_id` 가 어긋나면 **A씨 실적이 B씨 배정에 붙습니다.**
- **어느 쪽이 새 것인지 모릅니다.** `updated_at` 이 있는 표는 셋뿐
  (대상자·기록지메모·메모지). **돈이 걸린 표에는 `created_at` 뿐**입니다.
- **지운 것을 기록하지 않습니다.** 집에서 지운 대상자가 월요일에 되살아납니다.

그리고 **동기화가 틀리면 조용히 틀립니다.** 되돌리기는 안전 백업이 있지만
동기화는 오류창도 안 뜨고 숫자만 다릅니다. 그게 지자체 청구서로 나갑니다.

→ 현장앱이 붙으면 사무실 PC 가 서버가 되어 **복사본이 하나**가 됩니다.
  동기화는 복사본이 둘일 때만 필요한 기능이라, 지금 만들면 그때 버립니다.
  (기관 망이 바깥 접속을 막으면 이 길이 닫힙니다. 그때 다시 봅니다.)

### USB 를 오갈 때 — **합쳐지지 않습니다**

주말에 USB 로 집에서 일하고 오셨을 때,
**사무실 것을 아무도 안 건드렸다면** USB 백업으로 되돌리면 됩니다.
그런데 **양쪽 다 바뀌었으면 자동으로 합칠 방법이 없습니다.**
어느 쪽을 버릴지는 사람이 정해야 합니다 — 그래서 고르기 전에
양쪽 숫자를 나란히 보여 주는 것입니다.

→ 두 곳에서 각각 일하는 것을 제대로 받치려면 **동기화**가 필요하고,
  그건 백업이 아니라 **다른 기능**입니다. 지금은 하지 않습니다.

---

## 무엇을 팔 것인가 → `수익화.md`

제품 방향과 요금제는 **`수익화.md`** 에 따로 모았습니다 (2026-08-27 시작).
요약만 —

- **제1 기능은 상담 실무자용 프로그램**입니다 (레퍼런스: 위노트). 통합돌봄 보조는 제2 기능
- 대상자 등록·메모는 **무료**, 그것만 쓰면 **바깥 연결이 아예 없습니다**
- 통합돌봄 보조는 파일럿 이후 **유료**. 구매자가 **묶음을 골라** 삽니다
- 라이선스는 **오프라인 열쇠** — 인터넷이 끊겨도 기록지가 나와야 합니다

---

## 완성 뒤에

- [ ] 여기까지 만든 것을 바탕으로 **학습 커리큘럼** 만들기 (기초부터)

---

## 물어볼 것

- **월별 제공실적 · 대상자 명단 회신**의 지자체 서식 파일이 있는지
- 지자체가 **엑셀**도 받는지, 한글·PDF만 받는지
- 주간 배정표를 **벽에 붙여** 쓰시는지 (그러면 글씨를 키웁니다)

→ 인쇄 관련 물음은 `인쇄목록.md` 4장에 모여 있습니다.

정리된 것 (2026-08-23)

- 시각 입력 → **안 넣습니다.** 주간계획은 요일만. 지침에 서비스별 소요시간이 없습니다
- 중점 / 일반 / 긴급 → **통합돌봄에 없는 개념**
- 단체 프로그램 → **통합돌봄에 없는 개념**

---

## PDF 변환 — 2026-08-27 에 밝힌 것 (다시 헤매지 않도록)

**한글 2022 의 「PDF 로 저장」은 한 줄에 칸이 7개 이상인 표의 마지막 칸을 통째로 빠뜨립니다.**

- 기록지 서식의 **「직접 서비스 제공일」 칸**이 그래서 사라졌습니다.
- 우리가 만든 파일 탓이 아닙니다. **아무것도 안 채운 원본 빈 서식**과
  **한컴이 배포한 작성예시**도 똑같이 사라집니다. (직접 변환해 확인)
- 한글의 **화면·미리보기는 멀쩡합니다.** 한글이 스스로 다시 저장한 파일의
  미리보기 그림에도 7칸이 다 있습니다. **내보내기(PDF) 쪽만 문제입니다.**
- PDF 안을 뜯어보면 한글은 칸마다 잘라내기 영역을 하나씩 찍는데,
  7칸짜리 줄에서는 **6개만 찍고 7번째는 아예 안 찍습니다.**

**해 본 것 중 안 되는 길**

- 쪽 여백 줄이기(12mm·5mm) — 표가 왼쪽으로 밀릴 뿐, 그대로 사라짐
- 표 격자를 13열 → 14열로(겉모습 동일) — 그대로 사라짐
- 한글이 다시 저장한 hwpx, 옛 `.hwp` 를 거친 변환 — 그대로 사라짐

**되는 길 — 「인쇄」**

`SaveAs(…, "PDF")` 대신 **「Microsoft Print to PDF」로 인쇄**합니다.
종이에 뽑는 길이라 화면에 보이는 그대로 나옵니다. **서식은 손대지 않습니다.**

```
$set = $hwp.HParameterSet.HPrint
$hwp.HAction.GetDefault("Print", $set.HSet)
$set.PrinterName = "Microsoft Print to PDF"
$set.PrintToFile = 1
$set.FileName    = <나올 PDF 경로>
$hwp.HAction.Execute("Print", $set.HSet)
```

인쇄는 스풀러를 거쳐 나중에 끝나므로 **파일 크기가 멎을 때까지 기다려야** 합니다
(`Wait-ForFile`). 프린터가 없으면 옛 방식으로 물러서되, 칸이 빠진다고 알립니다.

## 한글 경고창 — 해결됨 (2026-08-27)

한컴 보안 승인 모듈로 없앴습니다. **경고창 0번** 확인.

- 파일: `도구/FilePathCheckerModuleExample.dll` (32비트, 한컴 공식 배포)
  받은 곳: `https://github.com/hancom-io/devcenter-archive` → `hwp-automation/보안모듈(Automation).zip`
- 등록: `HKCU\Software\HNC\HwpAutomation\Modules` 에
  **값 이름** `FilePathCheckerModuleExample`, **값** = DLL 전체 경로
  (`HwpCtrl\Modules` 에도 같이 넣어 둡니다)
- 코드: `RegisterModule("FilePathCheckDLL", "FilePathCheckerModuleExample")`
- **주의: 한글 2022 는 제대로 얹혔는데도 `RegisterModule` 이 False 를 돌려줍니다.**
  (한컴 개발자 포럼에 올라온 문제) 반환값으로 판단하면 안 됩니다.
- 모듈 소스를 보면 `IsAccessiblePath` 가 무조건 TRUE 를 돌려줍니다 —
  얹히기만 하면 창이 아예 안 뜹니다.
- **이 DLL 은 프로그램과 함께 배포해야 합니다.** 없으면 40명에 80번 창이 뜹니다.

### 알아 둘 것

- 한글 2022 는 **32비트**(`C:\Program Files (x86)\HNC\Office 2022\...`)이고,
  모듈도 32비트라 짝이 맞습니다.
- `powershell.exe` 를 **백그라운드(창 없는 자리)** 에서 부르면 한글이
  경고창을 못 띄워 `Open` 이 그냥 False 를 돌려줍니다. 모듈을 넣으면 이 문제도 없어집니다.
- PowerShell 의 `cd` 는 .NET 쪽 현재 폴더를 안 바꿉니다.
  스크립트 맨 위에서 `[System.Environment]::CurrentDirectory` 를 맞춰 둡니다.
- `.bat` 안에 **한글 경로를 쓰면 cmd 가 깨뜨립니다.** 시험용 배치는 영문 경로로.

---

## 주소 칸 전수 적용 · 저장 단추 · 창 두 개 (2026-08-28)

30가지 시험 통과 (`/tmp/주소전체·저장시험.mjs`).

### 주소 칸을 한 부품으로 모았습니다

주소를 적는 자리는 넷입니다 — **대상자 · 종사자 · 기관 정보 · 첫 설치**.
대상자에만 「주소 찾기」가 있으면 **종사자 주소만 손으로 적혀 읍면동이 달리
잡히고**, 기관 주소는 손으로 친 오타가 **청구서·영수증 머리글에 그대로 찍혀
지자체까지 갑니다.**

`web/src/components/AddressField.tsx` 하나로 모았습니다 (`주소칸`).
한 곳만 고치면 네 자리가 같이 좋아집니다.

- [x] 대상자 — 고르면 **읍면동 칸도** 함께 채웁니다 (비어 있을 때만)
- [x] 종사자 — 읍면동 칸이 따로 없습니다(주소에서 뽑음, `staff.ts dongOf`).
      그래서 라벨에 **「읍면동은 「해남읍」로 잡힙니다」**를 띄워 확인하게 합니다
- [x] 기관 정보 · 첫 설치 — 청구서 머리글로 나가는 값이라 더 중요합니다
- [x] **지자체 엑셀로 들어오는 대상자는 예외** (`Inbox.tsx`). 그 주소는 지자체가
      준 것이지 우리가 고쳐 넣을 자리가 아닙니다 — 엑셀에 있는 대로 받습니다

### 대상자 상세 — 저장 단추

**저절로 저장되지 않습니다.** 그런데 화면이 그 말을 안 했습니다. 저장 단추가
긴 칸 **맨 아래**에만 있어 스크롤 밖으로 나갔고, 사람은 「적었으니 됐겠지」
하고 나갑니다.

- [x] 고치는 동안 머리글 **삭제 바로 왼쪽**에 저장을 둡니다 (자리가 늘 같아야
      손이 기억합니다). `form="대상자고치기"` 로 폼 밖에서 누릅니다
- [x] **안 고쳤으면 「고친 것 없음」**으로 안 눌립니다 — 헛눌림을 막습니다
- [x] 고치면 **「아직 저장하지 않았습니다」** 띠가 뜹니다
- [x] 취소는 **한 번 더 묻습니다** — 단추가 「고친 것을 버릴까요?」로 바뀝니다.
      무서운 경고창(`confirm`) 대신 단추 자체가 바뀝니다

### bat 을 켜면 창이 두 개 뜨던 것

**여는 곳이 두 군데였습니다.** `run.ps1` 이 `/api/health` 를 찔러 보고 열고,
`server/index.ts` 도 자기가 켜지면서 열었습니다.

**여는 일은 프로그램에게 맡겼습니다**(`run.ps1` 의 opener 삭제). 이유 둘:

1. 프로그램은 **자기가 실제로 잡은 번호**를 압니다. `run.ps1` 은 5757 부터
   찔러 봤는데, **옛 프로그램이 5757 에 남아 있고 새것이 5758 로 갔으면
   엉뚱한 쪽을 엽니다.** 창 두 개보다 이쪽이 더 고약합니다
2. 나중에 하나짜리 실행파일로 묶으면 `run.ps1` 이 없습니다. 그때도 열려야 합니다

- [x] `통돌Note 시작.bat` 을 만들어 두었습니다. **일부러 영문만** 씁니다 —
      cmd 는 .bat 를 그 컴퓨터 코드페이지로 읽어서, 한글을 넣으면 깨지다가
      명령까지 망가집니다
- [ ] 혹시 쓰시던 bat 에 `start http://127.0.0.1:5757` 같은 줄이 있으면 그것도
      지워야 합니다 (그 파일은 아직 못 봤습니다)

---

## 글자 크기 (2026-08-28)

24가지 시험 통과 (`/tmp/글자크기시험.mjs`).

**하루 종일 보는 화면**이고, 사무실에는 나이가 있는 직원이 함께 앉아 있습니다.
만든 사람 눈에 알맞은 크기가 그분들께는 **읽히지 않는 크기**입니다.
안 보이면 잘못 읽고, **잘못 읽은 것이 청구서로 나갑니다.**

윈도우 배율(125%)을 올리면 되긴 하지만 **컴퓨터 전체가 커집니다** — 남의
프로그램까지 다 바뀌는 것을 공용 PC 에서 하기는 어렵습니다. 그래서
**이 프로그램만** 크게 볼 수 있게 했습니다.

- [x] 설정 · **「보기」** 상자에 밝기와 나란히 (좌우). 둘 다 「이 사람이 이 화면을
      어떻게 보는가」라 한자리에 있어야 찾습니다
- [x] **셋뿐입니다** — 보통(16) · 크게(19) · 아주 크게(22). 넷으로 하면 단추가
      한 줄에 안 들어가고, 「크게」와 「더 크게」는 차이가 눈에 안 띕니다.
      눈이 어두우신 분께 필요한 것은 **눈에 띄게 큰 것**이지 미세 조정이 아닙니다
- [x] **글자만 커지지 않습니다.** 화면 치수가 전부 `rem` 이라 칸·여백·차림표가
      같이 커집니다. 글자만 키우면 칸이 그대로라 **글이 잘립니다**
      (`.side` 210px · `.facts` 100px · `.rec-split` 262px · `.panel` → 전부 rem 으로)
- [x] **그려지기 전에** 붙입니다(`index.html`). 그린 뒤에 바꾸면 켤 때마다
      화면이 한 번 커지며 덜컹거립니다
- [x] **뽑는 종이는 늘 16px** (`@media print`). 화면 크기는 그 사람 눈의 문제인데
      그것이 종이까지 따라가면 **크게 보시는 분이 뽑은 제공기록지와 눈 좋은 분이
      뽑은 것이 쪽 수도 줄 수도 달라집니다.** 지자체에 내는 종이가 사람마다
      다르면 안 됩니다
- [x] **그 사람 브라우저에만** 저장합니다(테마와 같은 자리). 자료함에 넣으면
      눈이 좋은 직원이 정한 크기를 옆자리가 물려받습니다
- [x] 가장 크게 해도 대상자·제공실적·정산·통계·종사자 화면이 **옆으로 안 넘칩니다**

**배운 것** — 「아주 크게」에서 단추 묶음이 넓어져 두 칸에 안 들어가자
**밝기 단추가 접혀 「어둡게」만 아랫줄로 떨어졌고**, 둥근 모서리가 어긋나
고장처럼 보였습니다. 화면 너비로 재는 `@media` 는 **글자 크기를 못 봅니다**
(미디어 쿼리의 `em` 은 뿌리 크기를 안 따릅니다). `repeat(auto-fit, minmax(14rem, 1fr))`
로 바꿔, 안 들어가면 **스스로 위아래로 서게** 했습니다.

---

## 상세 주소 칸 · 문구 (2026-08-29)

24가지 + 8가지 시험 통과 (`/tmp/상세주소시험.mjs` · `서식주소시험.ts`).

### 주소를 두 칸으로 — 도로명 · 상세(동·호수)

처음에는 검색창에서 고른 뒤 **커서를 주소 칸 맨 뒤로** 보내 이어 적게 했습니다.
그런데 그러면 **주소를 다시 고를 때 적어 둔 동·호수가 통째로 지워집니다.**
포털이 칸을 나눠 두는 이유가 그것이었습니다.

- [x] 네 자리 **모두** — 대상자 · 종사자 · 기관 정보 · 첫 설치
- [x] 고르면 **커서가 상세 칸으로** 갑니다 (도로명은 방금 정해졌으니)
- [x] 다시 골라도 **상세는 그대로**

### 합치는 곳은 한 군데뿐 — `주소한줄()`

자료함에는 두 칸으로 들어가지만 **나가는 자리는 여럿**입니다 — 화면 · 계약서 ·
기록지 · 청구서 · 영수증. 자리마다 합치는 코드를 따로 쓰면 **언젠가 한 곳을
빠뜨리고**, 그때 어르신 댁 동·호수가 빠진 서류가 나가 제공인력이 헛걸음합니다.

- [x] `server/util.ts` 의 **`주소한줄(payload)`** 하나만 씁니다
      (`api.ts` · `contract.ts` · `recordbook.ts` · `staff.ts` · `invoice.ts` · `receipt.ts`)
- [x] 화면이 **고칠 때만** 갈라 받습니다 (`addressRoad` · `addressDetail`).
      합쳐진 주소는 도로 가를 수 없기 때문입니다
- [x] **종사자 읍면동은 도로명만 보고** 뽑습니다. 상세주소는 주소 체계의 일부가
      아니라 **사람이 자유롭게 적는 메모**라, 무슨 글자가 들어올지 모르는 값을
      자동 해석에 넣지 않습니다
- [x] 자료함 — 대상자·종사자는 **payload 안**이라 표를 안 건드렸고,
      기관만 `organization.address_detail` 칸 하나 추가 (SCHEMA_VERSION 15, 더하기만)

### 문구

- [x] 「고치기」 → **「수정」**
- [x] 「고친 것을 버릴까요?」 → 단추는 늘 **「수정 취소」**.
      한 번 더 묻는 말은 **띠**로 옮겼습니다 —
      「한 번 더 누르면 고치신 내용이 사라집니다」.
      단추 이름이 눌릴 때마다 바뀌면 **무엇을 누르는 중인지 헷갈립니다**

**배운 것** — 「커서를 뒤로 보내 이어 적게」는 만들기는 쉬웠지만
**두 번째로 주소를 고치는 순간 무너지는** 설계였습니다.
사람이 이미 쓰는 방식(포털)이 있으면 그 이유부터 봐야 합니다.

---

## 갱신 뼈대 — 7-가 (2026-08-29)

18가지 + 5가지 시험 통과 (`갱신시험.ts` · `/tmp/판번호시험.mjs`).

고칠 것이 수시로 나올 텐데 **고친 것을 기관에 닿게 할 길**이 없었습니다.
코드를 열어 보니 구멍이 셋이었고, 셋 다 막았습니다.

- [x] **판 번호를 한 곳에서만** (`server/version.ts` → `package.json`).
      `index.ts` 에 손으로 박혀 있던 `"0.1.0"` 을 걷어냈습니다.
      **판 견주기는 숫자로** 합니다 — 글자로 하면 `0.10 < 0.9` 가 됩니다
- [x] 설정 → **「이 프로그램」**에 판 번호 + 자료함 구조 번호.
      원격 지원의 첫 물음이 「지금 몇 판 쓰세요?」인데 답할 데가 없었습니다
- [x] **내림 막이** — `db.ts` 가 `schema_version` 을 **쓰기만 하고 읽지 않았습니다.**
      더 새 판이 만든 자료함을 옛 판이 그냥 열고 버전을 되돌려 썼습니다.
      이제 **켜지지 않고 멈춥니다** — 자료가 조용히 어긋나는 것보다
      오늘 못 켜는 것이 낫습니다. 못 켜는 것은 눈에 보이고, 어긋난 것은 안 보입니다
- [x] **갱신 전 백업** — `migrate()` **전에** 한 부. 파일 이름에 어느 판에서
      왔는지 남깁니다. **자동 정리에서 뺐습니다**(`backup.ts` 의 `갱신전것`) —
      되돌아갈 유일한 자리를 개수가 찼다고 지우면 안 됩니다
- [x] 백업을 못 떠도 **켜기는 합니다.** 되돌리기와 다릅니다 — 되돌리기는 지금
      자료를 버리는 일이라 못 뜨면 멈춰야 하지만, 갱신은 자료를 버리지 않습니다

**시험은 딴 폴더에서 프로그램을 실제로 켜 봅니다.** 진짜 자료함을 건드리지 않고,
「더 새 판 자료함으로는 안 켜진다」를 **종료 코드로** 확인합니다.

`배포하기.md` — exe 만드는 순서와 다음 판 내보내는 순서를 적어 두었습니다.

