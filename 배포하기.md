# 배포하기 — 기관에 무엇을 어떻게 주나

`C:\Users\Bak Byeongseob\Desktop\care-saas\chaengkim-note\배포하기.md`

> **2026-09-08 에 다시 씀.** 「설치」와 「배포」가 헷갈린다는 지적이 있어
> 낱말부터 못박습니다.

---

## 낱말 세 개

| 낱말 | 무엇 | 누구 것 |
|---|---|---|
| **배포본** | 기관에 건네는 **zip 하나.** 프로그램·화면·서식·글꼴·도구·문서·설치 파일이 **전부** 들어 있음 | 기관에 줍니다 |
| **설치 파일** | 그 zip **안에 든 조각 하나.** 기관이 압축을 풀고 두 번 누르는 것 | zip 안에 있습니다 |
| **만들기** | 소스를 묶어 **배포본을 찍어내는 도구** | **무무 님 것. 절대 안 줍니다** |

배포본이 상자라면 **설치 파일은 상자 안의 「여기부터 누르세요」 종이**이고,
**만들기는 상자를 만드는 기계**입니다.

---

## ★ 기관에 주는 것은 **파일 하나**입니다

```
   무무 님 PC                          기관 PC
   ─────────────                       ─────────
   통돌Note 만들기.bat
        │ 두 번 클릭 · 3~5분
        ↓
   통돌Note_1.19.0.zip  ── 메일/USB ──→  받아서 압축 풀기
        │                                     │
        │ 안에 든 것                           ↓
        ├ 통돌Note.exe            95MB   통돌Note 설치.bat  두 번 클릭
        ├ 통돌Note 설치.bat + install.ps1     │
        ├ 설치 안내.html                       ↓
        ├ 사용설명서.html               C:\통돌Note 에 깔림
        ├ 통돌Note 갈아끼우기.bat              바탕화면에 바로가기
        └ dist-web · 서식 · 글꼴 · 도구
```

**세 개가 아니라 한 개입니다.** 설명서와 안내서는 zip 안에 이미 있습니다.

### `통돌Note 만들기.bat` 을 주면 안 되는 까닭 둘

1. **기관 PC 에서는 아무 일도 안 일어납니다.** 소스 코드·bun·node_modules 가
   있어야 도는데 기관 PC 에는 하나도 없습니다
2. **제품 소스를 통째로 넘기는 것**입니다

---

## 배포본 만드는 법

### 1. 버전 번호를 정합니다

`package.json` 의 `version` 을 보고 그대로 쓰거나 올립니다.

### 2. 「이번에 바뀐 것.txt」를 씁니다

**기관이 읽을 말로** 적습니다. 이 줄들이 기관 화면의 새 버전 알림에
그대로 뜨고, 기관은 그걸 읽고 「깔지 말지」를 정합니다.

### 3. `통돌Note 만들기.bat` 두 번 클릭

버전 번호 하나만 묻습니다. 나머지는 저절로 돕니다.

### 4. 스스로 점검하는 것을 봅니다

만들기가 끝나기 전에 **혼자 셉니다** —

- 실행 파일이 제대로 묶였나 (20MB 미만이면 이상)
- 화면(dist-web) · 서식 · 글꼴 · 도구가 다 들어갔나
- **제출 서식이 원본과 한 글자도 다르지 않나**
- 설치 파일이 **한 쌍**인가 (하나만 있으면 눌러도 아무 일이 없습니다)
- 안내문·설명서에 버전 번호가 찍혔나
- **`data` 가 안 들어갔나** (자료는 기관 것입니다)

**하나라도 어긋나면 「이대로 보내지 마세요」 하고 멈춥니다.**

### 5. 나온 zip 을 건넵니다

```
   통돌Note_1.19.0.zip     ← 이것 하나
```

---

## 서명 열쇠가 없으면

「⚠ 서명 열쇠가 없습니다」가 뜹니다.

**zip 은 정상으로 나옵니다.** 안 나오는 것은 **자동 업데이트용 파일**뿐입니다.
손으로 zip 을 건네는 길은 그대로 살아 있습니다.

자동 업데이트까지 쓰시려면 `서명열쇠 만들기.bat` 을 **한 번만** 누르시면 됩니다.
그 뒤로는 만들기가 알아서 `올릴것\` 폴더에 업데이트 파일을 함께 만듭니다.

---

## 두 번째 기관부터는

같은 zip 을 그대로 주시면 됩니다. **기관마다 다시 만들 것이 없습니다.**
기관 이름·단가·한도는 기관이 처음 켤 때 **마법사에서** 넣습니다.

---

## 새 버전을 낼 때

1. `이번에 바뀐 것.txt` 를 새로 씁니다
2. `package.json` 의 버전을 올립니다
3. `통돌Note 만들기.bat`
4. **자동 업데이트를 쓰면** — `올릴것\` 을 GitHub Releases 에 올립니다
   (자세한 것은 `업데이트 올리는 법.md`)
5. **안 쓰면** — 새 zip 을 기관에 건넵니다.
   기관은 압축을 풀고 `통돌Note 설치.bat` 을 **같은 자리**에 다시 누릅니다.
   프로그램만 갈아 끼우고 **자료함은 손도 안 댑니다**
