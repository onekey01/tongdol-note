/**
 * QR 읽개의 입구.
 *
 * 이 한 줄짜리 파일을 묶으면 `jsQR` 이라는 이름 하나가 생깁니다.
 * 현장앱짓기.ts 가 이것을 묶어서 앱 파일에 심습니다.
 *
 * 왜 묶은 결과를 파일로 안 들고 있나 —
 *   127KB 짜리 남의 코드를 우리 폴더에 두면, 그것이 언제 어느 판에서
 *   왔는지 아무도 모르게 됩니다. package.json 에 적어 두고
 *   **지을 때마다 새로 묶는** 편이 낫습니다.
 */
import jsQR from "jsqr";
globalThis.jsQR = jsQR;
