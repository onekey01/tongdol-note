# 업데이트 올리는 법

`C:\Users\Bak Byeongseob\Desktop\care-saas\chaengkim-note\업데이트 올리는 법.md`

**무무 님이 보시는 종이입니다.** 기관에 나가는 것이 아닙니다.
버전을 낼 때마다 이 순서를 그대로 밟으시면 됩니다.

---

## 한눈에

```
   ① 「이번에 바뀐 것.txt」에 무엇이 바뀌었는지 적는다
   ② 「통돌Note 만들기.bat」을 두 번 누른다
   ③ 「깃 올리기.bat」을 두 번 누른다 — 소스를 GitHub 에 남긴다
   ④ 「올릴것」 폴더가 열린다 — 그 안의 파일 세 개를 GitHub 에 올린다
                                                    (다 합쳐 8분)
```

기관 쪽에서는 아무것도 안 하셔도 됩니다. 다음에 프로그램을 켤 때
**저절로 띠가 뜨고**, 관리자가 「지금 업데이트」를 누르면 끝납니다.

---

# 딱 한 번만 하는 것

## 1. 서명 열쇠 만들기

**「서명열쇠 만들기.bat」을 두 번 누릅니다.** GitHub 주인 이름을 묻습니다.
`https://github.com/【여기】/tongdol-note` 의 【여기】 부분입니다.

만들어지는 것 —

| 무엇 | 어디에 | 무엇에 쓰나 |
|---|---|---|
| `개인열쇠.pem` | `C:\Users\Bak Byeongseob\통돌Note-서명열쇠\` | 새 버전에 **서명**할 때 |
| 공개 열쇠 | `server\업데이트열쇠.ts` 안에 | 기관 프로그램이 **확인**할 때 |

### ★ 개인열쇠.pem 을 잃어버리지 마세요 ★

이것을 가진 사람은 **기관 PC 가 믿는 새 버전을 만들 수 있습니다.**
그래서 두 가지를 지켜 주세요.

- **남에게 보내지 마세요.** 메일에도, 카톡에도.
- **프로그램 폴더 안으로 옮기지 마세요.** 그러면 언젠가 zip 에 딸려
  들어가 기관에 나갑니다. 그 순간 서명은 아무 뜻도 없어집니다.
- **어딘가에 한 부 더 두세요.** USB 든 다른 폴더든.

잃어버리면 새 열쇠를 만들어야 하고, **이미 나가 있는 기관은 손으로
한 번 갈아 끼워야** 합니다 (옛 버전은 새 서명을 못 믿으니까요).

## 2. GitHub 저장소 만들기

1. <https://github.com/new> 로 갑니다
2. **Repository name** 에 `tongdol-note` 를 넣습니다
   (**이 이름 그대로**여야 합니다 — 프로그램에 박혀 있습니다)
3. **Public / Private** 은 **Public** 으로 둡니다

> **왜 Public 인가** — Private 저장소의 파일은 **열쇠 없이는 못 받습니다.**
> 그러면 기관 프로그램에 GitHub 열쇠를 넣어 보내야 하는데, 그 열쇠는
> exe 안에 들어가니 **누구나 꺼낼 수 있습니다.** 숨긴 셈이 안 됩니다.
>
> Public 이어도 **소스는 안 올립니다.** 올리는 것은 완성된 zip 세 개뿐이고,
> 그건 어차피 기관에 나가는 물건입니다. **대상자 자료는 한 글자도 안
> 올라갑니다** — 그건 기관 PC 에만 있습니다.

4. **Create repository** 를 누릅니다
5. 만들어졌으면 끝입니다. 파일을 넣거나 할 일 없습니다.

---

# 버전을 낼 때마다 하는 것

## ① 「이번에 바뀐 것.txt」를 적습니다

프로그램 폴더의 **`이번에 바뀐 것.txt`** 를 메모장으로 열어,
무엇이 바뀌었는지 **한 줄에 하나씩** 적습니다.

```
청구서 반올림이 군 방식과 어긋나던 것을 고쳤습니다
대상자 명단을 인쇄하면 주소 열이 잘리던 것을 고쳤습니다
기록지에 상세주소(동·호수)가 함께 실립니다
```

- `#` 으로 시작하는 줄은 안 들어갑니다 (설명용)
- **스무 줄까지, 한 줄 120자까지**
- 저장할 때 인코딩은 신경 안 쓰셔도 됩니다 (UTF-8 이든 옛 방식이든 읽습니다)

### 이 줄들이 기관 화면에 그대로 뜹니다

기관은 이것을 읽고 **깔지 말지**를 정합니다.
**「버그 수정 및 안정성 개선」은 아무 말도 안 한 것입니다.**
그렇게 적으면 안 깝니다. 그리고 안 깐 채로 몇 달이 갑니다.

**한 줄도 안 적으면 만들기가 멈춥니다.** 일부러 그렇게 해 두었습니다.

## ② 「통돌Note 만들기.bat」을 두 번 누릅니다

전과 똑같습니다. 버전 번호를 묻고, 짓고, 묶습니다.
달라진 것은 **끝에 하나 더 묻는다**는 것뿐입니다.

```
  이 버전은 **셈이 틀리던 것을 고친 버전**입니까?
  (그런 버전을 안 깔면 엉뚱한 청구서가 어르신께 갑니다.)
  꼭 해야 하는 버전이면 y, 아니면 그냥 엔터: _
```

**`y` 는 정말 셈이 틀렸던 것을 고쳤을 때만** 넣으세요.
아무 버전에나 붙이면 그 표시가 아무 뜻도 없어지고, 그러면 정작 급한
버전이 왔을 때 기관이 안 알아봅니다.

`y` 를 넣은 버전은 기관 화면에서 **띠 색이 달라지고** 「꼭 하셔야
합니다」가 함께 뜹니다.

## ③ 「깃 올리기.bat」을 두 번 누릅니다 — 소스 남기기

**이것은 배포가 아닙니다.** 기관이 받는 것은 ④ 의 Release 입니다.
이 단계는 **만든 글(소스)을 GitHub 에 남겨 두는 것**뿐입니다.

왜 하나 —

- 지금 통돌 Note 는 **이 PC 한 대에만** 있습니다. PC 가 죽으면 끝입니다.
- 「어제는 됐는데 오늘 안 되네」일 때 **어제 것으로** 돌아갈 수 있습니다.
- 무엇을 언제 왜 고쳤는지가 **한 줄 설명과 날짜**로 남습니다.

처음 한 번은 git 을 깔아야 합니다. 안 깔려 있으면 검은 창이
받는 곳(`https://git-scm.com/download/win`)을 알려 주고, 열어 줄지
물어봅니다. **Next 만 계속 누르면 됩니다.** 다 깔았으면 검은 창을
닫았다가 다시 누르세요.

그 다음 처음 한 번만 세 가지를 묻습니다 — **올릴 주소 · 이름 · 메일**.
주소는 `server\업데이트열쇠.ts` 에 박힌 것을 읽어 오므로 보통 엔터만
치면 됩니다.

두 번째부터는 묻는 것이 **한 줄 설명 하나뿐**입니다.

### 소스가 남에게 보입니다 — 먼저 정하세요

기관 프로그램은 **로그인 없이** Release 를 받아 가야 해서 그 저장소는
**공개**라야 합니다. 같은 저장소에 올린 소스도 누구나 보게 됩니다.
안 보이게 두시려면 GitHub 에서 **비공개 저장소를 하나 더** 만들고 첫
실행 때 그 주소를 치세요. Release 는 지금 저장소 그대로 갑니다.

### 열쇠가 든 파일은 못 올라갑니다

`.env` 에는 우편함(Supabase) 열쇠가 들어 있습니다. 이런 파일은 한 번
올라가면 **지워도 기록에 남습니다.** 그래서 올리기 전에 멈추고
아무것도 올리지 않습니다.

---

## ④ 「올릴것」 폴더의 세 파일을 올립니다

만들기가 끝나면 **탐색기가 「올릴것」 폴더를 열어 줍니다.**
안에 딱 세 개가 있습니다.

```
  올릴것\
    version.json                버전 번호 · 날짜 · 바뀐 것 · 지문
    version.json.sig            그 파일에 찍은 서명
    tongdol-note-1.16.31.zip    프로그램 (40MB쯤)
```

> **왜 파일 이름이 영문인가** — GitHub 은 올린 파일 이름에서 영문·숫자가
> 아닌 글자를 **점(.)으로 바꿔 버립니다.** 「버전정보.json」을 올리면
> 「`.....json`」이 되어 프로그램이 못 찾습니다. 그래서 올리는 것만
> 영문으로 씁니다. 기관이 볼 일은 없는 이름입니다.

### 올리는 순서 (GitHub 화면)

1. `https://github.com/【주인이름】/tongdol-note` 로 갑니다
2. 오른쪽의 **Releases** 를 누릅니다
   (처음이면 「Create a new release」, 다음부터는 **Draft a new release**)
3. **Choose a tag** 를 누르고 — 여기가 제일 중요합니다 —

   > ### ★ 태그는 반드시 `v` + 버전 번호 ★
   >
   > 버전이 `1.16.31` 이면 태그는 **`v1.16.31`** 입니다.
   >
   > `1.16.31` (v 없이) 도 안 되고, `v1.16` 도 안 됩니다.
   > **버전정보에 적힌 받는곳 주소가 이 태그로 만들어집니다.**
   > 어긋나면 기관 프로그램이 「받는 중」에서 멈춥니다.
   >
   > 만들기 창 맨 끝에 **써야 할 태그가 그대로 찍혀 나옵니다.**
   > 그것을 보고 넣으시면 틀릴 일이 없습니다.

   입력칸에 `v1.16.31` 을 치고 **「+ Create new tag …」** 를 누릅니다
4. **Release title** — 태그와 같게 (`v1.16.31`) 두시면 됩니다
5. 아래 **「Attach binaries by dropping them here」** 자리에
   **「올릴것」 폴더의 세 파일을 통째로 끌어다 놓습니다**
   - zip 이 40MB 라 올라가는 데 **1~3분** 걸립니다
   - 세 개 다 **Uploaded** 로 바뀔 때까지 기다립니다
6. **Publish release** 를 누릅니다

### 다 됐습니다

기관 프로그램은 **다음에 켤 때** 알아서 봅니다. 아무에게도 연락
안 하셔도 됩니다.

---

# 확인하는 법

올리고 나서 **주소 두 개를 브라우저에 쳐 보시면** 바로 압니다.

```
https://github.com/【주인이름】/tongdol-note/releases/latest/download/version.json
```

방금 올린 내용이 그대로 보이면 됐습니다.
**`404`** 가 뜨면 태그나 파일 이름이 어긋난 것입니다.

더 확실하게 보시려면 — 프로그램을 켜고
**설정 → 이 프로그램 → 업데이트 안내 → 「지금 확인」** 을 누르십시오.
「지금이 가장 새 버전입니다」가 뜨면(= 개발 PC 는 이미 그 버전이라)
읽는 길은 살아 있는 것입니다.

---

# 기관 쪽에서는 무슨 일이 벌어지나

| 언제 | 무엇이 |
|---|---|
| 켤 때 (하루 한 번) | 조용히 `version.json` 을 읽어 봅니다 |
| 새 버전이면 | **뒤에서** zip 을 받습니다. 관리자는 하던 일을 계속합니다 |
| 다 받으면 | 화면 위에 띠 — 「**새 버전(1.16.31)의 업데이트가 준비됐습니다**」 |
| 「지금 업데이트」를 누르면 | 프로그램이 꺼지고 → 검은 창 8초 → **새 버전으로 다시 켜집니다** |

**관리자가 하는 일은 단추 하나입니다.** 압축을 풀거나 파일을 옮길
일이 없습니다.

- **자료는 안 건드립니다** — `data\` 폴더는 손도 안 댑니다
- **옛 버전은 `이전버전\` 에 남습니다** — 새 버전이 이상하면 돌아갈 자리
- **인터넷이 없어도 평소대로 켜집니다** — 3초 안에 답 없으면 조용히 넘어갑니다
- 관리자가 아닌 사람 화면에는 **띠가 안 뜹니다**

---

# 안 되는 것과 그 까닭

## 「묻지 않고 저절로」는 안 합니다

기술 때문이 아니라 **업무 때문**입니다.

이 프로그램은 청구서를 만듭니다. 밤사이 셈이 바뀌면 **어제와 오늘
금액이 다른데 아무도 이유를 모른 채** 군에 서류가 나갑니다. 나중에
「왜 달라졌냐」에 기관이 답할 말이 없습니다. 그래서 바뀐 것을 읽고
**누른 흔적**이 남는 편이 낫습니다.

## 백신이 한마디 할 수 있습니다

서명 인증서가 없는 exe 라서, 바꾸는 그 순간 백신이 잠깐 잡을 수
있습니다. **처음 한 번은 파일럿 기관 PC 에서 직접 보시는 것이 좋습니다.**

잡히더라도 자료는 안전합니다 — 옛 exe 가 `이전버전\` 에 그대로 있고,
`data\` 는 건드린 적이 없습니다. 최악의 경우 옛 exe 를 제자리에 다시
갖다 놓으면 그 전으로 돌아갑니다.

(코드 서명 인증서를 사면 이 문제도 「알 수 없는 게시자」 경고도 한꺼번에
없어지지만, **연 60만~95만 원대**라 지금 규모에서는 안 사는 것으로
정했습니다 — 2026-09-08 무무.)

---

# 잘 안 될 때

| 뜨는 말 · 증상 | 무엇이 잘못됐나 |
|---|---|
| 만들기 끝에 「서명 열쇠가 아직 없습니다」 | 「서명열쇠 만들기.bat」을 한 번 눌러 주세요 |
| 만들기 끝에 「이번에 바뀐 것.txt 가 비어 있습니다」 | 무엇이 바뀌었는지 한 줄이라도 적어 주세요 |
| `version.json` 주소가 `404` | Release 를 **Publish** 안 했거나, 파일을 안 올렸습니다 |
| 기관 화면에 띠가 안 뜸 | 버전 번호를 안 올렸을 수 있습니다 (같은 버전이면 안 뜹니다) |
| 기관이 「받는 중」에서 안 끝남 | **태그가 `v1.16.31` 이 아닐** 가능성이 큽니다 |
| 「버전정보의 서명이 맞지 않습니다」 (검은 창) | `version.json` 만 바꾸고 `.sig` 를 안 올렸습니다 — **셋을 늘 같이** |

> **`version.json` 을 GitHub 에서 직접 고치지 마세요.** 한 글자만 고쳐도
> 서명이 어긋나 기관 프로그램이 **아예 안 받습니다.** 고칠 것이 있으면
> 「이번에 바뀐 것.txt」를 고치고 만들기를 다시 돌리십시오.

---

# 손으로 건네는 길도 그대로 있습니다

GitHub 을 안 쓰셔도 됩니다. **`통돌Note_1.16.31.zip` 하나를 메일이나
USB 로 건네고**, 기관이 압축을 풀어 덮어쓰는 길은 그대로 살아 있습니다
(설치 안내문에 적혀 있습니다).

서명 열쇠가 없어도 zip 은 늘 만들어집니다. GitHub 은 **편해지는 길**이지
없으면 안 되는 것이 아닙니다.
