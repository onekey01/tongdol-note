# ─────────────────────────────────────────────────────────────
#  서식8 시험 — 한글이 여는지만 봅니다 (고치지 않습니다)
# ─────────────────────────────────────────────────────────────
#
#  「서식8시험」 폴더의 세 장을 한글로 열어 보고 표를 내놓습니다.
#
#     A  세 줄        서식이 준 다섯 줄 안에서 씁니다 (칸 안 늘림)
#     B  여덟 줄      다섯 줄을 넘겨 **칸이 세 줄 늘어납니다**
#     C  스무 줄      한 쪽을 넘겨 **표가 이어져 다음 쪽으로** 갑니다
#
#  세 장 다 열리면, 열어서 이것을 봐 주세요 —
#    · 늘어난 줄의 **선 굵기**가 위아래 줄과 같은가
#    · 표 맨 밑의 **굵은 줄**이 하나뿐인가 (총액 줄 바로 위)
#    · C 에서 표가 **쪽을 넘어가며 이어지는가** (통째로 넘어가지 않고)
#
#  이 파일도 **변수 이름은 영문**, 저장은 **UTF-8 BOM** 입니다.
# ─────────────────────────────────────────────────────────────
$root = $PSScriptRoot
$folder = Join-Path $root "서식8시험"
& (Join-Path $root "fixforms.ps1") -DiagnoseOnly -Folder $folder
