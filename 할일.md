# 통돌 Note — 할 일 (번호순)

**정리 2026-08-28.** 번호가 곧 순서입니다. **위에서부터 하나씩** 합니다.
「3번 하자」처럼 번호로 부르면 됩니다.

날짜로 잡지 않고 **끝난 것으로 치는 기준**으로 판단합니다.
밀렸을 때 계획 전체가 무너지지 않게 하기 위해서입니다.

- 순서를 왜 이렇게 잡았는지 → 설계서 9장 「앞으로의 작업 플랜」
- 지금까지 무엇을 왜 정했는지 → `남은일.md`

---

## 지금 서 있는 자리

**관리자 화면은 한 달을 한 바퀴 돌릴 수 있습니다.**
의뢰 엑셀 → 대상자 → 배정 → 실적 달력 → 정산·청구서 → 기록지 → 통계.
시험은 서버 9벌 · 브라우저 10벌이 매번 돕니다.

**못 하는 것 둘** — ① 현장에서 찍는 것 ② 남의 컴퓨터에서 켜는 것.
**②를 먼저 합니다.** 켜지지 않으면 고쳐야 할 것을 알려 줄 사람이 없습니다.

---

## 파일럿 예행 (2026-09-09) — 관리자 입장으로 처음부터 끝까지

`bun 파일럿예행.ts` — **빈 자료함**에서 기관 개설부터 기록지 받기까지
설명서를 한 줄도 안 읽고 눌러서만 걷습니다. 사진은 `/tmp/예행사진`.

**결과: 된 것 130 · 막힌 것 0 · 헤맬 자리 1**

2026-09-10 에 세 갈래를 더 이어 걸었습니다 —
**의뢰 엑셀 올리기 · 제공인력 휴대폰 왕복 · 달 넘기기.**

### 예행이 찾아서 고친 것

- **★ 의뢰 그대로 「주 2회」가 첫 배정에서 막혔습니다.**
  1회 서비스 시간을 안 적으면 **설정의 하루 최대(3시간)**로 세어
  가사지원 월 9번 × 3시간 = 27시간, 한도 12시간 → 225%.
  관리자는 군이 보낸 의뢰를 그대로 옮겼을 뿐인데 저장이 안 됐습니다.
  → **안 적었으면 1시간으로 봅니다** (2026-09-09 무무).
  `server/계획시간.ts` 의 `기본계획분`. 하루 최대는 **청구 천장**으로만 씁니다.
  시험: `계획막기시험.ts` 7-나 (돌연변이로 확인함)

- **★ 못 간 사유를 브라우저 prompt 에 타이핑**하게 두었습니다.
  사유는 정해진 목록인데 손으로 치게 하면 「부제」·「병원」 같은 말이
  들어가고 **그대로 기록지에 실려 지자체로 나갑니다.**
  → 실적 창 안에서 **눌러서 고릅니다**. `web/src/pages/실적창.tsx`

- **직접 넣은 대상자가 배정 화면에 안 떴습니다.** (받는 서비스가 없어서)
  고장이 아니라 순서인데 말을 안 해 첫날 여기서 막힙니다.
  → 배정 화면에 「받는 서비스가 아직 없는 대상자 N명은 여기 안 나옵니다」
  와 가는 길을 띄웁니다. `서비스없는대상자수()` · `web/src/pages/Assign.tsx`

- **화면이 프로그램과 다른 말을 하고 있었습니다.**
  배정 창에 「넘어도 막지는 않습니다, 두 번 여쭙습니다」가 남아 있었는데
  2026-08-31 부터 실제로는 막습니다. → 문구를 맞췄습니다.

- **역할은 켜져 있는데 제공인력 줄만 없는 사람**에게도
  「역할이 없습니다」라고 했습니다. → 두 경우를 갈라 말합니다.

- **종사자 등록에서 이름만 적고 누르면 화면에 아무 말이 없었습니다.**
  → 아이디·비밀번호가 왜 필요한지 **칸 위에 미리** 적어 둡니다
  (규칙은 그대로 — 2026-09-09 무무).

### 그 밖에 고친 것 (2026-09-10)

- **폰이 안 이어진 제공인력**을 홈이 알려 줍니다 (`server/home.ts`)
- `방문표시험` 이 옛 글자(「떼지 마세요」)를 붙들고 있었습니다 —
  실제 종이에는 「사업 종료 시까지 제거하지 마세요」로 바뀌어 있었습니다.
  뜻을 보게 고쳤습니다. **문구 자체를 어느 쪽으로 할지는 무무 님이 정하실 일**
  — 어르신 문에 붙는 종이라면 「떼지 마세요」가 더 읽힙니다.

### 남은 헤맬 자리 (막지는 않습니다)

- 정산에서 「확정」이 안 보일 때가 있습니다 — 배정 기간이 안 끝나
  「쌓이는중」이면 맞는 동작입니다. 다만 **왜 확정을 못 하는지**는
  화면에 한 줄 있으면 좋겠습니다.

### 2026-09-10 에 이어 걸은 것

**① 의뢰 엑셀** — `서식시험/의뢰명단_예시(배정시험).xlsx` 를 올려
40건 반영 → 대상자 25명 · 배정 30줄. 암호 걸린 파일(암호 5052)도
**암호를 안 넣으면 그렇다고 말하고**, 넣으면 읽힙니다.
엑셀이 아닌 파일은 까닭을 말하고 막습니다.

**② 제공인력 휴대폰 왕복** — 폰이 올린 보고가 실적 칸에 「현장에서
올라온 것」으로 뜨고, 진행시간·한 것·서명이 보이고, **특이사항이
대상자 메모장으로** 넘어옵니다(`[날짜 · 서비스 · 이름]` 이 함께 붙습니다).

  · 새로 찾은 것 — `할일보내기.ts` 는 **휴대폰이 안 이어진 사람을
    건너뜁니다**(`if (!r.폰번호) continue;`). 맞는 동작인데 사무실에
    아무 말도 안 나왔습니다. 관리자는 배정을 다 해 두고 「왜 폰에
    아무것도 안 뜨죠」에서 막힙니다. → **홈 설정 점검에 한 줄** 넣었습니다.
  · 진짜 기기 등록은 우편함(Supabase)을 거치므로 **인터넷 있는 PC**
    에서 한 번 해 보셔야 합니다. 예행에서는 이은 셈 치고 걸었습니다
    (`도구/폰보고넣기.ts`).

**③ 달 넘기기** — 실적·정산·기록지 셋 다 ◀ ▶ 로 지난 달·다음 달을
오갑니다. 이번 달 것이 다음 달로 안 샙니다. **배정이 12월까지이므로
다음 달에도 계획 칸이 깔립니다**(10월에 실적을 못 찍으면 큰일입니다).

### 아직 안 걸어 본 것

- 진짜 휴대폰으로 **기기 등록 → 오늘 갈 곳 → 보고** 한 바퀴
  (우편함이 필요해 무무 님 PC 에서만 됩니다)
- 사무실 와이파이로 여는 길 (`사무실열기시험` — 랜이 있어야 합니다)

---

## 0. 파일럿 기관에 물어볼 것 — 만나면 5분

번호를 0 으로 둔 이유: **이것 때문에 개발이 막히지 않습니다.**
셋 다 설정이나 서식이라 나중에 바꿔도 됩니다. 다만 **물어볼 기회는 만날 때뿐**입니다.

- [ ] 해남이 **선납인지 후납인지** (지금은 후납으로 둠)
- [ ] **본인부담금 반올림 방식** (지금은 기본값 = 광주 방식)
- [ ] **지자체가 원하는 PDF 형태** — 대상자별 개별 파일인지, 한 파일로 묶는 것인지
- [x] **편람 서식 중 어느 것을 우리가 채우나** — **답 나왔습니다 (2026-08-31)**
      1-1~1-6 서약서는 **수행기관**, 2 계약서는 **대상자 등록 시 출력**,
      3~5 기록지는 **지원인력이 현장앱에서**, 6-1·6-2 는 **외부업체 계정으로**,
      7 영수증·8 비용총괄·9 기록지는 **수행기관**.
      표는 `편람반영_구현목록.md` 「누가 무엇을 쓰나」

- [x] **서식8 에 「2.」 장이 따로 있나** — **없습니다** (2026-08-31 무무 님 확인).
      **없다는 전제로 만들고**, 나중에 군이 2번 장을 더하면 그때 반영합니다.
      그래서 서식8 은 **일상생활돌봄 네 가지만** 담습니다 —
      안전생활환경개선은 서식8 에 안 들어갑니다 (서식6-1·6-2 가 그 자리입니다)

**끝난 것으로 치는 기준** — 넷 다 답을 듣고 설정·서식에 반영했다.

---

# 가. 며칠 안에 끝나는 것 (1~3)

## 1. 주소 찾기 실물 확인 ★ 제일 먼저

개발 컨테이너는 바깥 인터넷이 막혀 있어 **진짜 검색창이 뜨는 모습을 아직 못 봤습니다.**
시험은 창 자리에 가짜를 앉혀 우리 쪽 처리만 봤습니다.

- [ ] 인터넷 되는 사무실 PC → 대상자 등록 → 「주소 찾기…」
- [ ] 기관 망이 `daumcdn.net` 을 막지 않는지 확인

**끝난 것으로 치는 기준** — 진짜 검색창이 뜨고, 고른 주소와 읍면동이 칸에 들어갔다.
(안 열려도 손입력은 되므로 막히는 일은 아닙니다. 다만 **모르고 넘어가면 안 됩니다.**)

## 2. 시작 bat 확인

창이 두 개 뜨던 원인(`run.ps1` 과 프로그램이 각자 여는 것)은 고쳤습니다.
따로 만들어 쓰시던 bat 은 아직 못 봤습니다.

- [ ] 그 bat 에 `start http://127.0.0.1:5757` 같은 줄이 있으면 지우기
- [ ] 또는 새로 넣어 둔 `통돌Note 시작.bat` 을 쓰기

**끝난 것으로 치는 기준** — 두 번 눌러 켰을 때 브라우저 창이 **하나만** 뜬다.

## 3. 인쇄 다듬기 — 7개 화면

얼개(`@media print`)는 다 깔렸고 통계는 확인했습니다.
남은 것은 **화면마다 A4 폭에 열이 맞는지 눈으로 보는 일**입니다.

- [ ] 대상자 명단
- [ ] 대상자 한 명 요약 (기본정보 · 서비스 · 담당 · 상태 이력)
- [ ] 종결·중단 검토 명단 (홈의 그 목록)
- [ ] 제공인력별 주간 일정 (한 사람 것만 뽑아 손에 들려 보내는 용도)
- [ ] 종사자 명부 (자격 · 교육 이수 포함)
- [ ] 배상책임보험 대조표 (가입 인원 vs 실제 제공인력)
- [ ] 본인부담금 청구 대장

**끝난 것으로 치는 기준** — 일곱 장을 실제로 뽑아 보았고, 잘린 열이 없다.

---

# 나. 배포와 갱신 — 남의 컴퓨터에서 켜지고, 고친 것이 닿게 (4~7) ★ 가장 중요

**파일럿의 첫 관문입니다. 여기서 막히면 그 뒤가 전부 멈춥니다.**

## 4. 실행 파일 하나로 묶기

**「통돌Note 만들기.bat」을 두 번 누르시면 됩니다.** 윈도에서 하셔야 합니다.
명령어도, 폴더 손으로 꾸리기도 없습니다 — 자세한 것은 `배포하기.md`.

- [ ] 두 번 눌러서 `배포_0.1.0.zip` 이 나오는가
      (묻는 것은 판 번호 하나. 엔터면 그대로 갑니다)
- [ ] 창의 **「점검」** 줄이 전부 「있음」인가
      — exe · dist-web · 서식 · 글꼴 · **한글 도우미 dll** · data 없음

빠뜨림은 스크립트가 셉니다. 하나라도 어긋나면 **아무것도 만들지 않고 멈춥니다.**
특히 `도구/FilePathCheckerModuleExample.dll` — 빠지면 기록지를 뽑을 때마다
**한글 보안 경고창**이 뜨므로, 없으면 시작조차 하지 않습니다.

**끝난 것으로 치는 기준** — 개발 도구가 없는 컴퓨터에서 두 번 눌러 켜지고,
기록지 PDF 가 경고창 없이 나온다.

## 5. 빈 자료함에서 첫 실행 밟아 보기

개발하며 **거의 안 지나가는 길**입니다. 그래서 여기가 잘 깨집니다.

- [ ] 자료함 파일을 지운 상태에서 켜기
- [ ] 기관 정보 · 관리자 계정 입력 → 로그인 → 대상자 등록까지

**끝난 것으로 치는 기준** — 아무것도 없는 상태에서 시작해 대상자 한 명을 넣었다.

## 6. 설치 안내문 한 장

- [ ] **백신·SmartScreen 경고** — 무엇이 뜨고 어디를 누르는지, **그림과 함께**
      (서명 없는 exe 는 「알 수 없는 게시자」로 막힙니다. 안 적으면 첫날에 전화가 옵니다)
- [ ] **한글 2014 이상 필요** — 없으면 .hwpx 를 받아 직접 「PDF로 저장」
- [ ] 처음 켜는 순서 · 바탕화면 바로가기 만들기

**끝난 것으로 치는 기준** — 이 종이만 보고 남이 혼자 설치했다.

## 6-나. 프로그램 아이콘 (2026-08-29 더함 — **6번 뒤에**)

지금 exe 는 **bun 기본 아이콘**입니다. 바탕화면에 두면 무엇인지 알 수 없고,
「알 수 없는 게시자」 경고와 겹치면 **더 수상해 보입니다.** 아이콘 하나가
「제대로 만든 물건」과 「어디서 받은 파일」을 가릅니다.

- [ ] 아이콘 하나 — **256·48·32·16 픽셀**이 한 `.ico` 에 들어가야 합니다
      (작은 자리에서 그냥 줄이면 뭉갭니다. 16 픽셀은 따로 그립니다)
- [ ] exe 에 심기 — `bun build --compile --windows-icon=...`
- [ ] 브라우저 탭에 뜨는 그림(파비콘)도 **같은 것으로**
- [ ] 만들기 스크립트가 **아이콘 빠졌는지 점검**에 넣기

**모양은 6번 끝나고 정합니다.** 이름이 「통돌 Note」이니 글자 「ㅌ」이나
공책·체크 표시, 손 맞잡는 모양 쪽이 후보입니다. 어르신 돌봄이라 **의료 느낌은 피합니다.**

**끝난 것으로 치는 기준** — 바탕화면과 작업표시줄에서 **한눈에 우리 것**으로
보이고, 16 픽셀로 줄여도 뭉개지지 않는다.

---

## 7. 갱신(업데이트) 길 만들기

**초기에는 고칠 것이 수시로 나옵니다.** 그런데 지금은 고친 것을 기관에
**닿게 할 방법이 없습니다.** 지금 코드에 구멍이 셋 있습니다.

- 판 번호가 **두 곳에 손으로 박혀** 있습니다 (`package.json` · `server/index.ts`).
  한쪽만 고치면 어긋납니다
- **화면 어디에도 판 번호가 안 보입니다.** 전화로 「지금 몇 판 쓰세요?」를 물어도
  답할 수가 없습니다
- **내림 막이가 없습니다.** 새 판이 만든 자료함을 옛 판으로 열면 그냥 열리고,
  `schema_version` 을 옛 번호로 **되돌려 씁니다.** 그 뒤로 어긋난 것을
  아무도 모릅니다 — **이것이 셋 중 제일 위험합니다**

### 7-가. 뼈대 — 됐습니다 (2026-08-29)

18가지 시험 통과 (`갱신시험.ts`) + 화면 5가지 (`/tmp/판번호시험.mjs`).

- [x] **판 번호를 한 곳에서만** — `server/version.ts` 가 `package.json` 을 읽습니다.
      `index.ts` 에 손으로 적혀 있던 `"0.1.0"` 을 걷어냈습니다
- [x] 설정 → **「이 프로그램」**에 판 번호와 자료함 구조 번호
- [x] **내림 막이** — 자료함 구조가 프로그램보다 크면 **켜지지 않고 멈춥니다.**
      검은 창에 어느 판으로 켜야 하는지 뜨고, 자료는 건드리지 않습니다
- [x] **갱신 전 백업** — 판이 바뀐 것을 알아채면 `migrate()` **전에** 한 부
      (`tongdol-갱신전-0.0.9.db`). 자동 정리에서 빠지고 되돌릴 수 있습니다
- [x] 같은 판으로 다시 켜면 **또 뜨지 않습니다** (보관이 헛되이 차지 않게)
- [x] **자료함은 exe 와 따로**(`data/`) — 딴 폴더에서 켜 실제로 확인했습니다

### 7-나. 파일럿 동안 — 손으로 갈아 끼우기

기관이 한 곳일 때는 **이게 제일 빠르고 안전합니다.** 전화 한 통이면 됩니다.

- [ ] 새 exe 를 메일 · USB 로 전달 → **끄고 → 파일 덮고 → 켜기**
- [ ] **무엇이 바뀌었는지 한 줄.** 모르는 변화는 무섭습니다
- [ ] 옛 exe 는 `이전판/` 에 남겨 둡니다 — 새 판이 이상하면 되돌아갈 자리

### 7-다. 업데이트 안내 — **됐습니다 (2026-09-08, v1.16.31)**

> 이름을 「새 판 알림」에서 **「업데이트 안내」**로 바꿨습니다 (2026-09-08 무무).
> 그리고 **알려 주기만** 이 아니라 **한 번 누르면 저절로 갈아 끼웁니다.**
> 아래 옛 계획은 그대로 두고, 바뀐 것은 맨 끝의 「업데이트 안내 (2026-09-08)」에
> 적었습니다.

#### (옛 계획 — 참고용)

**4·5·6번 끝나고 바로 합니다** (2026-08-29 정함). 원래는 「기관이 늘면」으로
미뤄 두었는데, 기관이 한 곳이어도 **보낸 것을 안 깐 채로 지나가는 일**이
생깁니다. 띠는 사람이 잊어도 남아 있습니다. 두세 시간이면 붙습니다.

- [ ] 켤 때 **작은 판 정보 파일 하나**를 읽어 지금 판과 견주기
- [ ] 새 판이면 화면 위에 띠 — 「**새 판 0.1.2 가 나왔습니다 (9월 5일)**」
- [ ] **바뀐 것을 그 자리에서 펼쳐 보여 줍니다** ★
      링크를 눌러 딴 데로 나가야 알 수 있으면 아무도 안 봅니다.
      **왜 바꿔야 하는지 모르는 갱신은 안 깝니다**
- [ ] **「내려받기」 단추 하나** ★ 주소를 보여 주고 옮겨 적게 하지 않습니다.
      누르면 **파일이 바로 받아집니다.** 주소는 화면에 안 보입니다
- [ ] 하루 한 번만 확인 (켤 때마다 두드리지 않게)
- [ ] 설정 → **「새 판 알림」 켜고 끄기 · 읽어 올 주소**
- [ ] 설정 · 이 프로그램에 **「바뀐 것 지난 목록」** — 띠를 닫은 뒤에도 볼 자리

**정한 것 셋**

1. **둘 자리는 GitHub** — 무료이고 서버를 돌릴 필요가 없어 오늘 됩니다.
   주소는 **설정값**으로 둡니다. 코드에 박으면 자리를 옮길 때 exe 를
   새로 뿌려야 합니다
2. **기본은 켜기.** 설정에서 언제든 끌 수 있고, 무엇이 나가고 안 나가는지
   그 자리에 적어 둡니다
3. **판 번호를 보내지 않습니다.** 파일을 받아 와 **내 컴퓨터 안에서** 견줍니다.
   그래야 「대상자 정보는 물론이고 **몇 판을 쓰는지조차 안 나갑니다**」라고
   적을 수 있습니다 — 통합돌봄 자료를 다루는 곳에 이 문장은 기능보다 값집니다

**판 정보 파일은 이렇게 생깁니다.**

```json
{
  "판": "0.1.2",
  "낸날": "2026-09-05",
  "바뀐것": [
    "청구서 반올림이 지자체 방식과 어긋나던 것을 고쳤습니다",
    "대상자 명단을 인쇄하면 주소 열이 잘리던 것을 고쳤습니다",
    "기록지에 상세주소(동·호수)가 함께 실립니다"
  ],
  "꼭해야하나": false,
  "받는곳": "https://..."
}
```

- **`바뀐것` 은 사람 말로, 한 줄에 하나.** 「버그 수정 및 안정성 개선」은
  아무 말도 안 한 것입니다. 기관은 그걸 읽고 **깔지 말지**를 정합니다
- **`꼭해야하나`** — 셈이 틀렸던 것을 고친 판은 안 깔면 **엉뚱한 청구서가
  어르신께 갑니다.** 그런 판만 `true` 로 두고 띠 색을 달리합니다.
  아무 판에나 붙이면 그 표시가 아무 뜻도 없어집니다
- **글자는 글자로만 그립니다.** 이 내용은 바깥에서 받아 온 것이라
  화면에 **HTML 로 넣지 않습니다**. 길이도 잘라 둡니다
- **`받는곳` 은 화면에 안 보입니다.** 「내려받기」 단추 뒤에 숨깁니다.
  주소를 보여 주면 옮겨 적다 틀립니다
- **`받는곳` 은 판 정보 파일과 같은 곳에서만 받습니다.** 아무 주소나 따라가면,
  그 파일을 고칠 수 있는 사람이 **아무 파일이나 내려받게** 만들 수 있습니다.
  `https` 가 아니거나 자리가 다르면 **단추를 아예 안 그립니다**

**끝난 것으로 치는 기준** — 인터넷을 끊고 켜도 **평소대로 켜지고**(3초 안에
답 없으면 조용히 넘어감), 판 정보 파일의 번호를 올리면 **띠가 뜨며
바뀐 것 세 줄이 그 자리에 보인다.**

> **스스로 바꿔 끼우지 않습니다.** 서명 없는 exe 를 프로그램이 덮어쓰면
> 백신이 랜섬웨어처럼 봅니다. 반쯤 받다 끊기면 **켜지지도 않는 파일**이 남고,
> 그때 기관은 아무것도 못 합니다. 알려 주는 데까지만 합니다.
> 자동 설치는 **코드 서명 인증서**(연 60만~95만 원대)가 전제입니다.
> 기관이 다섯 곳쯤 되면 그때 다시 꺼냅니다.

---

# 다. 편람에 맞추기 — 서식과 셈 (7-라 ~ 7-타)

**2026-08-31 더함.** 「2026년 해남군 통합돌봄 서비스 제공기관 업무편람」을
받아 전문을 훑고 나온 것들입니다.
검토 원문은 `해남편람_검토.md`,
**만들 때 펴 놓을 종이는 `편람반영_구현목록.md`** — 꼭지마다 편람 근거·자료함·
화면·시험·끝난 것으로 치는 기준을 적어 두었습니다.

**번호를 `7-x` 로 단 이유** — 1~18 번은 이미 「3번 하자」처럼 부르고 있어
가운데를 밀어 넣으면 부르던 번호가 어긋납니다. 그래서 **번호는 7 뒤에 매달고,
할 자리는 여기(8번 앞)** 로 둡니다. 6-나·7-가와 같은 방식입니다.

**왜 8번보다 앞이냐** — 여기 아홉은 전부 **군이나 어르신께 나가는 것에 닿습니다.**
파일럿이 첫 기록지와 첫 청구서를 내기 전에 맞아 있어야 합니다.
설명서(8번)는 서식과 셈이 맞고 난 뒤에 써야 다시 안 씁니다.

> **이미 끝난 것** — 단가 세 가지(식사 11,000 · 동행 27,000 ·
> 안전생활환경 생애 100만원)와 방문목욕 빼기는 2026-08-31 에 했습니다.
> `server/seed.ts` 의 시작값이고, 기관은 **설정 → 서비스**에서 고쳐 씁니다.

## 7-라. 기록지를 편람 서식9(세로)로 — **거의 됐습니다 (2026-08-31)**

우리가 쓰던 것은 편람 서식이 아니었습니다. **가로 7칸** vs 편람 **세로 6칸**.
기록지는 달마다 군에 냅니다 — 서식이 다르면 첫 달에 반려됩니다.

- [x] 편람(.hwp) → .hwpx — `편람 hwpx로 바꾸기.bat` (한글에게 시킴)
- [x] 그 .hwpx 에서 **서식9 만 오려** `서식/모니터링 기록지(서식9).hwpx` (37,893바이트)
- [x] `recordbook.ts` — 표 1개·6칸·**4줄**로 다시 맞춤. 제목은 문단이라
      `Hwpx.머리문단글()` 을 새로 만들어 읽음
- [x] 좁아진 칸에서 우리 줄바꿈이 글자를 끊던 것 고침 (`7.⏎30.(1회차⏎)`)
- [x] `서식지킴시험.ts` 다시 씀 — **20가지 통과**
- [x] 옛 가로 서식은 `서식/이전/` 으로. 배포에는 **맨 위 hwpx 만** 들어감
- [ ] **한글에서 열어 눈으로 보기** ← 여기만 남았습니다

**끝난 것으로 치는 기준** — 뽑은 기록지를 편람 서식9 옆에 놓고 겹쳤을 때
**칸 하나 안 다르고**, 채워진 것은 값뿐이다.

> 우리 엔진은 「읽고 안 고치고 저장하면 **1바이트도 안 다름**」까지
> 확인했습니다. 하지만 **한글이 실제로 어떻게 그리는지는 사람 눈**입니다.
> 자세한 것은 `편람반영_구현목록.md` 7-라.

## 7-마. 연간 지원한도 150만원

편람: **1인당 연간 150만원** 안에서 서비스를 짭니다.
**안전생활환경개선은 이 한도 바깥**이고, 따로 **생애 100만원**입니다.

- [x] 자료함에 **연간 한도** 칸 — `region_profile.annual_cap`·`housing_cap` (v16)
- [x] **0 은 「한도를 안 봅니다」**. 150만원은 **편람 값이고 지침에 없어서**
      기준선(지침 표준)은 0 으로 두고 **「우리 지자체 규칙」에만** 넣었습니다
- [x] 대상자 화면에 **남은 한도** 한 줄
- [x] 홈에 **「한도 임박」** 목록 (90% 넘은 사람)
- [x] 안전생활환경개선은 **따로 셈** — `outside_annual_cap`. 주거는 **생애**라
      해를 안 가리고 처음부터 셉니다
- [x] 확정된 청구는 **얼린 단가**(`calc_snapshot`)로 셈 — 단가를 고쳐도 지난 몫이
      안 흔들립니다
- [x] 시험 — `한도시험` 32가지 · `한도화면시험` 10가지
- [ ] 배정·계획을 **짜는 자리에서** 넘으면 알려 주기 (지금은 실적·대상자 화면에서만)

**끝난 것으로 치는 기준** — 150만원을 넘게 짜면 저장 전에 눈에 띄고,
안전생활환경개선을 아무리 넣어도 그 숫자가 안 움직인다.

## 7-바. 30분 단위 환산

편람: **15분 미만 미산정 · 15~45분 → 30분 · 45분 이상 → 1시간.**
지금 우리는 「횟수 × 단가」라 **40분을 1시간으로 칩니다.** 돈이 틀립니다.

- [x] `money.ts` 에 **자르는 함수 하나** — `인정분()`·`인정시간()` (2026-08-31)
- [x] **시험을 먼저** 썼습니다 — `환산시험` 43가지
- [x] 실적·정산·청구서·월한도가 **다 그 함수 하나만** 봅니다
- [x] 시간제 서비스(가사·동행)에만. **식사(끼) · 이미용(회)** 는 안 건드림
- [x] `delivery.minutes` (v17). **비어 있으면 예전처럼 한 번 = 한 시간**
- [x] **군에 확인(0번)** — **답 나왔습니다 (2026-09-08).** 설정의
      **반올림 켬/끔**으로 이미 조절되는 것이라 따로 만들 것이 없습니다.
      지금대로 **30분마다 되풀이** (1시간 15분 → 1시간 30분)

**끝난 것으로 치는 기준** — 40분 찍힌 실적이 청구서에 **30분**으로 나온다.

## 7-사. 서비스별 월 한도 · 개시일부터 2개월

편람: 가사 **월 12시간** · 식사 **월 8회** · 동행 **월 6시간·월 2회** ·
이미용 **월 1회**. 그리고 **개시일부터 2개월** 안에서 씁니다.

- [x] 서비스마다 **월 한도** 칸 — `monthly_cap_minutes`·`monthly_cap_count` (v18).
      동행지원은 **시간과 횟수 둘 다** 걸려서 칸이 둘입니다
- [x] 실적에서 넘으면 알려 주기 — 「이 달 3시간/6시간 · 3/2회 넘음」,
      **무엇 때문에 넘었는지** 밑줄로 갈라 보임. **막지는 않습니다**
- [x] 편람 값은 **「우리 지자체 규칙」에만**. 기준선(지침 표준)은 0 = 안 봄
- [x] 시험 — `월한도시험` 25가지 · `월한도화면시험` 7가지
- [ ] **2개월**은 이미 `period_months` 로 셈하고 있음 — 한도와 맞물리는지 확인

## 7-아. 노쇼와 사전취소 가르기

지금 미제공은 **한 가지**입니다. 편람은 둘을 갈라 놓습니다 —
**사전취소**는 비용이 안 나가고, **노쇼**(당일 부재·거부)는 **비용이 나가고
한도도 깎입니다.** 갈라 놓지 않으면 어르신 한도가 조용히 새거나,
기관이 나간 인건비를 못 받습니다.

- [x] 미제공 사유에 **「노쇼」/「사전취소」/「기관사정」/「기타」** 갈래
      (2026-08-31 · `server/미제공.ts` · `delivery.miss_kind`·`notified_on` · v19)
- [x] 노쇼는 **정산에 태우고 한도에서 뺌** · 사전취소는 둘 다 안 함
      — `한도에드나()` 한 곳과 SQL 조각 `돈드는실적SQL` 한 곳이 정합니다.
      청구서·연간한도·월한도·생애한도가 **모두 그것만** 봅니다
- [x] **옛 미제공은 갈래가 비어 있으므로 「기타」** — 지난 청구서가 안 흔들립니다
      (마이그레이션에 UPDATE 를 안 넣었습니다. 지어내면 없던 돈이 생깁니다)
- [x] 사유를 고르면 갈래를 **미리 골라 줌** (부재·거부 → 노쇼 · 입원·본인요청 →
      사전취소 · 기관사정 → 기관사정 · 기상·재해 → 기타).
      **사람이 고친 것이 언제나 이깁니다**
- [x] 사전취소인데 **하루 전보다 늦게 알린 것**은 「늦음」으로 알려 줌 — **막지 않음**
- [x] 실적 달력에 갈래 칩 · 위 숫자 칸에 「노쇼」 · **눌러서 고치기**
- [x] 통계에 **갈래별** 셈 (`갈래` · `노쇼합`) — 사유와 **다른 축**입니다.
      사유는 「무슨 일이 있었나」, 갈래는 **「돈이 나갔나」**
- [x] 시험 — `노쇼시험` 54가지 · `노쇼화면시험` 11가지
- [ ] **기록지(서식9)·서식8 에 노쇼를 어떻게 적을지** — B 단계에서 서식을 채울 때
- [x] **노쇼는 월 한도든 연간 한도든 무조건 깎습니다** (2026-08-31 무무 님 결정).
      편람 계약서 4조가 글자로 적은 것은 「연간 지원한도액」뿐이지만, 돈이 나간
      몫을 월 한도에서 빼지 않으면 청구서와 월 한도가 다른 말을 합니다.
      → **지금 코드가 그렇게 하고 있어 고칠 것이 없습니다**
- [x] **노쇼가 어느 서비스에 붙는가 — 답 나왔습니다 (2026-09-01 무무 님)**

      **어디에도 안 붙습니다.** 현장은 이렇게 돕니다 —

      · **가사·동행·이미용** — 이용자 사정이면 **일정을 사전 조율**해서 다시 잡습니다.
        노쇼로 처리하지 않습니다. (계약서 4조는 「대면이 아니면 서비스를 아예
        제공할 수 없다」를 정의하려고 있는 조항이지, 현장 처리 방식이 아닙니다)
      · **식사지원** — 축이 아예 다릅니다. **비대면 배달**은 **미제공이 아니라
        제공**입니다 — 음식이 이미 만들어져 나갔으니 **비용이 무조건 발생**합니다.
        그리고 **비대면 3회 연속이면 통합돌봄 전체 중지**(계약서 5-2조).

- [x] **A-4. 식사지원 대면/비대면 — 끝** (2026-09-01 · v20)

      **틀리게 세고 있던 것을 바로잡았습니다.** `syncMeal()` 이 **미제공 연속**을
      세었는데 편람이 세는 것은 **비대면 배달 연속**입니다.
      안 간 것과 놓고 온 것은 다른 일입니다.

      · `delivery.face_to_face` — 1 대면 · 0 비대면 · **비면 대면**(원칙이 대면).
        옛 실적으로 **없던 중지 경고가 안 뜹니다**
      · 셈은 `server/대면배달.ts` **한 곳**이 합니다 (`비대면연속`·`비대면다시셈`)
      · **비대면도 돈이 나갑니다** — 음식이 이미 만들어졌으니 제공입니다
      · **미제공은 건너뜁니다** — 안 간 날은 배달이 아니라 연속에 안 듭니다
      · 실적 달력 식사지원 제공 칸에 「대면 / 비대면 배달」 칩, 눌러서 바꿈
      · 위 숫자 칸과 서비스 줄에 「비대면 배달 N」
      · 대상자 화면 「연속 비대면 N/3」 — **읽기만** 합니다
      · 시험 — `대면배달시험` 36가지 · `대면배달화면시험` 12가지

- [x] **손으로 세던 단추를 없앴습니다** — 겹쳐 쓰던 버그도 같이 사라졌습니다.
      전에는 대상자 화면 「미수령 기록」이 숫자를 직접 올렸는데, 다음 식사 실적이
      들어오면 `syncMeal()` 이 **말없이 덮어썼습니다.** 이제 숫자는 **실적에서만**
      나옵니다. 그 자리에는 「다시 세기」만 남겼습니다.

- [x] **노쇼 갈래는 기본 꺼짐** (`service.noshow_billable`, 기본 0)

      해남 현장은 안 씁니다 — 가사·동행·이미용은 **일정을 사전 조율**해 다시 잡고,
      식사는 위의 대면/비대면이 그 자리입니다. 꺼져 있으면 **갈래를 아예 안 묻고**
      고르개에도 노쇼가 안 뜹니다.
      없애지 않은 이유 — 계약서 4조가 **어르신께 인쇄되어 나가는 종이**(서식2)에
      적혀 있어서, 그 종이를 뽑으면서 적을 데가 없으면 프로그램이 제 종이와
      어긋납니다. 다른 지자체는 **설정 · 서비스**에서 켭니다.

- [x] **한도에 닿으면 경고창** — 「비대면 수령 횟수가 3회 되었습니다.
      서비스 중지가 필요합니다.」 **프로그램이 상태를 안 바꿉니다.**
      중단은 지자체와 협의할 일이라 사람이 대상자 화면에서 누릅니다
      (시험이 「상태가 「이용」 그대로인가」를 못박아 봅니다)
- [x] **3회는 설정값** — `region_profile.f2f_limit` (v21). 지침 개정 대비.
      0 은 「안 봅니다」. **기준선에도 3 을 심었습니다** — 계약서 5-2조는
      어느 기관이나 어르신과 맺는 것이고, 돈이 아니라 **서비스가 끊기는 경고**라
      꺼 두면 끊길 때까지 아무도 모릅니다
- [x] **처음 깔면 「우리 지자체 규칙」으로 시작** (2026-09-01 · 큰 것)
      그동안 최초 설정이 **잠긴 기준선**을 골랐습니다. 그 규칙은 한도가 전부
      0(= 안 봄)이라, **기관마다 한도 경고가 꺼진 채로 시작**했습니다.
      파일럿 기관이 실제로 그 상태였습니다

**끝난 것으로 치는 기준** — 놓고 온 배달을 찍으면 대상자 화면 「연속 비대면」이
따라 오르고, 3회에서 **경고창**이 뜨고, 그 배달의 비용은 그대로 나가고,
**상태는 사람이 바꾼다.** → 넷 다 됩니다.

## 7-자. 본인부담금 영수증을 **서식7 로** 갈아 끼우기  ★ 끝 (2026-09-01 · B-1)

지금은 `receipt.ts` 가 **HTML 로 직접 그립니다.** 조항 글자는 맞아도
글꼴·선·여백이 우리 값이라 **같은 서식이 아닙니다.** 원본으로 바꿉니다.

- [x] **「이름표 뒤에 값만 붙이기」** 를 새로 만들었습니다 ★
      `server/서식채우기.ts` — **빈칸에 값을 얹고 글자 수를 그대로** 둡니다.
      그래야 뒤따르는 「원」·「(서명)」이 제자리에 남습니다.
      · `얹기(글, 이름표, 값, 왼쪽|오른쪽)` — 금액은 오른쪽(「원」 앞)
      · `자리바꾸기(글, "◯◯◯◯", 값)` — **자리 표시는 그 자리를 대신**합니다.
        뒤에 붙이면 「◯◯◯◯ 해남돌봄센터」가 되어 종이가 우스워집니다
      · `날짜얹기` — 「20   년    월     일」·「 년   월   일」 두 모양
      · **못 찾으면 `null`** → 부르는 쪽이 터뜨립니다.
        조용히 넘어가면 **빈 영수증이 어르신께 갑니다**
      · `Hwpx.얹기 / 자리바꾸기칸 / 날짜얹기칸` — **run 하나의 글자만** 고칩니다.
        문단을 통째로 다시 쓰면 run 마다 다른 글자 모양(밑줄·굵기)이 뭉개집니다
      · 길이를 지킨 문단은 **줄 정보를 안 건드립니다** — 여전히 맞는 기록입니다
      · **B-2 계약서·B-3 비용총괄에서 그대로 씁니다**
      · 시험 `서식채우기시험` 23가지
- [x] `server/영수증.ts` — 서식7 원본을 채웁니다 (`서식자리.ts` 로 서식 폴더 한 곳)
      서식7 은 「거래번호 :」와 값이 **같은 칸**에 있어, 지금처럼 칸을 통째로
      갈아 끼우면 **이름표까지 지웁니다.** 기록지의 「○ → ☑」와 같은 생각으로
      서식에 적힌 줄을 그대로 두고 `:` 뒤만 채웁니다
- [x] 서비스 가격 / 정부 지원금 / 본인 부담금 세 줄 — **세 몫을 더하면 서비스
      가격이 됩니다.** 종이가 스스로 맞는지 시험이 봅니다
- [x] 한 장에 두 벌(제공기관 보관 · 이용자 보관) — 같은 값을 두 번 채웁니다
- [x] **서명란은 비웁니다** — (서명)과 (인) 둘 다. 어르신과 대표가 손으로
      하는 자리입니다. 우리가 찍으면 서명을 대신한 것이 됩니다
- [x] `영수증시험.ts` — 54가지. **실적 → 확정 → 수납 → 영수증**이 한 줄로
      이어지는지까지 봅니다 (정산 화면이 말한 금액과 종이가 같은가)
- [x] **본인부담금이 0원(면제)이면 영수증을 안 냅니다** — 받은 적 없는 돈의
      영수증은 거짓 서류입니다
- [x] 정산 화면 **「수납」 뒤에만** 「영수증 (서식7)」 단추. 「수령하였습니다」라고
      적힌 서식이라 돈을 받기 전에 뽑으면 거짓말이 됩니다
- [ ] **한글에서 열어 사람 눈으로 보기** — 우리 엔진은 「서식이 안 바뀌었다」까지
      확인했지만, 한글이 실제로 어떻게 그리는지는 사람 눈입니다
- [ ] 옛 `receipt.ts` 는 **지우지 않고** 남겨 둡니다

## 7-차. 계약서를 **서식2 로** 갈아 끼우기 — 대상자 등록 시 출력

셈은 이미 다 있습니다(`contract.ts`). **그리는 자리만** 바꿉니다.

- [ ] 표 넷에 값 채우기 — 이용자 / 제공기관 / 계약 사항 / 서명란
- [ ] **「□ 확인」은 비웁니다** ★ 어르신이 그 자리에서 체크하는 칸입니다.
      우리가 미리 찍으면 **받지도 않은 확인을 받은 것으로** 만듭니다
- [ ] 서명란도 비웁니다
- [ ] `계약서시험.ts` — 조항 글자가 편람과 한 자도 다르지 않은가

## 7-카. 서식8 비용총괄 — 정산이 끝나면 저절로

**정산 확정 뒤**에 대상자별로 취합해 자동으로 만듭니다. 확정 전에 뽑으면
숫자가 흔들립니다. 정산 화면의 「확정」 다음 자리에 둡니다.

- [ ] **진행시간**(실제) · **제공시간**(30분 환산) 두 칸 — **7-바 가 먼저**
- [ ] **투입인원** 칸 — 우리에게 없던 칸입니다
- [ ] **만 나이** — 생년월일에서 셉니다
- [ ] **※ 서비스비용 지원대상자만** — 전액 자부담은 여기 안 들어갑니다
      (반대로 서식3 가사지원 기록지는 **전액 자부담도 작성·제출**입니다.
       두 서식의 범위가 다릅니다)
- [ ] `비용총괄시험.ts` — 확정 전에는 못 뽑게 막는가

## 7-타. 서약서 서식1-1 ~ 1-6 — 수행기관이 씁니다

- [ ] **먼저 1-1~1-5 가 서로 무엇이 다른지** 나란히 놓고 확인 ★
      제목이 다섯 개 다 같아 보입니다
- [ ] 기관 정보와 날짜 채우기
- [ ] **1-6 은 제공인력마다 한 장** — 종사자 목록에서 골라 여러 장 한 번에
- [ ] 서명·직인은 비웁니다

---

# 라. 관리자 사용설명서 (8)

## 7-하. 최초 설정 마법사 — **필수 설정까지 마쳐야 홈이 열립니다** ★ 무무 님 안

**2026-09-01 정함.** 지금은 기관정보·관리자만 넣으면 곧장 홈으로 갑니다.
그래서 단가·한도·공휴일이 **손도 안 댄 채로** 쓰이기 시작합니다.
설명서에 적어도 안 읽힙니다. **순서로 막습니다.**

```
기관 정보  →  관리자 등록  →  ★ 필수 설정  →  홈
```

**핵심 — 새로 적는 화면이 아니라 「확인하는 화면」입니다.**
편람 기본값이 **이미 다 채워져 있습니다.** 맞으면 그냥 「이대로 시작」을
누르면 되고, 다른 지자체면 그 자리에서 고칩니다. **1분.**

### 무엇을 묻나 (한 화면, 위에서 아래로)

- [ ] **① 우리 지자체 이름** — 규칙 이름을 「○○군 규칙」으로.
      기본값이 「우리 지자체 규칙」이라 그대로 두면 남의 프로그램처럼 보입니다
- [ ] **② 서비스 단가** — 다섯 줄. 편람 값이 채워져 있음
      (가사 24,000 · 식사 11,000 · 동행 27,000 · 이미용 24,000 · 안전생활환경 실비)
- [ ] **③ 지원한도** — 1인 연간 150만원 · 안전생활환경 생애 100만원
- [ ] **④ 서비스별 월 한도** — 가사 12시간 · 식사 8회 · 동행 6시간과 2회 · 이미용 1회
- [ ] **⑤ 연속 비대면 배달 한도** — 3회 (계약서 5-2조)
- [ ] **⑥ 본인부담 구간** — 면제 · 20% · 100%. 지자체마다 다릅니다
- [ ] **⑦ 공휴일** — 그해 관공서 공휴일. **여기만 「나중에」를 둡니다** —
      달력을 손에 들고 있어야 넣을 수 있고, 휴일 할증이 꺼져 있으면 급하지 않습니다.
      대신 **홈에 「올해 공휴일이 하나도 없습니다」**를 띄웁니다

### 못박을 것

- [ ] `organization.setup_done_at` — 비어 있으면 **어느 주소로 들어와도**
      이 화면으로 보냅니다 (지금 `needsSetup()` 이 하는 것과 같은 방식)
- [ ] **건너뛰기 단추를 두지 않습니다.** 값이 다 채워져 있어 누를 것이 확인뿐입니다.
      빠져나갈 구멍을 두면 반드시 그리로 갑니다
- [ ] 화면마다 **왜 묻는지 한 줄** — 「이 값이 틀리면 청구 금액이 틀립니다」
- [ ] 마친 뒤에도 **설정에서 언제든 고칠 수 있습니다.** 여기는 시작점일 뿐
- [ ] 이미 쓰고 있는 기관은? — `setup_done_at` 이 없으니 다음에 켤 때 이 화면이
      한 번 뜹니다. **그게 맞습니다** — 확인한 적이 없는 기관이니까요
- [ ] 시험 — 「설정을 안 마치면 홈이 안 열린다」 · 「마치면 다시 안 뜬다」 ·
      「고른 값이 실제로 규칙에 들어갔다」

**끝난 것으로 치는 기준** — 빈 자료함으로 처음 켜서 **설명서를 한 줄도 안 읽고**
끝까지 갔을 때, 단가·한도·부담률이 우리 지자체 값으로 들어가 있다.

> **이건 E(넘기기) 단계 일입니다.** 남에게 넘길 때 필요한 것이라 B·C·D 보다
> 뒤입니다. 다만 **E 의 맨 앞**에 둡니다 — 설치 안내문·아이콘보다 먼저입니다.

---

## 8. 관리자 사용설명서

목표 — **업무를 처음 맡은 사무직원이 첫 달을 혼자 마무리하는 것.**
화면별 안내는 이미 프로그램 안에 있으므로, **화면 사이를 잇는 순서**에 집중합니다.

- [ ] **맨 앞장은 「설정부터」** ★ 무무 님 못박음 (2026-09-01)
      기관에 처음 드릴 때 **설정을 먼저 훑는다**는 것이 **1장**이어야 합니다.
      단가 · 지원한도 · 월 한도 · 연속 비대면 한도 · 공휴일 다섯 가지.
      (`설치 안내.txt` 5번에 이미 넣었습니다 — 설명서는 그것을 펼쳐 씁니다)

      > **다만 설명서만으로는 부족합니다.** 아무도 안 읽고, 읽어도 무엇이
      > 꺼져 있는지 모릅니다. 그래서 **최초 설정이 「우리 지자체 규칙」으로
      > 시작하도록 기본값을 고쳤습니다** (2026-09-01 · `설정시험` 6번 마당).
      > 남은 것은 아래 「설정 점검」입니다.

- [ ] **홈에 「설정 점검」 한 줄** — 프로그램이 스스로 말하게 (E 단계)
      단가가 0 인 서비스가 있거나 · 공휴일이 한 개도 없거나 ·
      기준선(잠긴 규칙)을 쓰고 있으면 홈에 띄웁니다.
      **문서는 안 읽혀도 홈은 매일 봅니다.**

- [ ] **월말 순서 한 장** (실적 확인 → 정산 확정 → 청구서 → 기록지 → 제출)
- [ ] **청구 시점 세 가지**의 뜻과 고르는 기준
      (잘못 고르면 엉뚱한 기간이 찍힌 청구서가 어르신께 갑니다)
- [ ] **백업과 되돌리기** — 자리 정하기 · 주기 · USB 를 오갈 때 주의할 점
- [ ] **한글·PDF 내보내기** — 경고창이 뜰 때 무엇을 누르는지

**끝난 것으로 치는 기준** — 이 설명서만 보고 한 달치를 처음부터 끝까지 해냈다.

---

# 마. 현장앱 — 제공인력 휴대폰 (9~13) ★ 제일 큼

**이것이 없으면 종이가 그대로 남습니다.**
셋으로 쪼갰습니다. **9번만 되어도 종이 한 장은 줄어듭니다.**

## 9. 현장앱 — 최소 (오늘 갈 곳 + 방문 찍기)

- [ ] 제공인력 로그인 → **오늘 갈 곳 목록**
      (대상자명 · 주소 · 연락처 · 서비스명 **네 가지는 반드시**)
- [ ] **방문 전 확인**(반려동물 · CCTV · 주의사항) 그 자리에 노출
- [ ] **방문 찍기** — QR + 위치 오차반경 → 제공 / 미제공(사유)
- [ ] **미제공 사유는 「노쇼」/「사전취소」를 갈라 받습니다** (7-아 와 짝)

### 9-가. 시작·종료 단추 — 잘못 누르면 돈이 날아갑니다 ★ 못박음

**2026-08-31 무무 님 지시.** 현장에서 지원인력이 **종료 단추를 잘못 눌러
곧장 끝나 버리는 사고**가 없어야 합니다. 30분 환산(7-바)과 맞물립니다 —
40분에 끊으면 **30분만 인정**되고, 그 차이는 기관이 떠안습니다.
14분에 끊으면 **아예 0원**입니다.

- [ ] 종료 단추를 누르면 **눈에 확 띄는 확인 창** — 그냥 안 끝납니다
- [ ] 창에 **남은 시간을 사람 말로**
      「**서비스 제공시간 충족까지 12분 남았습니다.**
        지금 끝내면 **30분**으로 인정됩니다. (예정 1시간)」
- [ ] **다음 눈금까지 남은 시간**을 셈해 보여 줍니다 (15분·45분 고비)
- [ ] 충족했으면 **초록으로 「1시간 채웠습니다」** — 겁주는 창이 아니라
      **끝내도 된다고 알려 주는 창**이기도 합니다
- [ ] 단추 두 개를 **멀리** 두고 **「그만두기」가 기본**
      (누르던 자리에 「끝내기」가 오면 두 번 눌러 끝나 버립니다)
- [ ] **시작 단추도 같이** — 이미 시작한 건을 또 누르면 알려 주기
- [ ] 그래도 끝내면 **왜 일찍 끝냈는지** 한 줄 받기 (일지에 그대로 남음)

**끝난 것으로 치는 기준** — 40분째에 종료를 눌렀을 때 창이 뜨고,
「30분으로 인정」이라 적혀 있고, 실수로 두 번 눌러도 안 끝난다.

**끝난 것으로 치는 기준** — 제공인력이 휴대폰으로 찍은 것이
사무실 제공실적 달력에 그대로 뜬다.

## 10. 현장앱 — 일지 · 사진 · 서명

- [ ] 일지 작성 — **일지 글만 나중에 수정 가능.** 제공·미제공 판정은 못 바꿈
      (판정이 흔들리면 청구가 흔들립니다)
- [ ] 사진 · 서명 첨부

### 10-가. 서식3~5 가 **저절로 완성됩니다** ★ 못박음 (2026-08-31)

**무무 님 지시** — 지원인력이 휴대폰으로 실행하면 **서식이 자동으로
완성되어 출력만** 하면 되어야 합니다.

| 서식 | 무엇 | 눈여겨볼 것 |
|---|---|---|
| 3 | 가사지원 제공기록지 (21줄) | **전액 자부담 이용자도 작성·제출** |
| 4 | 식사 관리 제공기록지 (40줄) | 한 달치가 한 장 |
| 5 | 병원동행·이미용 제공기록지 (13줄) | 두 서비스가 한 장 |

**지원인력은 서식을 보지도 않습니다.** 방문을 찍고 일지를 쓰면
그것이 서식의 칸으로 들어갑니다. 사무실에서 출력합니다.
서식을 화면에 띄워 사람이 채우게 하면 **종이가 화면으로 옮겨간 것뿐**입니다.

- [ ] **9번을 만들기 전에** 세 서식의 칸을 먼저 펴 봅니다 ★
      나중에 「그 값을 안 받았네」가 나오면 **다 다시 만들어야 합니다**
- [ ] 원본은 이미 `서식/` 에 있습니다 (서식3·4·5)
- [ ] 사무실 화면에서 그 달치 **한 번에 뽑기** (기록지 묶음과 같은 길)

## 11. 현장앱 — 오프라인 저장 · 배포

- [ ] **오프라인에서 찍고 나중에 올리기** (시골은 전파가 끊깁니다)
- [ ] 위치정보 동의는 **계약 단위**. 퇴사하면 자동 철회
- [ ] 안드로이드 · 아이폰 설치 안내 (QR 또는 PDF)

**끝난 것으로 치는 기준** — 비행기모드로 찍고 나중에 켰을 때 그대로 올라간다.

## 12. 권한(`perm.ts`) 전체 재검토

현장앱이 붙어야 실전에 들어가는 규칙이라 **여기서 합니다.**

- [ ] 제공인력이 **자기 대상자만** 보게
- [ ] 메모장을 사무직원에게 열지 정하기
- [ ] **별표1 제공인력 자격 기준**을 고르는 값으로 — 지금은 글로 받습니다.
      고르는 값이면 배정할 때 「이 사람은 이 서비스를 못 합니다」를 압니다
      (가사=요양보호사·활동지원사·가사민간자격+실무 6개월…,
       식사=영양사·조리사·요양보호사·**배달 한정 운전면허증 소지자**…,
       이미용=면허 소지자)

### 12-가. **외부업체 계정** — 사용자 유형이 하나 더 생깁니다 ★★ (2026-08-31)

**무무 님 지시** — 서식6-1·6-2 는 **등록된 외부업체가 관리자에게 배정받은
계정으로 휴대폰에서** 작성하고, **자동으로 완성되어 출력만** 합니다.

안전생활환경개선(문턱 제거·안전손잡이)은 우리 기관 사람이 아니라
**시공업체**가 합니다. 지금 우리에게 그 사람들이 들어올 자리가 없습니다.

| 지금 | 앞으로 |
|---|---|
| 관리자 · 사무직원 · 제공인력 | + **외부업체** |

- [ ] 자료함에 **업체** 표 — 상호·대표·연락처·사업자번호·계약 여부
- [ ] 관리자가 **계정을 만들어 그 건에 배정**
- [ ] 업체 화면은 **아주 좁게** — 배정받은 건 · 6-1 쓰기 · 6-2 쓰기.
      대상자 명단도, 다른 업체 건도 **안 보입니다**
- [ ] **hwpx 에 그림 넣기** ★ 지금 우리 엔진은 **글자만** 넣습니다.
      6-1 은 「관련 사진」, 6-2 는 **위치별 전/중/후 세 장**이라
      여기서 처음 필요해집니다. **이것이 이 꼭지에서 제일 큰 일입니다**
- [ ] 6-2 의 **「※ 견적서 첨부」** — 파일 붙이기
- [ ] 업체가 보는 것은 **이름 · 주소 · 연락처** 셋 (2026-08-31 무무 님 확인).
      시공하러 가려면 **전화로 시간을 잡아야** 하므로 연락처까지 봅니다.
      그 밖은 **안 보여 줍니다** — 생년월일·성별·본인부담 등급·상태·메모·다른 서비스
- [ ] 업체 계정으로는 **아무것도 못 지웁니다**

**끝난 것으로 치는 기준** — 시공업체가 자기 휴대폰으로 전/중/후 사진을 올리면
사무실에서 서식6-2 가 **채워진 채로** 나온다.

## 13. 동기화 재검토 — **길은 이미 정했습니다 (2026-08-28)**

> **현장앱이 붙으면 사무실 PC 가 서버가 되어 복사본이 하나가 됩니다.**
> 동기화는 복사본이 둘일 때만 필요한 기능이라, 지금 만들면 그때 버립니다.
> — `남은일.md` 「정한 것 (2026-08-28)」

**그러니 13번은 「만들 것」이 아니라 「안 만들기로 한 것」입니다.**
휴대폰은 사무실 PC 에 붙고, 자료함은 그 PC 안에 하나뿐입니다.
전파가 끊기는 동안만 **휴대폰에 담아 두었다 돌아와서 올립니다**(11번).

- [x] 사무실 PC 가 서버가 되는 구조로 갈지 판단 — **그렇게 갑니다**
- [ ] **기관 망 확인** — 같은 와이파이에서 휴대폰이 사무실 PC 에 붙는지.
      막히면 그때 다시 봅니다 (그때도 오프라인 담아 두기는 그대로 씁니다)
- [ ] 자동번호가 겹치는 문제는 **안 생깁니다** — 번호를 만드는 곳이 한 곳뿐이라

---

# 바. 상담 기록 (14~15)

`수익화.md` 에서 정한 **제1 기능인데 아직 없습니다.**
통합돌봄 사업이 바뀌어도 **상담 기록이 서 있으면 쓰던 사람은 계속 씁니다.**

## 14. 상담 기록 — 모양 정하기

- [ ] 위노트(weenote.kr) 상담일지를 보고 **우리 모양** 정하기 (회기 기록 · 사례 관리)
- [ ] 무료로 열 범위 정하기

## 15. 상담 기록 — 만들기

- [ ] 대상자 한 사람 아래에 상담 기록이 쌓이는 구조

---

# 사. 그 뒤 (16~18)

## 16. 열람 기록

- [ ] 누가 언제 무엇을 열어 봤는지 (지자체 점검에서 물을 수 있음)

## 17. 기능 잠금 자리 · 오프라인 열쇠

- [ ] `plan.ts` — `수익화.md` 참고
- [ ] 인터넷 없이 도는 프로그램이라 **서버에 물어볼 수가 없습니다**

## 18. 학습 커리큘럼

- [ ] 여기까지 만든 것을 바탕으로 기초부터 되짚는 순서 정리

---

## 안 하기로 정한 것 — 되짚지 않게

- **「시간」 축을 안 만듭니다.** 지침에 서비스별 소요시간이 없습니다.
  없는 값을 만들어 채우면 그것이 지자체 서류로 나갑니다
- **엑셀 출력을 안 만듭니다.** 매달 내는 것은 기록지 하나뿐입니다(2026-08 확인).
  우리 안에서 볼 자료는 **화면 그대로 인쇄**로 씁니다
- **13번 전에는 동기화를 안 만듭니다.** 현장앱이 붙으면 구조가 바뀌어 버리게 됩니다.
  그동안은 백업·되돌리기로 감당하고 **합쳐 주는 척은 하지 않습니다**
- **제출 서식은 손대지 않습니다.** 값만 채우고 모양은 원본 그대로

---

## 이미 끝난 것 — 다시 손대지 않게

단계 0~5 와 그 뒤의 곁가지까지. 자세한 경위는 `남은일.md`.

대상자 · 의뢰 엑셀 접수(암호 파일 · 차수 대사 · 재의뢰) · 종사자(계정 · 비밀번호
재설정 · 퇴사 · 자격 · 교육 · 배상책임보험 · 임금 별도관리) · 배정과 주간계획 ·
제공실적 달력(제공 / 미제공 / 사유) · 정산(회차 × 단가 · 절삭 · `calc_snapshot` ·
청구 갈라짐 · 생애한도) · 청구서 · 영수증 · 계약서 PDF · 기록지 HWPX + PDF 일괄 ·
통계(월별 · 서비스별 · 대상자별 · 제공인력별 · 행정동별 · 미제공 사유 · 생애한도) ·
화면 인쇄 얼개 · 메모장 · 공휴일 · 기관 직인 · 설정의 서비스 신규 등록 ·
백업 · 되돌리기 · 폴더 고르개 · 주소 찾기 · 글자 크기 · 대상자 상세 저장 단추

---

## 안 만들기로 한 것 (2026-09-07)

### 자료 통째로 내보내기 (CSV·ZIP) — **안 만듭니다**

> (무무) 「구독을 하지 않던, 사업이 끝나던, 이 프로그램은 **관리자의 PC에
> 남기** 때문에 쓰는 것은 안 되지만 **읽는 것은 되잖아.** 굳이 만들 필요
> 없어. 기관에서 굳이 보관이 필요하다고 할 경우엔 **인쇄물로 출력**할
> 테니 굳이 기능을 늘려 복잡하게 만들 필요는 없어.」

「구독이 끝나면 자료를 인질로 잡는 것 아니냐」를 걱정해 넣으려던 기능인데,
**전제가 틀렸습니다.** 이 프로그램은 기관 PC에 그대로 남고 자료함도 그
PC에 있습니다. 읽는 것은 언제든 됩니다. 종이가 필요하면 **인쇄 일곱 화면**
(v1.14.31)으로 뽑습니다.

**만들었으면 생겼을 문제** — 내보낸 CSV 는 암호 없는 개인정보 덩어리라
바탕화면에 굴러다니게 됩니다. 그러면 반출 이력 관리·파기 안내가 따라붙고,
개인정보처리방침에 적을 것이 늘어납니다. **안 만드는 것이 안전하기까지 합니다.**

나중에 「군이 엑셀로 달라」 같은 요구가 실제로 오면 그때 다시 봅니다.

---

# 업데이트 안내 — 됐습니다 (2026-09-08, v1.16.31)

> (무무) 「**업데이트 내용이 있으면 관리자가 프로그램 실행 시 업데이트
> 알림과 함께 자동으로 업데이트 할 수 있는 방법은 없을까?**」

**됩니다. 만들었습니다.** 관리자는 **단추 하나**를 누릅니다.

## 제가 「안 됩니다」라고 적어 두었던 것 중 둘은 틀렸습니다

| 옛 근거 | 실제 |
|---|---|
| ① 실행 중인 exe 는 못 덮는다 | 맞지만 **꺼진 뒤에 바꾸면** 됩니다 — .bat 이 기다렸다 바꿉니다 |
| ② 받다 끊기면 켜지지도 않는 파일이 남는다 | **SHA-256 지문 대조**로 완전히 막힙니다. 안 맞으면 아예 안 바꿉니다 |
| ③ 백신이 랜섬웨어로 본다 | **이건 진짜 남습니다.** 서명 인증서가 없어서 |

## 흐름

```
  켤 때 (하루 한 번)
    version.json + .sig 를 읽는다 → 서명 확인 → 판 견주기
      ↓ 새 판이면
    뒤에서 zip 을 받는다 → 지문 대조 → data\update\payload\ 에 푼다
      ↓
    띠: 「새 버전(1.16.31)의 업데이트가 준비됐습니다」 + 바뀐 것
      ↓ 관리자가 「지금 업데이트」
    pid 를 적고 → 갈아끼우기.bat 을 띄우고 → 스스로 꺼진다
      ↓ (검은 창 8초)
    옛 exe → data\update\old\  ·  payload → 제자리  ·  다시 켜기
      ↓ 새 판이 켜지면
    뒷정리: 옛 exe 를 「이전판\통돌Note_1.15.31.exe」로 옮긴다
```

**무거운 것은 켜져 있는 동안 다 끝내 둡니다.** 마지막에 남는 것은
**폴더 옮기기 두 번과 켜기 하나**, 몇 초짜리입니다.

## 만든 것

| 파일 | 무엇 |
|---|---|
| `server/업데이트.ts` | 읽기 · 서명 확인 · 받기 · 풀기 · 갈아끼우기 · 뒷정리 |
| `server/업데이트열쇠.ts` | 공개 열쇠와 기본 주소 (열쇠 만들기가 채웁니다) |
| `통돌Note 갈아끼우기.bat` | 이름 바꾸기 두 번과 켜기. **한글 한 글자 없음** |
| `서명열쇠 만들기.bat` · `도구/서명열쇠만들기.ts` | 한 번만. Ed25519 한 쌍 |
| `도구/버전정보만들기.ts` | 만들기.bat 끝에 붙어 `올릴것\` 을 만듭니다 |
| `이번에 바뀐 것.txt` | 판마다 새로 적습니다. **비면 만들기가 멈춥니다** |
| `web/src/lib/업데이트띠.tsx` | 화면 맨 위의 띠 |
| `업데이트 올리는 법.md` | **무무 님이 보는 종이** |
| `업데이트시험.ts` | 81가지 |

## 못박은 것

1. **판정보에 서명합니다 (Ed25519).** 저절로 갈아 끼우는 순간부터,
   판정보를 고칠 수 있는 사람은 **기관 PC 에 아무 프로그램이나 깔 수
   있게** 됩니다. 서명이 있으면 저장소가 털려도 남이 만든 판은 안 깔립니다.
   개인 열쇠는 **프로젝트 폴더 바깥**(`%USERPROFILE%\통돌Note-서명열쇠\`)
   — 안에 두면 언젠가 zip 에 딸려 나갑니다.
2. **열쇠가 없으면 확인을 아예 안 합니다.** 「열쇠가 없으니 그냥 믿자」로
   빠지는 길을 안 둡니다. 그 길이 있으면 언젠가 그리로 갑니다.
3. **묻지 않고 저절로는 안 합니다.** 기술이 아니라 업무 때문입니다 —
   밤사이 셈이 바뀌면 「어제와 오늘 청구액이 왜 다르냐」에 답할 사람이
   없어집니다. 바뀐 것을 읽고 **누른 흔적**이 남아야 합니다.
4. **판 번호를 안 보냅니다.** 파일을 받아 와 이 컴퓨터 안에서 견줍니다.
   「대상자 정보는 물론이고 **몇 판을 쓰는지조차 안 나갑니다**」
5. **.bat 이 다루는 길은 전부 영문** (`data\update\payload\`).
   기관이 보는 이름(`이전판\`)은 새 판이 켜진 뒤 **프로그램이** 붙입니다.
6. **관리자만** 봅니다. 사무직원 화면에 띠가 뜨면 관리자 모르게 판이 바뀝니다.
7. **올리는 파일 이름은 영문.** GitHub 이 한글을 점(.)으로 바꿉니다.
8. **태그는 `v` + 판 번호.** 받는곳 주소가 이 태그로 만들어집니다.

## 안 하기로 한 것 — **코드 서명 인증서**

> (2026-09-08 무무) 「코드 서명 인증서를 사는 것은 **지금의 규모에서는
> 운영에 말도 안 되는 가격대**라 고려 항목에서 **완전히 제외.**」

연 60만~95만 원대입니다. 이것이 있으면 「알 수 없는 게시자」 경고도,
갱신할 때 백신이 잡을 위험도 한꺼번에 없어지지만 — **다시 꺼내지 않습니다.**
기관이 여러 곳이 되어 매출이 그것을 감당할 때 다시 봅니다.

> 헷갈리지 마세요 — **판정보 서명(Ed25519)은 그것과 다른 것이고, 공짜이며,
> 뺄 수 없습니다.** 인증서는 *윈도가 exe 를 믿게* 하는 것이고, 판정보
> 서명은 *우리 프로그램이 판정보를 믿게* 하는 것입니다.

## 남은 것 하나 — 파일럿 PC 에서 진짜로 한 번

컨테이너에서는 갈아끼우기 .bat 이 실제로 도는 것을 못 봅니다 (윈도가
아니라서). **파일럿 PC 에서 한 번은 눈으로 보셔야 합니다** — 특히
백신이 뭐라 하는지.

---

# 순서를 바꿨습니다 (2026-09-08)

> (무무) 「사실상 **빈 자료함 걸어 보기까지 끝나면 기능적인 부분은 완성**
> 된 거잖아? 최초 설정 마법사 생성작업 전에 **보안관련 문제와 운용테스트를
> 점검**한 후 최초설정 마법사 단계로 넘어갔으면 해.」

```
   ① 업데이트 안내            ← 됐습니다 (2026-09-08)
   ② 빈 자료함 걸어 보기       ← 다음
   ③ 보안 점검 · 운용 테스트   ← ★ 여기가 새로 들어왔습니다
   ④ 최초 설정 마법사
   ⑤ 아이콘
   ⑥ 설치 안내문 · 사용설명서
```

**②가 ④보다 먼저인 까닭** — ④의 「끝난 것으로 치는 기준」이 「**빈
자료함으로 처음 켜서** 설명서를 한 줄도 안 읽고 끝까지 갔을 때」입니다.
빈 자료함은 만들 기능이 아니라 **마법사가 딛고 설 땅**입니다. 거꾸로
하면 마법사를 다 만든 뒤 빈 자료함에서 깨져서 **또 고칩니다.**

---

# 2026-09-08 저녁에 고친 것

## ① 설정 화면에서 **안 보여도 되는 것**을 걷어냈습니다

> (무무) 「바꾼말의 목록을 보면 수행기관의 관리자가 **굳이 알지 못해도 되는
> 기능들**이 있거든? … **이 프로그램 사용자가 알면 어떤 도움이 되길래
> 넣은거야???**」

**답: 도움 안 됩니다. 제가 잘못 넣었습니다.** 뺀 것과 까닭 —

| 뺀 것 | 왜 뺐나 |
|---|---|
| 「새 버전을 확인하는 주소」 칸 | 관리자가 **바꿀 일이 없습니다.** 만든 곳이 자리를 옮겼을 때뿐인데, 그때는 어차피 전화로 알려야 합니다. 칸을 두면 **잘못 고쳐 갱신만 끊깁니다** |
| 「이 주소로 바꾸기」 단추 | 위와 함께 |
| 「받아 둔 새 버전 지우기」 | 받아 둔 것이 깨졌으면 **점검이 이미 거절**합니다. 관리자가 손댈 일이 없습니다 |
| 「확인용 열쇠가 안 들어 있습니다」 | **만든 쪽 실수**라 기관이 고칠 수 없습니다. 화면에 뜨면 프로그램이 고장 난 것처럼 보이기만 합니다 → **「통돌Note 만들기.bat」이 내보내기 전에** 크게 알립니다. 그게 고칠 수 있는 유일한 자리입니다 |

**남긴 것과 까닭** —

- 켜고 끄기 — 인터넷에 닿는 기능이니 끌 수 있어야 합니다
- **「무엇이 나가나」 두 문단** — 통합돌봄 자료가 든 PC 입니다. 바깥으로
  무언가 오간다면 기관은 그게 무엇인지 알 권리가 있습니다. 안내가 아니라
  **묻기 전에 답하는 것**입니다
- 「새 버전이 있는지 지금 확인」 — 하루 한 번만 보므로, 「방금 올렸어요」
  전화가 왔을 때 **다시 켜도 소용이 없습니다.** 당길 자리가 필요합니다
- 「지금까지 바뀐 것」 — 몇 주 뒤 청구액이 달라 보일 때 볼 자리

**앞으로의 잣대** — 화면에 무엇을 넣기 전에 **「관리자가 이걸 알면 무엇을
할 수 있나」**를 먼저 묻습니다. 답이 「아무것도」면 안 넣습니다.

## ② 「판」을 모두 「버전」으로

> (무무) 「판 대신 모두 버전으로 명칭 변경해 줘. **혼용사용하지 마 헷갈리니까.**」

화면·검은 창·문서·코드에서 **682군데**를 바꿨습니다.

**바꾸지 않은 것 (다른 낱말입니다)** — 판단 · 판정 · 간판 · 판가름 ·
서명판(서명 그리는 板) · 실적사진판 · 몫판 · 한판 ·
`server/index.ts` 의 `첫판`(= 처음 도는 것)

**통째 치환이 망가뜨렸다가 되돌린 것 넷** —
`강판례`(대상자 이름) · `조판`(組版) · `제어판`(윈도) · `.사진판`(CSS 이름).
**낱말 하나하나를 눈으로 보지 않았으면 그대로 나갈 뻔했습니다.**

**폰 앱(`현장앱/앱.html`)은 그대로 둡니다** — 거기 「판」은 대부분
화면 판(板)·캔버스이고, 제공인력에게 **보이는 글자에는 없습니다.**
다만 `확인판` → `확인버전` 은 바꾸고, **서버가 옛 이름도 받게** 했습니다
(폰에 옛 화면이 캐시로 남아 있을 수 있습니다).

## ③ 죽어 있던 시험 둘을 찾았습니다

- **`영수증화면시험.ts`** — 없는 계정(`admin`/`admin1234`)으로 들어가고
  있어 로그인 화면에 머문 채 터졌습니다. **4가지 → 12가지**가 됐습니다.
  여덟 가지는 여태 한 번도 안 돌았습니다
- **`서비스손입력시험.ts`** — 「기관 개설부터」를 밟는 시험인데 이미 개설된
  자료함에서는 못 돕니다. **다음 단계(빈 자료함 걸어 보기)에서 같이 고칩니다**

---

# ② 빈 자료함 걸어 보기 — **끝났습니다 (2026-09-08)**

**끝난 것으로 치는 기준** — 「아무것도 없는 상태에서 시작해 대상자 한 명을
넣었다」. **됐습니다.** 그리고 그 길에서 **탈이 하나도 안 났습니다.**

## 걸어 본 길

```
   빈 폴더에서 켜기 → 기관 개설 → (저절로 로그인) → 홈
   → 홈·의뢰접수·대상자·종사자·배정·제공실적·정산·통계·기록지·설정 열 화면
   → 대상자 직접 추가 → 대상자 상세
```

콘솔 오류 0 · 화면 터짐 0 · 400 이상 응답 0. 빈 화면마다 **왜 비어 있고
무엇을 하면 되는지**가 적혀 있었습니다 (「아직 의뢰가 없습니다. 「의뢰
접수」에서 지자체 명단을 올리면 여기 나옵니다」 같은 식).

**설정 기본값도 편람 그대로 다 채워져 있습니다** — 단가 다섯(가사 24,000 ·
식사 11,000 · 동행 27,000 · 이미용 24,000 · 안전생활환경 실비) · 연간
150만원 · 생애 100만원 · 본인부담 세 구간 · 하루 180분 · 연속 비대면 3회 ·
확인사항 열한 가지. **마법사가 「새로 적는 화면」이 아니라 「확인하는
화면」이면 된다는 전제가 실물로 확인됐습니다.**

> 다만 **규칙 이름이 「우리 지자체 규칙」** 그대로입니다. 마법사의 ① 번
> (「우리 지자체 이름」)이 필요하다는 것도 여기서 눈으로 봤습니다.

## 여기서 찾은 것 — **죽어 있던 시험**

`서비스손입력시험.ts` 는 맨 위에 「**빈 자료함에서 시작합니다**」라고
적어 두고도, 실제로는 **개발용 5757 에 붙고 있었습니다.** 그 자료함에는
이미 기관이 개설되어 있어 「기관 개설 화면」을 30초 기다리다 터졌습니다.
**아무것도 안 보고 있었습니다.**

고쳤습니다 → **38가지 전부 통과.**

### 그래서 만든 것 — `도구/빈자료함.ts`

시험하기가 번거로워서 죽어 있던 것이므로, **한 줄로 되게** 만들어 둡니다.

```ts
const 집 = await 빈자료함띄우기();   // 빈 폴더에 프로그램을 하나 띄웁니다
... 집.주소 로 브라우저를 붙입니다 ...
await 집.끄기();                     // 폴더째 지웁니다
```

포트는 검은 창에 찍힌 주소를 **읽어서** 씁니다 — 짐작하면 개발용과
부딪칠 때 엉뚱한 프로그램을 시험하게 됩니다. 그게 제일 나쁜 갈래입니다.

---

# 관리자는 한 명뿐 (2026-09-08)

> (무무) 「관리자는 현재는 1명으로 제한 해야해. 추후에 관리자를 추가하는
> 경우엔 **PC 백업 문제 등 얽히는 문제들**이 다수 발생할 여지가 있어서
> 제한 하는게 맞을 것 같아.」

## 넣은 것

- **천장** — 관리자가 있으면 두 번째 관리자를 못 만들고, 있던 사람을
  관리자로 못 올립니다 (`staff.천장확인`)
- **바닥** — 마지막 관리자는 역할을 못 빼고, 계약종료·휴직도 못 합니다
  (전부터 있던 것)
- **넘기기** — `관리자넘기기(주는이, 받는이)`. **한 덩어리로** 씁니다.
  「내려놓기」와 「지정하기」를 따로 두면 그 사이에 관리자가 0명이거나
  2명인 순간이 생기고, 그때 정전이 나면 **기관이 잠깁니다**
- 넘긴 사람은 **사무직원**으로 남습니다 — 역할을 비우면 방금까지
  관리자였던 사람이 갑자기 화면에 못 들어갑니다
- 화면 — 「관리자」 단추가 **눌리지 않고**, 그 자리에 까닭과
  「관리자 자리를 이 분에게 넘기기」가 뜹니다

**천장만 세우면 안 됩니다.** 넘기는 길이 없으면 관리자가 그만두는 날
기관이 통째로 잠깁니다 — 천장만 있는 편이 더 위험합니다.

## ★ 이 일을 하다 찾은 **진짜 구멍** ★

**관리자를 세는 법이 두 가지였습니다.**

| 세는 법 | 어디에 |
|---|---|
| `parseRoles(...)` 로 세기 | 화면·권한 판정 |
| SQL `roles LIKE '%admin%'` | 「관리자가 한 명은 있어야」 검사 |

자료함에 `roles` 가 `'"admin"'`(따옴표까지 저장된) 줄이 하나 있었는데,
**앞쪽은 사무직원으로 읽고 뒤쪽은 관리자로 셌습니다.** 그래서
「관리자가 한 명은 있어야 합니다」가 **2명으로 알고 통과**했고,
**마지막 관리자가 계약종료되었습니다** (시험을 돌리다 실제로 그렇게
됐습니다).

고친 것 —

1. `parseRoles` 가 `'"admin"'` 도 알아봅니다. 못 알아보면 **권한이 조용히
   낮아지는데**, 그게 이 파일 주석이 원래 막으려던 바로 그 일입니다
2. **세는 법을 하나로** 합쳤습니다 (`staff.관리자들`). SQL 로 세던 자리
   네 곳을 다 걷어냈습니다
3. `도구/시험관리자.ts` 가 역할을 글자로 받아도 버팁니다 —
   `시험관리자("이름", "admin")` 처럼 쓴 곳이 그 이상한 줄을 만들었습니다
4. 시험이 **그 두 법이 같은 답을 내는지** 봅니다. 나중에 누가 SQL 로 세는
   길을 다시 만들면 거기서 웁니다

## 시험

`관리자한명시험.ts` — **33가지**. 일부러 망가뜨려 본 여섯 가지
(천장 두 곳 · 바닥 두 곳 · `parseRoles` · 넘길 때 앞사람 안 빼기)를
**모두 잡습니다.**

시험이 **자료함을 원래대로 돌려놓는 것**까지 시험합니다 — 처음 짤 때
진짜 관리자를 계약종료시켜 놓고 끝난 적이 있습니다.

---

# 보안 점검 ① — 바깥에서 뚫고 들어오는 길 (2026-09-08)

> (무무) 「**외부에서 뚫고 들어올 가능성**에 대한 보안점검까지 이루어져야 해.」

## 「127.0.0.1 에만 열어 뒀으니 안전하다」는 **틀렸습니다**

이 프로그램은 자기 컴퓨터에만 귀를 엽니다. 그런데 **길이 하나 더
있습니다 — 관리자의 브라우저입니다.**

관리자가 점심때 뉴스를 보다 나쁜 광고가 붙은 쪽을 엽니다. 그 쪽의
자바스크립트는 **그 컴퓨터 안에서** 돕니다. 그러니 127.0.0.1 도 두드릴
수 있습니다. 나쁜 쪽이 자기 이름을 잠깐 127.0.0.1 로 바꿔 두면
(**DNS 되박기**) 브라우저는 「같은 쪽」이라 믿고, 그 쪽 자바스크립트가
**우리 답을 그대로 읽습니다.** 브라우저의 다른 막이(같은 출처 규칙 ·
SameSite 쿠키)는 이 수법에 **안 걸립니다.**

## 찾은 것 넷

| | 무엇이 | 얼마나 나쁜가 |
|---|---|---|
| ① | **`Host` 머리글을 아무거나 써도 받았습니다** | 되박기가 그대로 됩니다 |
| ② | **`/api/bootstrap` 이 로그인 없이 기관 표를 통째로** — 기관명·대표자·전화·**사업자등록번호·계좌번호** | ①과 겹치면 남의 서버로 샙니다 |
| ③ | **`/api/health` 가 자료함 전체 경로를** — `C:\Users\Bak Byeongseob\...` 처럼 **윈도 사용자 이름**이 그대로 | 사람 이름이 샙니다 |
| ④ | **로그인 비밀번호를 몇 번을 틀리든 그대로 받았습니다** | 사무실 열기가 켜진 동안 옆 컴퓨터에서 두드릴 수 있습니다 |

**제일 무서운 갈래** — 빈 자료함으로 **처음 켠 PC** 는 `/api/setup` 이
열려 있습니다. 되박기로 그것을 먼저 부르면 **남이 자기 아이디로
관리자를 만들어** 버립니다. 그 뒤로는 그 사람이 그 기관의 관리자입니다.

## 막은 것 — `server/문지기.ts`

1. **이름 검사** — `Host` 가 `127.0.0.1` · `localhost` (사무실 열기가
   켜져 있으면 그 랜 주소)가 아니면 **403.** 되박기가 여기서 끝납니다
2. **어디서 눌렀나** — 자료를 바꾸는 요청(POST·PATCH·DELETE)에 `Origin`
   이 붙어 있고 우리 쪽이 아니면 **403.** 읽기만 하는 GET 은 안 봅니다
   (막으면 화면이 안 돕니다)
3. **로그인 전에는 기관 **이름 한 줄**만** — 나머지는 `/api/settings`
   에서만 나옵니다(관리자만)
4. **`/api/health` 는 경로 대신 지문** — 같은 자료함인지 견주는 데는
   지문으로 충분하고, 되돌려 읽을 수 없습니다. 경로는 **들어온 사람에게만**
5. **자꾸 틀리면 늦춥니다** — 다섯 번까지는 그냥, 그 뒤로 30초 → 1분 →
   5분. **잠그지는 않습니다** — 영영 못 들어가게 하면 남이 일부러
   틀려서 기관을 잠글 수 있습니다. 아이디마다 따로 셉니다
6. 사무실 열기의 **여섯 자리 번호도 같이** — 백만 가지라 늦추기가
   없으면 랜 안에서 몇 시간이면 다 눌러 볼 수 있었습니다

### 이미 되어 있던 것 (확인만)

- 쿠키에 `HttpOnly` · `SameSite=Lax` — 화면 자바스크립트가 못 읽고,
  남의 쪽에서 온 요청에는 안 실립니다
- CORS 머리글을 아예 안 붙입니다 — 붙으면 남이 답을 읽습니다
- 비밀번호는 `Bun.password`(argon2)로 저장
- 파일 길로 빠져나가기(`/../../etc/passwd`)는 막혀 있었습니다
- 아이디가 있는지 없는지 답으로 안 새어 나갑니다

## 시험 — `바깥문시험.ts` **51가지**

★ **진짜 소켓으로 날 HTTP 를 씁니다.** `fetch` 로는 `Host` 를 마음대로
못 바꿉니다 — 공격하는 쪽과 같은 방식으로 두드려야 진짜로 본 것입니다.

일부러 막이를 부순 다섯 가지(이름 검사 · Origin 검사 · 늦추기 ·
기관정보 · 경로)를 **모두 잡습니다.**

## ★ 고치다 제가 낸 버그를 시험이 잡았습니다 ★

`자료함지문` 을 쓰는 자리(`이미돌고있나`)보다 **아래**에 두었습니다.
`const` 는 선언 전에 못 읽는다는 규칙에 걸려 그 자리에서 터졌는데,
그 함수의 `catch` 가 그것을 **삼켰습니다.** 그래서 늘 「아무도 없다」로
답했고, **두 번째로 켠 프로그램이 물러나지 않고 둘이 같이 돌았습니다.**
화면은 멀쩡해 보이는데 어느 쪽에 적었는지 모르게 되는 갈래입니다.

`지킴이시험.ts` 가 잡았습니다. **21가지 전부 통과**로 돌아왔습니다.

> 그리고 그 시험은 **5757 을 자기가 써야** 합니다. 개발 서버를 켜 둔 채
> 돌리면 엉뚱한 프로그램을 보고 열한 가지가 헛되이 실패합니다.

## 우편함(Supabase) — 진짜로 인터넷에 있는 쪽

맨 Postgres 에 올려 다시 돌렸습니다.

| 시험 | 결과 |
|---|---|
| 기관 격리 (`002`) | **37가지 통과** — 남의 기관 것이 한 줄도 안 보입니다 |
| 사진 (`007`) | **10가지 통과** — 남의 기관 자리에 못 올리고, 못 읽습니다 |
| 계정 로그인 (`009`) | **20가지 통과** — 남이 낸 요청도, 남의 폰 이름으로도 못 합니다 |

> 시험마다 **새 자료함**에 올려야 합니다. 한 곳에 몰아 넣으면 앞 시험이
> 넣어 둔 줄 때문에 뒤 시험이 헛되이 실패합니다 (오늘 그렇게 헤맸습니다).
> `002` 는 `005` 를 먼저 넣어야 합니다 — `register_device` 의 모양이
> `005` 에서 바뀌었습니다.

## 아직 못 본 것

- **사무실 열기(랜) 실제 노출** — 이 컨테이너의 주소가 `192.0.2.2`
  (문서용 대역)라 「사무실 네트워크」로 안 잡혀 `사무실열기시험.ts` 가
  못 돕니다. **파일럿 PC 에서 봐야 합니다**
- 운용 테스트(오래 켜 두기 · 자료 늘려 놓고 느려지는 곳 · 백업 복원 왕복)

---

# 보안 점검 ② — 운용 테스트 (2026-09-08)

## 자료를 **실제 규모로 불려 놓고** 재 봤습니다

`도구/무겁게하기.ts` — 기관 한 곳이 2년 쓴 만큼을 넣습니다.

| 잣대 | 대상자 | 실적 | 청구 | 자료함 |
|---|---|---|---|---|
| **넉넉히 (2년)** | 300명 | 14만 6천 줄 | 1만 4천 줄 | 41MB |
| **파일럿 (1년)** | 100명 | 2만 4천 줄 | 2천 4백 줄 | 6.9MB |

## 찾은 것 셋 — 다 고쳤습니다

### ① `assignment` 과 `billing` 에 **색인이 하나도 없었습니다**

홈은 대상자를 **한 명씩 돌면서** 그 두 표를 봅니다. 300명 × (배정
600줄 + 청구 1만 4천 줄)을 매번 통째로 훑고 있었습니다.

→ 자료함 v32 에 색인 열두 개를 넣었습니다. **정산 539ms → 208ms.**

색인은 자료를 **안 바꿉니다.** 찾는 길만 하나 더 놓는 것이라 이미
쓰고 있는 기관에 넣어도 잃을 것이 없습니다.

> 덤으로 **아이디에 겹침 금지**를 자료함 자체에 걸었습니다. 겹치면
> 누구로 들어가는지가 자료함 차례에 달리게 됩니다. 이미 겹쳐 있는
> 자료함에서는 켜지지 않으면 안 되므로 보통 색인으로 물러납니다.

### ② 주거(생애한도)를 세려고 **그 사람 평생 실적을 통째로** 읽었습니다

`연간쓴돈()` 이 `실적줄들(id, "1900-01-01", "9999-12-31")` 을 부르고
나서 **주거만 남기고 나머지를 버렸습니다.** 홈은 그것을 300명 전부에
대해 합니다. 주거를 한 번도 안 받은 분도 평생 실적을 다 읽었습니다.

→ 읽을 때 걸러 옵니다(`{ 연간바깥만: true }`). **홈 859ms → 493ms.**
셈하는 방법은 한 글자도 안 바뀝니다.

### ③ 생애한도 화면이 **실적 전부**를 훑었습니다

생애한도가 붙은 것은 안전생활환경 하나뿐인데, 그것을 가리려고
가사·식사·동행 실적 십수만 줄을 다 읽었습니다.

→ 서비스 번호로 먼저 좁힙니다. **생애한도현황 1.1초 → 0ms.**

## 지금 얼마나 걸리나

| 화면 | 파일럿(100명·1년) | 넉넉히(300명·2년) |
|---|---|---|
| 홈 | **139ms** | 535ms |
| 대상자 목록 | 23ms | 117ms |
| 제공실적 | 49ms | 177ms |
| 정산 | 59ms | 212ms |
| 통계 | 310ms | 1,091ms |
| 기록지 | 4ms | 30ms |

**파일럿 규모에서는 다 빠릅니다.** 300명·2년까지 가면 통계가 1초쯤
됩니다 — 한 달에 몇 번 여는 화면이라 지금은 둡니다.

### 안 건드린 것과 그 까닭

통계는 `recordMonth()` 를 **열두 번** 부릅니다. 따로 합계 쿼리를
쓰면 빨라지지만, **제공실적 화면과 통계가 서로 다른 셈을 하게**
됩니다. 그 갈래에서 예전에 청구액이 어긋난 적이 있습니다
(한도 화면 vs 정산 화면, 2026-09-05). 빠르게 만들려고 그 위험을
다시 만들지 않습니다. **정말 느려지면 그때 `recordMonth` 자체를
빠르게 합니다** — 그러면 두 화면이 같이 빨라집니다.

> 제공실적은 한 달치가 **5.4MB**입니다(300명 기준). 서버는 177ms 인데
> 브라우저가 그것을 그리는 데는 더 걸립니다. 파일럿(100명)은 1.8MB 라
> 괜찮습니다. 기관이 커지면 **읍면동·담당으로 나눠 받는 것**을
> 봐야 합니다.

---

## 자료를 잃지 않는가 — `운용시험2.ts` **18가지**

### ★ 찾은 것 — 자료함이 잠기면 **기다리지 않고 포기했습니다**

SQLite 는 한 번에 한 쪽만 쓰게 합니다. 그 자리가 잡혀 있을 때 기본값은
**기다리지 않고 그 자리에서 「잠겼습니다」**를 내는 것입니다. 그러면
화면에는 「처리 중 문제가 생겼습니다」만 뜨고 **방금 적은 것은
사라집니다.**

언제 생기나 —
- **사무실 열기**로 옆 컴퓨터가 붙어 있을 때 두 사람이 같은 순간에 저장
- 백업을 뜨는 동안 누가 저장
- 제공인력 폰이 보낸 보고가 들어오는 순간 사무실이 저장

**고친 것 둘** —

1. `PRAGMA busy_timeout = 5000` — **5초를 기다립니다.** 사람이 하는
   저장은 몇 밀리초라 그 안에 거의 다 풀립니다
2. 5초를 넘게 잡혀 있으면 **「다른 곳에서 저장하는 중입니다. 잠시 뒤에
   다시 눌러 주세요. 방금 적으신 것은 그대로 있습니다」**(409).
   「처리 중 문제가 생겼습니다」(500)로 두면 다시 누르면 되는 일에
   기관이 전화를 겁니다

### 확인한 것

- **백업을 열어서 줄 수를 셌습니다** — 대상자 100 · 실적 24,278 ·
  청구 2,400 이 그대로. 「파일이 생겼다」만 보면 **속이 빈 파일**도
  통과합니다. 백업은 열어 보기 전까지 백업이 아닙니다
- 한꺼번에 스무 번 저장해도 **스무 개가 다 들어갑니다**
- 나가면 로그인 줄이 지워집니다
- 백업이 6.9MB 를 순식간에 뜹니다. 보관이 무한정 안 늘어납니다

일부러 망가뜨린 둘(기다리기 · 잠김 안내)을 **모두 잡습니다.**

---

# 아이콘 — 됐습니다 (2026-09-08)

## 무엇으로 정했나

**집 하나에서 하트를 파낸 것.** 하트는 오른쪽 아래 구석에 12도 눕혀
놓았습니다. 색은 프로그램과 같은 초록(#3f6b4a), 바탕은 밝은 카드.

## 왜 이 모양인가 — 여섯 번 갈아엎고 배운 것

처음 다섯 번은 **집을 그리고 손을 더했습니다.** 다 실패했습니다.
까닭은 하나입니다 — **도형이 둘 이상이면 32px 아래에서 뭉갭니다.**

| 해 본 것 | 32px 에서 |
|---|---|
| 집 + 양옆에 손 | 손 흔드는 것처럼 |
| 집 + 손이 지붕 | 손을 번쩍 든 것처럼 |
| 집 + 아래에서 받치는 손 | 웃는 얼굴처럼 |
| 집을 감싸는 두 팔 | 고리처럼 |
| 글자 「돌」 | 뭉갬 |

무무 님이 보내 주신 참고 넉 장에서 **답이 나왔습니다.** 그 중 첫 장
(집+하트)이 하트를 집 **옆에 붙인 게 아니라 집에서 파냈습니다.**
그래서 도형이 **하나**입니다. 16px 까지 삽니다.

> **다음에 아이콘을 만들 일이 있으면 이것만 기억하면 됩니다 —
> 더하지 말고 파낼 것.**

하트를 가운데 두지 않고 오른쪽 아래로 내린 것은 무무 님 지시입니다
(「센터는 너무 멋이 없어」). 내리면서 **오른쪽 벽과 바닥에서 똑같이
떨어지도록** 여백을 맞췄고, 12도 눕혔습니다. 똑바로 두면 아무리
구석으로 밀어도 자로 잰 것처럼 보입니다.

## 들어간 자리 넷

| 자리 | 무엇 |
|---|---|
| `도구\아이콘.ico` | **7크기**(16·24·32·48·64·128·256)가 한 파일에. `통돌Note 만들기.bat` 이 이미 이 자리를 보고 있어서 **다음 만들기부터 저절로 붙습니다** |
| `web\index.html` | 브라우저 탭 아이콘 |
| `현장앱\앱.html` | 휴대폰 홈 화면 아이콘 (원본) |
| `현장앱\올릴것\index.html` | 이미 지어 둔 것에도 같이 |

**그림 파일을 따로 두지 않고 화면 파일 안에 심었습니다.** 파일로
두면 옮기다 잃어버리고, 잃어버리면 아이콘만 조용히 사라집니다.

`도구\아이콘-1024.png` 는 원본입니다. 나중에 명함·소개자료에 쓸 것이
있으면 여기서 줄여 쓰면 됩니다.

## 확인한 것

- `.ico` 안의 일곱 크기를 **하나씩 꺼내 봤습니다.** 16px 에서도
  집과 하트가 다 남습니다
- 현장앱을 브라우저로 띄워 **탈이 안 났는지** 봤습니다
  (지어 둔 `올릴것\index.html` 은 깨끗합니다. `앱.html` 은 아직
  코드가 안 심긴 원본이라 원래부터 탈이 납니다 — 제 손댐 전후가 같습니다)

---

# 군 답 반영 — 막기 · 연속/누적 · 가는 주기 (2026-09-08, v1.18.34)

## 무엇을 정했나

무무 님이 군 질의 **열 가지에 다 답**을 주셨습니다(`확인목록.md` 나 절).
**여덟은 지금 그대로가 맞았고**, 고칠 것은 둘이었습니다.

| | 답 | 한 일 |
|---|---|---|
| 3 | 한도 초과 — **막되 알린다.** 막는 자리는 **계획(배정)** | ★ 아래 |
| 6 | 비대면 3회 — **연속이 맞다.** 다만 연속/누적을 고르게 | ★ 아래 |

> (무무 — 「가. **계획이 안되면 실적도 따라서 할 수가 없어.**」)
> 실적에서 안 막는 까닭이 여기 있습니다. 현장에서 **이미 다녀온 것**을
> 못 적게 하면 기록이 사라지고, 사라진 기록은 되살릴 수 없습니다.

## ① 한도를 넘는 계획은 **저장되지 않습니다**

`saveAssign()` 안에서 막습니다 — **화면이 아니라 저장하는 곳**입니다.
화면 경고는 우회할 수 있고 주소를 바로 두드리면 화면을 안 지납니다.

막을 때 **네 가지를 함께** 말합니다 — 어느 한도가 · 얼마나 넘는지 ·
넘긴 몫은 누가 떠안는지 · 어떻게 고치면 되는지.

### ★ 「줄이는 저장」은 언제나 됩니다 (제 판단입니다)

곧이곧대로 막으면 덫이 생깁니다. **이미 넘겨 저장된 배정**(옛 자료함,
군과 협의해 넘긴 것)이 있으면 그 배정의 **담당자만 바꾸려 해도** 막혀
고칠 길이 없어집니다. 그래서 **지금 저장된 것보다 커지는 것만** 막습니다.

### ★★ 막고 나서야 드러난 것 — 동행·이미용은 배정이 아예 안 됐습니다

| 서비스 | 월 한도 | 주 단위로 짤 수 있는 **가장 적은** 계획 |
|---|---|---|
| 가사지원 | 월 12시간 | 주 1회 = 월 4~5시간 ✔ |
| 식사지원 | 월 8회 | 주 1회 = 월 4~5회 ✔ |
| **동행지원** | **월 2회** | 주 1회 = 월 4~5회 ✘ |
| **이미용** | **월 1회** | 주 1회 = 월 4~5회 ✘ |

배정은 **주간 계획**으로 짭니다. 한 주에 한 번이 제일 적은데 그것만도
한 달 4~5번입니다. **격주·「월 2회」를 적을 칸이 없었습니다.**
경고만 띄우던 동안에는 안 보였고, 막는 순간 드러났습니다.

## ② 배정에 **「가는 주기」** — v34

> (무무 — 「**가!로 하자**」 = 계획에 주기를 만들 것)

**「격주」가 아니라 「한 달에 몇 번」입니다** (제 판단). 편람이 정한 것은
**「월 2회」**지 격주가 아닙니다. 격주는 **다섯 주짜리 달에 3번**이 되어
그 달만 한도를 넘깁니다 — 막는 것을 만들어 놓고 다시 넘기는 계획을
짜게 됩니다.

    「한 달에 2번 · 수요일」 = 그 달 수요일 중 **앞의 두 번**

달이 몇 주짜리든 정확히 2번입니다.

### ★★★ 답하는 곳은 `server/가는날.ts` **하나**입니다

계획을 **날짜로 펴는 자리가 여섯 군데**입니다 — 실적 달력 · 계획 재기 ·
청구 · **현장앱 「오늘 갈 곳」** · 담당 일감 · 「계획된 날인가」 판정.

주기를 여섯 군데에 따로 넣으면 언젠가 한 곳만 고쳐지고, 그러면
**사무실 화면과 제공인력 폰이 서로 다른 날을 말합니다.** 어르신 댁
앞에서 「오늘 아닌데요」가 되는 고장입니다. **묻는 곳이 여섯이어도
답하는 곳은 하나**여야 합니다.

> 일부러 **안 넣은 두 곳**도 적어 둡니다 —
> `billing.ts` 의 「그 요일 계획 시간」은 **이미 다녀온 실적**의 분을
> 찾는 자리라 주기와 상관이 없습니다(주기를 넣으면 계획 밖 날에 다녀온
> 실적의 청구가 틀어집니다). `assign.ts` 의 담당 요일 일감은 **어림
> 안내**라 그대로 둡니다.

## ③ 비대면 배달 — 연속으로 세나 누적으로 세나 (v33)

`region_profile.f2f_mode` (`연속` 기본 / `누적`). **세는 것은 둘 다
그대로 셉니다.** 이 값이 정하는 것은 **경고하고 끊는 기준** 하나뿐이고,
대상자 화면에는 어느 쪽을 골라도 **둘 다 보이며** 지금 기준인 쪽에
「← 이 기관 기준」이 붙습니다. 어르신 서비스가 끝나는 기준이라
한쪽을 숨기면 안 됩니다.

## 화면에서 바뀐 것

**두 번 묻고 지나가던 것을 없앴습니다.** 이제 서버가 막으므로,
물어 놓고 「예」를 눌렀는데 거절당하면 **묻지 말았어야 할 것을 물은 것**
입니다. 확인창(confirm) 둘 → 알림창(alert) 하나로 바꿨습니다.
**화면이 못 박지는 않습니다** — 줄이는 저장을 통과시킬지는 서버만 알기
때문에, 화면이 앞서 막으면 이미 넘겨 저장된 배정을 영영 못 고칩니다.

## 시험

| 시험 | |
|---|---|
| `가는날시험.ts` **21가지** (새로) | 매주는 예전 그대로 · 다섯 주짜리 달에도 정확히 N번 · 두 길이 한 날도 안 어긋남(364날) |
| `계획막기시험.ts` **39가지** (새로) | 막기 · 알리기 · 줄이는 저장 · **현장앱과 사무실이 같은 날을 보나(23날)** |
| `계획재기시험.ts` 95가지 | 「두 번 묻고 지나간다」 → 「막는다」로 고쳐 씀 |
| `대면배달시험.ts` 57가지 | 연속/누적 여덟 가지 더함 |
| `대면배달화면시험.ts` 28가지 | 설정의 「세는 법」·「← 이 기관 기준」 |

**일부러 망가뜨려 본 13가지를 다 잡습니다** — 주기 무시 · N+1 · 기간 밖
셈 · 달 건너뜀 · 안 막음 · 줄이는 저장까지 막음 · 길잡이 없앰 ·
주기 저장 안 함(새로/고칠 때) · 재기가 주기 무시 ·
**현장앱만 옛 셈** · 설정 무시하고 늘 연속.

---

# ④ 최초 설정 마법사 — 됐습니다 (2026-09-08, v1.19.35)

## 만든 것

```
   기관 정보  →  관리자 등록  →  ★ 필수 설정  →  홈
```

`organization.setup_done_at` (v35) 이 비어 있으면 **어느 주소로 들어와도**
마법사로 보냅니다. **관리자에게만** 뜹니다 — 사무직원이 켰을 때 뜨면
고칠 권한도 없는 화면 앞에서 갇힙니다.

**새로 적는 화면이 아니라 「확인하는 화면」입니다.** 편람 값이 이미 다
채워져 있고(시험이 **숫자 칸에 빈칸이 0개**인지 셉니다), 맞으면 단추
하나로 끝납니다.

| | 묻는 것 | 왜 묻는지 화면에 적은 말 |
|---|---|---|
| ① | 우리 지자체 이름 | 그대로 두면 **남의 프로그램처럼** 보입니다 |
| ② | 서비스 단가 | **이 값이 틀리면 청구 금액이 그대로 틀립니다** |
| ③ | 지원한도 | 넘기면 **군이 안 줍니다 — 기관이 떠안습니다** |
| ④ | 서비스별 월 한도 | 시간과 횟수가 **둘 다** 걸리는 서비스가 있습니다 |
| ⑤ | 비대면 배달 한도 + 세는 법 | 돈이 아니라 **어르신 서비스가 끊기는** 기준 |
| ⑥ | 본인부담 구간 | **지자체마다 다릅니다** |
| ⑦ | 공휴일 | **여기만 「나중에」** — 대신 홈이 말합니다 |

**건너뛰기 단추를 두지 않았습니다.** 화면에 그 까닭까지 적었습니다 —
값이 다 채워져 있어 누를 것이 확인뿐이고, 빠져나갈 구멍을 두면 반드시
그리로 갑니다.

## ★★ 이게 왜 필요한지 — 만들다가 증거를 찾았습니다

**이 개발 자료함에는 8월 31일에 편람 값으로 고친 단가가 하나도 안 들어가
있었습니다.**

| | 자료함에 있던 값 | 편람 |
|---|---|---|
| 식사지원 | 10,100원 | **11,000원** |
| 동행지원 | 17,000원 | **27,000원** |
| 안전생활환경 생애한도 | 200만원 | **100만원** |
| 방문목욕 | **있음** | 해남 편람에 **없음** |

`seed.ts` 는 맞게 고쳐져 있습니다. 그런데 **그건 자료함을 처음 만들 때만
씁니다.** 이미 만들어진 자료함은 프로그램을 갱신해도 안 바뀝니다.

> **파일럿 PC 의 자료함도 8월 31일 전에 만들어졌으면 같은 처지입니다.**
> 마법사가 그 값을 눈앞에 보여 주고 확인받게 하는 것이 정확히 이것을
> 막습니다. 이미 쓰고 있는 기관에도 **한 번은 뜨는 것**이 그래서 맞습니다.

빈 자료함으로 새로 만들면 편람 값이 제대로 들어갑니다 — `첫설정시험` 이
식사 11,000 · 동행 27,000 · 생애 100만 · 방문목욕 없음을 하나씩 확인합니다.

## 홈이 스스로 말합니다 (`할일.md` 8번)

공휴일은 **달력을 손에 들고 있어야** 넣을 수 있어 마법사에서 못 묻습니다.
그래서 넣으실 때까지 **홈이 말합니다.** 함께 보는 것 셋 —

- 올해 공휴일이 하나도 없음
- **단가가 0원인 서비스**가 있음 (그대로 두면 청구서에 0원으로 나갑니다)
- 잠긴 **기준선**을 쓰고 있음 (한도가 다 꺼진 규칙입니다)

셋 다 **고칠 곳으로 가는 길**을 함께 답니다. 「무엇이 잘못됐다」만 말하고
어디서 고치는지 안 알려 주면 관리자는 설정 화면을 헤매다 그냥 닫습니다.

## 못박은 것

- **한 번에 쓰거나 아예 안 씁니다** (`db.transaction`). 가운데서 멈추면
  **단가는 새 값인데 한도는 옛 값**인 자료함이 남고, 그건 아무도 못 알아챕니다
- **마법사만 아는 값을 따로 두지 않습니다.** 설정 화면이 쓰는 그 표에
  그대로 씁니다 — 두 곳이 생기면 언젠가 어긋납니다
- 바꾼 것은 **이력(`rule_change_log`)에 남깁니다.** 나중에 「단가가 언제
  바뀌었나」를 물을 사람이 있습니다
- 잠긴 「복지부 지침 표준」은 **못 고칩니다**

## 시험

| | |
|---|---|
| `첫설정시험.ts` **43가지** | **빈 자료함**에서 처음부터. 값이 미리 채워지나 · 마쳐야 홈이 열리나 · **자료함에 정말 들어갔나** · 이상한 값이면 앞 줄도 안 바뀌나 |
| `첫설정화면시험.ts` **27가지** | **눌러서 끝까지.** 빈칸 0개 · 건너뛰기 없음 · 까닭이 적혀 있나 · 단추 하나로 홈이 열리나 · 다시 안 뜨나 · 설정에도 그대로인가 |

**끝난 것으로 치는 기준을 그대로 시험합니다** — 「빈 자료함으로 처음 켜서
설명서를 한 줄도 안 읽고 끝까지」. 시험이 글을 안 읽고 누르기만 합니다.

**일부러 망가뜨린 9가지를 다 잡습니다** — 마법사로 안 보냄 · 마친 뒤에도
계속 뜸 · 마쳤다고 표시 안 함 · 잠긴 기준선도 고쳐 줌 · 음수 단가를 받아 줌 ·
한 번에 안 쓰고 가다 멈춤 · 단가를 저장 안 함 · 홈이 아무 말도 안 함 ·
화면이 마법사를 안 띄움.

## 곁가지 — 시험 열 벌이 마법사에 막혔습니다

마법사가 **어느 주소로 들어와도** 막다 보니, 설정·인쇄 같은 화면 시험이
자기가 보려던 화면을 못 보게 됐습니다. `도구/설정마침.ts`(자료함 지름길)와
`도구/마법사지나기.ts`(브라우저로 눌러 지나기) 둘로 갈라 한 곳에 뒀습니다.

> **마법사 자체는 이 지름길을 안 씁니다.** `첫설정시험`·`첫설정화면시험`
> 둘은 빈 자료함에서 **정말로 걸어서** 지납니다.

---

# ⑥ 설치 파일 · 안내문 · 사용설명서 (2026-09-08, v1.19.35)

## ① 설치 파일 — `통돌Note 설치.bat` + `install.ps1`

> (무무) 「사용자 처음 사용 시 설치파일.」

### 만들다가 찾은 것 ★ 자료가 통째로 사라지는 길

윈도는 **zip 안의 exe 를 두 번 누르면 임시 폴더에 풀어서** 실행해
줍니다. 프로그램은 잘 돕니다. 그런데 자료함(`data\`)이 그 **임시
폴더**에 생기고, 윈도가 임시 폴더를 치울 때 **한 달치가 통째로**
사라집니다.

「압축을 푸세요」라고 안내문에 적혀 있었지만 **안 읽습니다.** 그리고
안 읽었다는 사실을 아무도 모르다가 한 달 뒤에 압니다.
**그래서 글이 아니라 프로그램이 막습니다.**

### 하는 일 다섯

| | |
|---|---|
| ① | **임시 폴더에서 돌면 멈춥니다** — 압축부터 풀라고 그림으로 |
| ② | `C:\통돌Note` 에 넣습니다 (Program Files 는 **거절**) |
| ③ | ★ **자료함은 손도 안 댑니다** — 다시 깔아도 자료는 그대로 |
| ④ | 바탕화면·시작 메뉴 **바로가기** (아이콘이 붙습니다) |
| ⑤ | ★ **「인터넷에서 받음」 꼬리표를 뗍니다** |

⑤ 를 조금 더 — 「Windows의 PC 보호」 파란 창은 파일이 바이러스라서
뜨는 것이 아니라 **꼬리표**(Zone.Identifier) 때문에 뜹니다. 기관이
이미 설치를 누른 뒤이니 우리가 방금 넣은 우리 파일의 꼬리표를 떼는
것은 맞는 일이고, **서명 인증서 없이 할 수 있는 유일한 길**입니다.
그래서 무서운 창이 **「설치할 때 한 번」으로 줄고, 프로그램을 켤 때는
안 뜹니다.**

### ★ 윈도가 없는데 어떻게 시험했나

**PowerShell 을 컨테이너에 받아서 진짜로 돌렸습니다.**
(`/opt/pwsh/pwsh` · 리눅스판이지만 같은 PowerShell)

「파일럿 PC 에서 봐 주세요」로 미루면 **아무도 안 봅니다.** 윈도만
되는 것(바로가기·꼬리표)은 못 보지만, **자료를 지키는 규칙은 여기서
다 볼 수 있습니다.**

돌려 보다 **탈 둘**을 잡았습니다 —
① 시작 메뉴 자리가 없으면 복사를 다 해 놓고 **마지막에 죽어서**
   「깔린 건지 아닌지 알 수 없는」 상태가 됐습니다
② 검은 창에 별표(`**`)가 그대로 찍히고 있었습니다

`설치시험.ts` **39가지.** 일부러 망가뜨린 **6가지를 다 잡습니다** —
압축 확인 빼기 · **자료함 덮어쓰기** · 설치 파일 짝 안 맞추기 ·
Program Files 받아 주기 · 반쪽만 깔기 · zip 에 설치 파일 안 넣기.

## ② 안내문과 설명서 — **HTML 두 장**

> (무무) 「설치안내문을 txt가 아니라 html로 보기 좋게. **상품으로
> 판매할껀데** 너무 보기 좋지 않아.」 / 「사용설명서의 경우 **매우
> 세세하게**.」

| 파일 | 무엇 |
|---|---|
| `설치 안내.html` | 받아서 켜기까지 · 6단계 · 10분 |
| `사용설명서.html` | 첫 달을 혼자 마무리하기까지 · **13장 + 부록** |

### 못박은 것 넷

1. **웹 글꼴을 안 씁니다.** 이 프로그램은 인터넷이 없어도 되는 것이
   전제인데, 웹 글꼴을 쓰면 **인터넷 없는 PC 에서 글자가 무너집니다.**
   윈도에 늘 있는 맑은 고딕으로 짜고 크기·굵기·자간으로 모양을 냅니다
2. **파일 하나에 다 들어 있습니다.** 모양(CSS)도 그림도 심었습니다 —
   옆 파일로 두면 하나만 옮겨졌을 때 **글이 무너진 채로** 열립니다
3. **인쇄용 모양을 따로** 넣었습니다. A4 로 뽑아 PC 옆에 둡니다
4. 색은 **프로그램에서 그대로** 가져왔습니다. 문서만 다른 초록이면
   같은 제품으로 안 보입니다

### 같은 것을 두 곳에 안 적습니다

단가는 **설명서에만**, PDF 뽑는 법도 **설명서에만** 적습니다.
설치 안내는 「받아서 켜기까지」라 짧아야 하고, 같은 숫자를 두 곳에
적으면 **한쪽만 낡습니다.** 대신 설치 안내가 설명서를 가리킵니다.

### ★ 설명서의 숫자가 프로그램과 어긋나면 시험이 잡습니다

`설명서시험.ts` **66가지.** 설명서에 적힌 단가·한도·인정시간을
**빈 자료함에서 꺼내 맞대어 봅니다.**

설명서는 한 번 쓰고 안 고치는데 단가는 바뀝니다. 그러면 둘이 다른
말을 하고, **기관은 설명서를 믿습니다** — 종이에 적힌 것이 더
그럴듯해 보이기 때문입니다.

## ③ 배포용 꾸러미

`통돌Note_문서와설치_<버전>.zip` — 문서 둘 + 설치 파일 한 쌍 +
「먼저 읽어 주세요.txt」.

**진짜 배포본**은 `통돌Note 만들기.bat` 이 만듭니다 — 95MB exe 는
윈도에서만 묶입니다. `build.ps1` 이 이제 문서 두 장과 설치 파일
한 쌍을 함께 넣고, **하나만 있으면 보내기를 멈춥니다** (짝이 안 맞으면
기관이 눌러도 아무 일이 없어 고장으로 봅니다).
